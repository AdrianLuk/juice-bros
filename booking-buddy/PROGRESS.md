# Booking Buddy — Implementation Plan

Built test-first, in vertical slices: one seam, one red test, one minimal green implementation, repeat. No horizontal slicing (no writing a batch of tests before any implementation). Refactoring happens at review time, not inside the red→green loop itself.

**Seams under test** — *revised after Phase 3, replacing the original plan. Read this before writing a test.*

The plan opened with "Server Actions are the primary seam: call the action, assert through another read action." Two phases shipped without a single test like that, because the actions kept turning out to have nothing testable in them — session check, parse the form, one Supabase call, map the error. Testing that needs a Next.js request context and a live database, and would mostly be testing Supabase. Rather than keep the rule and keep breaking it, the seams are now:

1. **Pure functions** — `node --test`, no dependencies, run constantly. Anything decidable without a database belongs here, and getting it here is a design job: input parsing and error-message mapping get *extracted out* of the actions (`friend-groups.ts`, `username.ts`, `visibility.ts`, `connections.ts`) rather than left inline and declared untestable. **If an action has interesting logic in it, that's the signal to pull the logic out, not to build a harness.**
2. **The database** — pgTAP, for anything the schema itself enforces: RLS (the ADR 0003 safety net), constraints, triggers. These query Postgres directly, on purpose.
3. **The browser** — Playwright, for journeys across several actions and both Users. This is what covers the actions end to end, and it earns its keep: it found the ambiguous member picker that two review passes missed.

What's left in a Server Action after that is glue, and glue is covered by 3.

**How to run any of this**: see [docs/testing.md](docs/testing.md) — what each suite needs running, and how to click through the app yourself.

**Test tooling**: `node --test` over `src/**/*.test.ts` for the pure functions; `supabase test db` (pgTAP) for the database; Playwright over `e2e/` for the browser. The last two need Docker and the local Supabase stack up.

**Proposed file layout** (extends the existing `src/components/apps/<slug>` convention):
- `src/app/booking-buddy/` — routes (auth-gated layout, per Q2)
- `src/lib/booking-buddy/actions/` — Server Actions (`connections.ts`, `friend-groups.ts`, `slots.ts`, `responses.ts`, `slot-links.ts`, `reminders.ts`), each colocated with its `.test.ts`
- `src/lib/booking-buddy/` — pure business-logic functions with no Next.js/DB dependency (`visibility.ts`, `capacity.ts`), colocated with tests
- `src/components/booking-buddy/` — components
- `supabase/migrations/` — schema + RLS policies (standard Supabase CLI convention)

---

## Phase 0 — Tooling & scaffolding (#3)

Not TDD (no behavior yet) — infra setup only. Tracked as issue #3.

- [x] 0.1 Install Supabase CLI, `supabase init`, confirm `supabase start` works locally (Docker required)
- [x] 0.2 Add `@supabase/supabase-js`, `@supabase/ssr`, `@tanstack/react-query` to `package.json`
- [x] 0.3 Scaffold `src/app/booking-buddy/` as its own top-level route group (Q2). Gating is **not** in `layout.tsx` — the sign-in page lives beneath that layout, so gating there would lock people out of the page they need. Instead: `src/proxy.ts` (optimistic, per Next.js 16 guidance) plus `verifySession` in each protected page/Server Action (authoritative).
- [x] 0.4 Add Supabase env vars to `.env.example`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server-only), `SUPABASE_DB_URL`. `.env.example` is now git-tracked (a `!.env.example` negation in `.gitignore`) and holds placeholders only.
- [x] 0.5 Add `test:rls` script — uses the Supabase CLI's native pgTAP runner (`supabase test db`) against `supabase/tests/*.test.sql`, separate from the default `test` script so quick runs aren't blocked on Docker being up.

### Notes carried out of Phase 0

- **Middleware is called Proxy in Next.js 16.** The file is `src/proxy.ts`, not `middleware.ts`. Next's own docs are explicit that it must not be the only line of defence, which is why the DAL (`src/lib/booking-buddy/dal.ts`) sits beneath it and coarse RLS beneath that (ADR 0003).
- **`NEXT_PUBLIC_*` must be read as literals.** Next only inlines these into the browser bundle where `process.env.NEXT_PUBLIC_X` appears verbatim; a dynamic lookup or an alias of `process.env` yields `undefined` in the browser. `readPublicSupabaseEnv` handles this — don't "simplify" it back to a dynamic read.

### Hosted environment

- [x] Schema is current as of the orgs/bookings/place_cache migration (2026-08-14). `npx supabase migration list` compares local against remote and is the quickest way to check whether that is still true — do it before assuming a hosted bug is a code bug.

- [x] Hosted Supabase project `juice-bros` (ref `zhvhddzpgxtjdyrhgsqd`, region `ca-central-1`, Postgres 17), linked to this repo via `supabase link` — so `supabase db push` targets it.
- [x] `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY` set on the Vercel project for Production and Preview, the service-role key marked sensitive. Repo linked to Vercel (`lukabaseballs-projects/juice-bros`).

  **Deviation from issue #3 as written**: the ticket said to use Vercel's Supabase *marketplace* integration. We used a directly-owned Supabase account instead, with env vars pushed via `vercel env add`. Reason: nearly every ticket from Phase 2 on ships migrations, and `supabase link` + `db push` against a project we own directly is the cleaner loop; the marketplace's benefit was a one-time env-var convenience. Same end state, different ownership.

- Project security settings chosen at creation, both reinforcing ADR 0003:
  - **Automatic RLS enabled** — every new table gets RLS on by default, so a table can't ship unprotected by accident. Expect a brand-new table to return zero rows until policies exist; that's the safety net, not a bug.
  - **"Automatically expose new tables" disabled** — tables are not granted to the Data API roles by default; each migration grants explicitly.

    In practice this means **every migration must include its own `grant`s**, or the table is invisible to the API even with RLS policies in place. Confirmed on the hosted project: `profiles` returns `42501 permission denied` to `service_role`, which was deliberately not granted, while `authenticated` (the role the app actually runs as) works because the migration grants it. Grant `service_role` only where a job genuinely needs to bypass RLS — the reminders work in Phase 8 is the first likely case.

### Outstanding — needs a human (cannot be done by an agent)

- [x] **Google Maps Platform API key for Places**, restricted to the Places API and to the app's own origins/IPs, exposed to the app as a server-only env var (never `NEXT_PUBLIC_*`). Provisioned 2026-08-14 (billing enabled on the existing Google Cloud project) and consumed by #18 — `GOOGLE_MAPS_API_KEY` in `.env.example`.

- [x] Google Cloud OAuth credentials, wired into Supabase → Authentication → Providers → Google.

  **The consent screen's publishing status does not gate Google sign-in.** ADR-0013 moved "Continue with Google" to Google Identity Services (`signInWithIdToken`) — pure OpenID Connect authentication, no consent screen, no scope request. Any Google account can sign in whether the Cloud Console screen is in Testing or In production. (This was not true of the original `signInWithOAuth` redirect flow, which is where the "only test users can sign in" note below originally came from — it's obsolete.) The Testing-mode limit still applies to the separate **Gmail-sync** client (`gmail.readonly`, a Restricted Scope — ADR-0009); see `docs/gmail-oauth-setup.md`.

- [x] **Auth URL configuration on the hosted project.** Site URL set to `https://juice-bros.vercel.app`, with `https://juice-bros.vercel.app/**`, `https://*-lukabaseballs-projects.vercel.app/**`, `http://localhost:3000/**` and `http://127.0.0.1:3000/**` on the redirect allow-list.

  This is not optional and it fails quietly: Auth ignores any `redirect_to` that is not on the allow-list and silently falls back to Site URL, so an OAuth or magic-link sign-in lands on `/` with a bare `?code=` and no route to handle it. The preview wildcard matters because every PR preview gets a fresh hostname.

  Applied through the dashboard rather than `supabase config push`: `config.toml` carries no `[auth.external.google]` section, so pushing it risks disabling the Google provider. If auth config ever moves into `config.toml` wholesale, add that section first.

- [ ] **Custom SMTP for magic-link sign-in.** Without it, GoTrue uses Supabase's built-in sender: ~2 emails/hour project-wide, members-only delivery, and the per-hour cap can't be raised. Symptom is a rate-limit error under the email field on `/booking-buddy/sign-in`. Fix and the rate-limit knob are in [docs/magic-link-email-setup.md](docs/magic-link-email-setup.md) — Resend SMTP reusing the already-verified domain, then Authentication → Rate Limits.

### Local vs hosted

`.env` points at the **local** Docker stack (`127.0.0.1:54321`) and should stay that way — `npm run test:rls` and day-to-day development run against local Postgres. The hosted project is only used by deployed environments.

### Deferred follow-up

- [x] Booking Buddy is now the first entry in `src/data/apps.ts` (launched 2026-08-26) — the lead card on `/tools`, linking to `/booking-buddy`. `/booking-buddy` serves a public marketing page to signed-out visitors and the dashboard to signed-in ones (session branch in `page.tsx`; `requiresSession` returns false for the exact root, every nested route still gated, `verifySession` still runs before the dashboard renders). Landing page: `src/components/booking-buddy/landing/`, in `sitemap.xml` and crawlable via `robots.ts`. The signed-in-only feature routes and `/s/` stay disallowed.

## Where to pick up

**[#348](https://github.com/AdrianLuk/juice-bros/issues/348) (consolidate duplicate Import Candidates across email + calendar feed) is shipped.** A User with both a Mailbox Link and a Calendar Feed for the same facility saw one reservation twice on the "Sync bookings" screen — an email `import` card (with Player(s)) and a feed card (no players, thinner court text). The confirm-time duplicate guard already stopped a second Booking, so this was a review-screen defect. Now the pair renders as **one consolidated card**, and confirming (or dismissing) it settles both sources at once.

- **New pure `mergeImportCandidates` (`merge-import-candidates.ts`)** — takes the email `ReviewItem[]` and the feed `CalendarFeedReviewItem[]`, returns `{ merged, emailItems, feedCandidates }` (the leftovers keep original order, all email kinds preserved). Match key is **Org + date + start time, not court** — the two sources genuinely disagree on court text (`"#5 - Hard"` vs `"#5"`), which is exactly why `isDuplicateBooking`'s four-field key can't do cross-source dedupe. Same "refuse to guess" posture as `matchCancellationToBooking`: an email import whose facility didn't resolve to an Org isn't merged (no key), and neither side merges when >1 candidate shares a slot key. The merged card takes the Player(s) + Details name + richer court label from the email side, the VEVENT UID / sequence / `starts_at` from the feed side. Alias-free, `node --test`.
- **`reviewCalendarFeed` (`calendar-feed-review.ts`)** — a feed event whose UID is already an `imported`, Booking-linked seen row is now re-affirmed as an auto-link **by UID**, before the court/date/time match. Without this a merged Booking saved with the email's `"#5 - Hard"` would be re-offered every sync (the feed event's `"#5"` won't court-match it); also fixes the latent case of a feed whose court wording drifts. If the linked Booking was deleted, it falls through to normal matching (re-offered, correct).
- **New `feed-events.ts`** — the `org_feed_events` upsert (row shape + `owner_id,org_id,uid` conflict key) extracted to a plain module taking the Supabase client, so both `actions/calendar-feed.ts`'s `recordFeedEvent` (now a thin revalidate wrapper) and the new merged confirm can share it. A `"use server"` file can't export a non-action helper.
- **`confirmMergedCandidate` / `dismissMergedCandidate` (`actions/email-sync.ts`)** — the union of `confirmImportCandidate` + `confirmFeedCandidate`: one `parseNewBooking` → the same confirm-time duplicate guard → `insertValidatedBooking` (email path, carries the players) → then **both** a `processed_messages` row and an `imported` `org_feed_events` row, tied to that Booking. Dismiss writes a `dismissed` row on each side. Live here (not `calendar-feed.ts`) because they need the module-private email-sync allowlist gate + provider. New `bb_sync_merged_import` analytics event — neither a pure email nor pure feed import.
- **`MergedCandidateCard` (`sync-from-email.tsx`)** + wiring in `SyncBookingsSection` — `mergeImportCandidates` runs client-side (the one place both source lists are in hand), the merged block renders above the email groups, and the card's `onResolved` clears **both** query caches. `nothingToReview` stays on the raw counts (a merged card always has a source in each, so it can't hide the empty state).
- **e2e** — `sync-bookings.spec.ts`: the old "same slot → two cards, confirm each" test rewritten as "same reservation → one consolidated card, confirmed once" (asserts the players, the "both sources" note, one Booking, an `imported`+linked feed row, and a second sync re-offering nothing); a new dismiss test (both sides settle). New `orgIdByName` helper in `e2e/support/calendar-feed.ts`.
- **Docs** — new [adr/0020-consolidate-cross-source-import-candidates.md](docs/adr/0020-consolidate-cross-source-import-candidates.md); CONTEXT.md's **Import Candidate** entry records the consolidation. Known limitation (in the ADR): an email import whose facility didn't match an Org stays a separate card.

**[#336](https://github.com/AdrianLuk/juice-bros/issues/336) (unify "Sync from Email" and "Sync facilities" into one "Sync bookings" action) is shipped.** The deferred follow-up ADR-0019 named — revisited now that both #288 (Calendar Feed) and #280 (Outlook) have landed. The two separate review sections on the Bookings page collapse into one.

- **New `SyncBookingsSection` (`sync-bookings.tsx`)** — one "Sync bookings" button, one review list, runs both sources. Holds both TanStack Queries (`email-sync-candidates`, `facility-feed-candidates` — same keys as before) behind a single `hasSynced` toggle; each query is additionally `enabled`-gated on its own source being available (`emailConnected` = allowlist/link *and* a connected provider; `hasConfiguredFeed`). One click fires whichever are configured; the merged list renders the email `ReviewItemGroups` and the feed `FeedCancellationCard` / `FeedCandidateCard` cards together (the "separate components, same shell" choice from #295, kept). Per-source failure reporting stays legible: an email error line, the reconnect prompt, and one dashed-border alert per un-fetchable feed all render side by side, and one source failing never hides the other's candidates. The one shared "No new bookings found." empty state replaces the two.
- **`sync-from-email.tsx` / `sync-facilities.tsx`** are now card libraries — the `SyncFromEmailSection` / `SyncFacilitiesSection` wrappers (and their own queries) are deleted; `ReviewItemGroups`, `FeedCandidateCard`, `FeedCancellationCard` are exported. `ReviewItemGroups` renders `null` when empty (the section owns the empty state now).
- **`bookings/page.tsx`** — one `<SyncBookingsSection>` gated on `canSyncFromEmail || hasConfiguredFeed`, replacing the two `<section>`s. `mailboxProvider` still passed through for the reconnect prompt; an allowlisted-but-unconnected User with no feed gets a "connect a mailbox in Settings" nudge and no button, and one who *also* has a feed keeps the button plus a lighter "connect a mailbox to sync from email too" line (review fix — the nudge was previously lost whenever a feed existed).
- **Failure surfacing** — a sync query that *rejects* (a thrown `verifySession` redirect, an uncaught adapter fault) now shows an error line, not the false "No new bookings found." the first cut rendered (review fix): `emailError` / `feedError` fall back to a generic message on `query.isError`.
- **Analytics unchanged** — `bb_email_sync_run` and `bb_facility_sync_run` stay separate event families (the issue's open question). Both still fire on one "Sync bookings" click, one per source that ran; the per-source dimensions (`provider`, feed/error counts) are cleaner than a single event with a `source` discriminator. Comment in `analytics.ts` records the decision.
- **Confirm-time dedupe** — email + feed candidates for the same slot still resolve to one Booking on confirm (either order), unchanged: the guard is server-side in `confirmImportCandidate` / `confirmFeedCandidate` against the live Bookings, not against what's on screen.
- **e2e** — new `sync-bookings.spec.ts` (Ben, allowlisted, both mocks running: Gmail 5603 + ICS 5605): one section/one button replaces the two; one source failing (feed 500) still shows the email candidate with the failure named; email + feed candidates for the same slot → one Booking on confirm. `calendar-feed.spec.ts` updated for the new heading/button ("Sync bookings"), the separation test rewritten as a merge test. `sync-from-email-scenarios.ts` button name updated ("Sync bookings"), all Gmail + Outlook journeys unchanged otherwise. `e2e/support/calendar-feed.ts` helpers point at the new section.
- Verified: `node --test` 910 green; `npx tsc --noEmit` (after `next typegen`) + `npm run lint` + `npm run build` clean. No migration, no schema change, no parser/ICS/confirm-behaviour change beyond the merge.

**[#296](https://github.com/AdrianLuk/juice-bros/issues/296) (Calendar Feed: feed-diff cancellation + safety rails — #288's Calendar Feed series, PR 6) is shipped.** The last piece of the Calendar Feed feature: a reservation that vanishes from a feed (or goes cancelled) and maps to a logged future Booking now surfaces as a cancellation candidate; confirming removes that Booking.

- **`reviewCalendarFeed` (`calendar-feed-review.ts`) gains the `cancellation` kind + all four rails.** New `CalendarFeedCancellationItem` type, new `cancellations` + `feedLooksWrong` on `ReviewedCalendarFeed`. `SeenFeedEvent` now carries `startsAt` + `bookingId` (both already on the `org_feed_events` row). The diff: for each `imported`, Booking-linked seen row — if still in the feed with an explicit cancelled status → flag unconditionally (rail 3); if vanished, and its start is still future *and* ≥ the earliest event the feed still shows → flag (rail 2). Rail 1 (healthy-fetch gate) is the caller's; rail 4 (sanity cap) suppresses all candidates and sets `feedLooksWrong` when the count exceeds `CANCELLATION_ABSOLUTE_CAP` (3) or, once there are more than 3 tracked Bookings, more than half of them. `unreadableUids` for the sync are passed in and treated as still-present so a one-sync parse gap never reads as a vanish. Rail 2's floor is the earliest *future* present event, so a stale past event lingering in the feed can't disable it; rail 4's >50% trigger needs ≥2 candidates so an ordinary lone cancellation still comes through. 22 pure tests (was 9).
- **`syncOneFeed` (`actions/calendar-feed.ts`)** — rail 1: a 2xx body that parses to zero usable events (empty calendar, non-calendar junk, every VEVENT unreadable) returns a per-Facility sync error and runs no diff. Selects `starts_at, booking_id` on the seen-set read, threads `cancellations` / `feedLooksWrong` through `FacilityFeedResult`.
- **New `confirmFeedCancellation` action** — re-verifies the `org_feed_events` row is `imported` and still linked to the same `booking_id` the form names (not trusting the hidden field), deletes the Booking via `deleteOwnedBooking`, marks the seen row `dismissed` + `booking_id` null so a later sync doesn't re-flag it. New `bb_facility_sync_cancellation` analytics event.
- **`SyncFacilitiesSection` (`sync-facilities.tsx`)** — new `FeedCancellationCard` (mirrors the email sync's `CancellationBody`: no Org picker, "Remove booking" / "Keep booking"), a "this feed looks wrong — check the URL" warning block for `feedLooksWrong`, cancellations rendered above import candidates.
- **e2e** — 3 new cases in `calendar-feed.spec.ts` (10 total): the vanish→cancellation→confirm→Booking-removed path, the unhealthy-fetch (500 + empty-body) no-op, and the sanity cap showing the warning instead of removing 5 bookings. `futureEvent(n)` helper for distinct multi-event feeds.
- Verified: `node --test` 910 green (+22); `npx tsc --noEmit` (after `next typegen`) + `npm run lint` + `npm run build` clean. `calendar-feed.spec.ts` 10/10 green (`--workers=1`). No migration (schema was #293). Full e2e suite not re-run — no shared selector/behaviour change outside the feed section.

**[#295](https://github.com/AdrianLuk/juice-bros/issues/295) (Calendar Feed: Facility-form field + "From facility feeds" review section — #288's Calendar Feed series, PR 5) is shipped.** The user-facing surface on top of #294's backend actions. The test-only `/api/e2e/calendar-feed` bridge route is deleted — the spec drives real UI now.

- **Facility edit form** (`orgs.tsx` → `CalendarFeedForm`, one per `OrgRow`) — an "Import from a calendar feed" URL field with help text pointing at CourtReserve ("your name → Calendar feed"). Save goes through `setCalendarFeedUrl`; the save-time validation error (bad format / non-CourtReserve host) renders inline via the shared `ActionError` and nothing is stored. A configured feed collapses to "A feed is configured for this facility." + a "Remove feed" control wired to `clearCalendarFeedUrl`. Keyed on `org.hasCalendarFeed` so a save/clear remounts the form into the right state after `revalidatePath`.
- **`Org.hasCalendarFeed`** (`actions/orgs.ts`) — `listOrgs` now selects `calendar_feed_url` and maps only its presence to a boolean; the ciphertext never leaves the server (ADR-0019). `setCalendarFeedUrl` / `clearCalendarFeedUrl` now `revalidatePath(BOOKINGS_PATH)` too, so the Bookings-page section appears/disappears immediately.
- **`CalendarFeedReviewItem.startsAt`** — the parsed start instant is now carried on the review item (`reviewCalendarFeed` already had `startInstant` in scope; it was previously only reachable via a separate `events` lookup in `syncOneFeed`). `FeedCandidateCard`'s confirm/dismiss forms post it as `starts_at`, which `confirmFeedCandidate` / `dismissFeedCandidate` already parse (their inline `startsAtRaw` guard, unchanged from #294) onto the `org_feed_events.starts_at` column.
- **Bookings page** — a new `SyncFacilitiesSection` (`sync-facilities.tsx`), its own `<section>` adjacent to but separate from "Sync from Email" (never merged into one list). Renders only when the User has ≥1 feed-configured Facility (`hasConfiguredFeed` boolean, derived in `page.tsx`); **not allowlist-gated**. A "Sync facilities" button (click-triggered TanStack Query, same `enabled` pattern as `SyncFromEmailSection`) calls `syncFacilityFeeds`. A per-Facility fetch error renders as a dashed-border alert naming the Facility, with the backend's own reason on a second line. Import candidates render through `FeedCandidateCard`, which reuses the detail-line formatters (`formatCandidateDate` / `formatCourtLabel` / `formatTimeLabel` / `BOOKING_FORMAT_LABEL`), `OrgSelect`, and the `bb-card` shell class the email `ReviewItemCard` uses — but is its own card (the email one switches over three kinds and carries no feed-event key). Confirm → `confirmFeedCandidate`, Dismiss → `dismissFeedCandidate`; the query cache drops the resolved item optimistically.
- **`useResolveOnSuccess`** extracted to `use-resolve-on-success.ts` and shared by both sync sections (it was about to be a verbatim second copy). `ActionError` stays a per-component local — it already is in ~12 components, and consolidating all of them is a separate cleanup.
- **e2e** — `calendar-feed.spec.ts` rewritten against real UI (7 tests, run as non-allowlisted Amy): invalid-URL inline rejection + no save; paste → Sync facilities → confirm → a Booking carrying facility/date/time/court/format + auto-link on resync; dismiss → resync never re-shows; clear → field empty and a later sync (with another feed still configured) never re-fetches the cleared one; per-Facility 500 names the Facility and doesn't stop the good feed; the field and section available to a non-allowlisted User; the section is separate from "Sync from Email". `e2e/support/calendar-feed.ts` keeps the direct-Postgres seeding helpers and gains page helpers; `FeedBridge` and `src/app/api/e2e/calendar-feed/route.ts` are deleted.
- Verified: `node --test` 892 green (one `calendar-feed-review.test.ts` assertion updated for `startsAt`); `npx tsc --noEmit` + `npm run lint` + `npm run build` clean; `npm run test:rls` 490 green (no schema change). `e2e/calendar-feed.spec.ts` 7/7 green PW-managed; `email-sync` / `orgs` / `bookings` specs re-run green (Org type + Bookings page changed). No migration.

**Issues #5 (Friend Connections) and #6 (Friend Groups & Visibility) are both closed out.** #5 is closed on GitHub, all six criteria checked. #6's seven acceptance criteria are all met and checked off on GitHub, but **it is deliberately left open**: the ticket's "What to build" line asks for the resolved visibility to be "actually enforced elsewhere in the app," and nothing enforces it yet — `resolveVisibility` has exactly one caller, `getGroupsPageData`, which renders the level as text on the groups page. There is nothing to enforce it against until a Slot exists. Enforcement is Phase 5's Slot RLS and Phase 6's `respondToSlot` guard, i.e. issue #8, which already lists #6 as a blocker. See the comment thread on #6 for the fuller version.

Verified: 116 `node --test` tests, 57 pgTAP tests, and 24 Playwright browser tests all pass; typecheck, lint and `npm run build` are clean. Migrations are deployed — `supabase migration list` shows all six in sync between local and the hosted project.

Everything that was outstanding from #5 and #6 is now done:

- [x] Remove-friend confirmation dialog, verified in a browser including that cancelling leaves the Connection untouched (`e2e/friends.spec.ts`).
- [x] Two-User flows (send/see/accept, decline/re-send) have browser coverage using two separate contexts (`e2e/friends.spec.ts`).
- [x] Username changes: `/booking-buddy/settings`, format rules mirroring the database, uniqueness left to the index (`e2e/settings.spec.ts`).
- [x] `supabase db push` — done, confirmed against the hosted project.
- [x] Test seam decision — see "Seams under test" at the top of this file and the Phase 3 notes below: pure functions get `node --test`, the database gets pgTAP, cross-action journeys get Playwright.
- [x] Booking Buddy is in `src/data/apps.ts` and linked from `/tools` (launched 2026-08-26 — see "Deferred follow-up" above).

### Notes carried out of the friends page

- **TanStack Query earns its place in exactly one spot here**: the search box, where debounced, cached, per-term fetching is the whole job. Everything else on the page is a server component reading through `listConnections`, mutated by plain `<form action={…}>` Server Actions that call `revalidatePath` — so the lists re-render from the server in the same roundtrip and the page still works without JavaScript. Don't "unify" these onto one mechanism; they are different problems.
- **`connections` references `auth.users`, not `public.profiles`**, so PostgREST has no relationship to embed across and `listConnections` reads profiles in a second query. A `select("*, profiles(...)")` will fail here.
- **Grouping lives in `src/lib/booking-buddy/connections.ts`**, deliberately free of Next.js and Supabase imports so it is unit-testable. A Connection row means different things depending on who is looking at it; that asymmetry is the logic worth testing, and it is.

**Phase 4 (issue #7, Org + Booking) is shipped and merged** ([#19](https://github.com/AdrianLuk/juice-bros/pull/19)), after being redesigned partway through: an Org is no longer a free-text name, it points at a Google Place. [adr/0005](docs/adr/0005-orgs-identified-by-google-place-id.md) is why; [Phase 4](#phase-4--org--booking) below has the steps and the notes carried out of it.

Both open decisions were settled before any code: the Google Places integration is **split into [#18](https://github.com/AdrianLuk/juice-bros/issues/18)** and search there will be **server-side, not a client autocomplete**. Reasoning is under Phase 4; don't relitigate either.

What that means for what shipped: `orgs`, `bookings` and `place_cache` all landed with the ADR 0005 shape, and the UI covers the hand-named path end to end. **No User-facing path writes a `place_id` yet** — that arrives with #18, which is blocked on a Google Maps API key only a human can provision (see "Outstanding — needs a human").

Verified: 143 `node --test` tests, 86 pgTAP tests and 29 Playwright browser tests all pass; typecheck, lint and `npm run build` are clean. Migrations are deployed — `supabase migration list` shows all eight in sync between local and the hosted project.

Note that the orgs/bookings migration went **to the hosted project before #19 merged**. It is additive — three new tables, nothing existing altered, and no deployed code touches them — so it could not break what's live. But the "rewrite it in place" freedom this phase used up is gone, and it was spent within the hour: the review found `orgs` had no index its RLS filter could use and `bookings.org_id` none for its cascade, which had to land as `20260814183000_index_orgs_and_bookings.sql` rather than as an edit.

### What the review caught

Worth reading before Phase 5 repeats any of it:

- **`Intl.supportedValuesOf("timeZone")` is not the same list on the server as in the browser.** Node 24 here returns 418 zones with the *legacy* spellings only — `Asia/Calcutta`, `Europe/Kiev` — and no `UTC` at all, while browsers report the canonical ids. Matching the detected zone strictly against the server's list meant a Chrome user in India detected as `Asia/Kolkata` found no match, got the disabled placeholder, and was blocked by `required` from submitting a form whose list never held their zone under a name they'd look for. The select now *adds* the detected zone when the list lacks it, and the page prepends `UTC`. Postgres accepts every spelling involved, so passing the browser's through is safe. Note that `resolvedOptions().timeZone` does **not** canonicalise aliases in Node — that was the first fix tried, and it doesn't work.
- **`Intl` accepts bare UTC offsets (`+05:30`) and `pg_timezone_names` doesn't**, so `isKnownTimeZone` was green-lighting values the trigger then refused. It now requires a leading letter.
- **`23514` arrives from five different rules** — both branches of `assert_booking_coherent` and three check constraints — so mapping it all to "that isn't your place" told people with a time-zone problem to go looking at ownership. `bookingWriteMessage` reads the message now.
- **A write that RLS filtered away returns `200 []`, not an error.** The seed script accepted a pending Connection as whichever party it happened to have, and a row found in the reverse direction meant the *requester* tried to accept — filtered to zero rows, reported as "connected", pair still pending. The lesson is the one already written into the Phase 3 notes: check the row count, never the status code.

**#18 (Google Places lookup for Orgs) is done** — see its own notes under Phase 4 above.

**#20 (`time_zone` belongs on `orgs`, not `bookings`) is done too**, grabbed ahead of #8 since it was small and already unblocked. `orgs.time_zone` is `not null`; a Place-backed Org derives it from `place_cache`'s coordinates via [`geo-tz`](https://www.npmjs.com/package/geo-tz) (offline, no second Google API), asking nothing. `bookings.time_zone` is gone — `createBooking` reads the Org's zone server-side (also doubling as the ownership check) and `getBookingsPageData` renders through it. Two migrations, additive then destructive, both shipped together since nothing is deployed to real users yet.

**The hand-named path's picker was pulled back out the same session, by request**: every early User (and everyone testing with them) is in Toronto, and a time-zone question stacked on top of "I couldn't even find my club on Google" read as an unrelated speed bump for a problem that doesn't exist yet. `CreateOrgForm` no longer renders one; `parseHandNamedOrg` defaults to `DEFAULT_HAND_NAMED_TIME_ZONE` (`America/Toronto`, in `orgs.ts`) when the form sends no `time_zone` field, but still honours and validates one if a caller does send it. `TimeZoneSelect` (`src/components/booking-buddy/time-zone-select.tsx`) is kept in place, unimported, for exactly this: bringing the picker back is wiring it into `CreateOrgForm` and passing the Orgs page's `zones` list again (the pattern is still in `bookings/page.tsx`'s git history if it's easier to copy than re-derive), not rebuilding anything.

Two things worth carrying forward:

- **A bundler can rewrite `__dirname` out from under a package that reads its own data files via `fs`.** `geo-tz` ships timezone-boundary data it loads relative to its own package directory; Turbopack rewrote that to a synthetic path, so every lookup failed with a silent `ENOENT` — caught by the same try/catch meant for out-of-range coordinates, so it read as "coordinates too exotic to place" rather than the bundler bug it was. Fixed via `serverExternalPackages: ["geo-tz"]` in `next.config.ts`, which tells Next to `require()` it natively instead of bundling it. Worth checking first for any future native/fs-backed dependency that behaves correctly in a standalone `node -e` check but not inside the app.
- **A pure-logic file that's safe for `node --test` isn't automatically safe for the client bundle, and vice versa.** `isKnownTimeZone` needed to stay in `timezone.ts` because `orgs.tsx`/`bookings.tsx` import `orgs.ts`/`bookings.ts` directly for form constants — pulling `geo-tz` into that same file would drag `fs` into the browser bundle (`Module not found: Can't resolve 'fs'`). It also couldn't just move to a `server-only`-marked file instead: that package throws unconditionally outside Next's `react-server` bundler condition, including under plain `node --test`, so marking `derive-time-zone.ts` that way would have made its own logic untestable. Split into a separate file, kept out of the `server-only` marker, and relied on import hygiene (only ever imported from the Server Action) instead.

Also beyond the ticket: Booking start/end times became a `<select>` of half-hour slots rather than a free `<input type="time">`, since a hand-typed or click-dragged time picker could produce something like `6:23 PM` that no court is actually booked in.

Verified: 179 `node --test` tests, 90 pgTAP tests, and 33 Playwright browser tests all pass; typecheck, lint and `npm run build` are clean.

**#28 (Phase 4.5, Availability Window: schema + `resolveAvailability` + RLS) is shipped.** See "Notes carried out of Phase 4.5" above.

**#8 (Slot as a poll, with Responses) is shipped too.** See "Notes carried out of #8" above, under Phase 6.

**#9 (Confirm a Slot — attaching Booking(s), Capacity, over-capacity signal) is shipped**, closing Phases 5 and 6. See "Notes carried out of #9" above, under Phase 6.

**#10 (Slot Link & Guest RSVP) is implemented too** — see "Notes carried out of #10" under Phase 7 below, including the one thing left to do before merging: this session had no Docker, so the new pgTAP and Playwright coverage is unverified. **Start the next session with [#11](https://github.com/AdrianLuk/juice-bros/issues/11)** (Email Reminders), now unblocked — #23 (dashboard calendar) remains independent of both.

Verified for #8: 204 `node --test` tests, 114 pgTAP tests pass; typecheck, lint and `npm run build` are clean. 32 of Booking Buddy's Playwright specs pass, including the new `e2e/slots.spec.ts` (4 tests) — `e2e/places.spec.ts`'s 5 tests are excluded from that count for a pre-existing, documented reason unrelated to this ticket: they need a dev server Playwright starts itself to pick up the Google Places mock env var, and this session reused an already-running one (see docs/testing.md's "If you already have `npm run dev` running on :3000" note).

`supabase db push` is done for #8's migration — and it turned out not to be the only one pending. `supabase migration list` showed the hosted project was three migrations behind `master` already, from #20 and #28 (both merged, neither ever pushed): `20260815090000`/`20260815090100` (Move Booking time_zone to Org) and `20260815100000` (Availability Windows). All four went up together in one `db push`; `migration list` now shows local and remote in sync across all twelve. Worth a beat before starting #9: **check `supabase migration list` at the start of every session**, not just after writing a new migration — this drift sat unnoticed across at least one full session.

Rendering Availability on a calendar is folded into [#23](https://github.com/AdrianLuk/juice-bros/issues/23) instead of a separate UI step — #23's body now covers both Bookings and Availability, and lists #28 as a blocker (now closed).

[#23](https://github.com/AdrianLuk/juice-bros/issues/23) (Dashboard calendar + upcoming bookings list, fka Phase 9.1's placeholder) is fully spec'd and ticketed, but deliberately queued **after** #18 and #8 — it only needs Bookings, which already ship, so it isn't blocked, but the existing #18-then-#8 order was a deliberate choice and this ticket doesn't unblock anything else in the graph.

Confirm the local stack is current first: `supabase start` (Docker), `npx supabase migration up --local` if there are new migrations, `npm run seed:users`. See [docs/testing.md](docs/testing.md) for what each test suite needs.

**Work happens on a branch with a PR per ticket now**, not direct commits to `master`.

## Phase 1 — User + Auth ([#4](https://github.com/AdrianLuk/juice-bros/issues/4))

- [x] 1.1 Schema: `public.profiles` (id references `auth.users`, display_name) + trigger to auto-create a profile row on signup. Also carries `username` — see `add_username`.
- [x] 1.2 🔴 Test: inserting a row into `auth.users` results in a matching `profiles` row → 🟢 implemented as `handle_new_user()`
- [x] 1.3 Auth UI: sign-in page offering magic link, Google sign-in, and email/password. All three verified. (Google sign-in later moved from the OAuth redirect flow to Identity Services — ADR-0013 — which is why the consent screen's Testing-mode status no longer restricts it to listed test users.)
- [x] 1.4 `/booking-buddy/settings`: change your Username. Signup assigns one so nobody has to think about it, but the handle you hand out shouldn't be one an algorithm picked. Rules in `src/lib/booking-buddy/username.ts` mirror the database's; uniqueness is the index's job, not a check-then-write.

## Phase 2 — Connection ([#5](https://github.com/AdrianLuk/juice-bros/issues/5))

- [x] 2.1 Schema: `connections` (requester_id, addressee_id, status: pending/accepted, created_at)
- [x] 2.2 🔴 Test: `sendConnectionRequest` creates a pending Connection between two Users → 🟢 implement
- [x] 2.3 🔴 Test: `acceptConnectionRequest` flips status to accepted; only the addressee can accept, requester attempting it is rejected → 🟢 implement
- [x] 2.4 🔴 Test: `sendConnectionRequest` rejects a duplicate pending request between the same pair → 🟢 implement
- [x] 2.5 🔴 RLS test: querying `connections` directly as a third User (not requester or addressee) returns no row → 🟢 implement coarse policy
- [x] 2.6 Discovery: `search_users` and Usernames (ADR 0004), plus `/booking-buddy/friends` — the search box, both pending-request lists and the accepted Connections list. Ahead of Phase 9's route work because issue #5 is only demonstrable with a UI.

## Phase 3 — Friend Group + Visibility ([#6](https://github.com/AdrianLuk/juice-bros/issues/6))

- [x] 3.1 Schema: `friend_groups` (owner_id, name, default_visibility), `friend_group_members` (group_id, connection_id), `visibility_overrides` (owner_id, connection_id, level)
- [x] 3.2 🔴 Test: `createFriendGroup` creates a group owned by the caller → 🟢 implement — **tested at the database seam, not the action seam** (see the deviation note below)
- [x] 3.3 🔴 Test: `assignToGroup` rejects a Connection that isn't `accepted` yet → 🟢 implement guard — shipped as `setGroupMembership`, since adding and removing are one control with two states. Same deviation.
- [x] 3.4 🔴 Test: `resolveVisibility` — a per-friend override always wins over any group default (pure function, no DB) → 🟢 implement
- [x] 3.5 🔴 Test: `resolveVisibility` — a friend in two groups with different levels resolves to the most permissive → 🟢 implement
- [x] 3.6 🔴 Test: `resolveVisibility` — a Connection with no group and no override defaults to no access → 🟢 implement
- [x] 3.7 🔴 RLS test: querying `friend_groups`/`visibility_overrides` directly as a non-owner returns no rows → 🟢 implement coarse policy

### Notes carried out of Phase 3

- **The agreed seam moved, deliberately — see "Seams under test" above.** 3.2 and 3.3 are not tested by calling the Server Action. Their rules live in a unique index and two triggers, asserted in `supabase/tests/friend_groups.test.sql`; the input handling that *is* decidable in TypeScript was pulled out into `src/lib/booking-buddy/friend-groups.ts` and unit tested there. Adrian chose this over building an action harness: the actions left behind are four lines of glue each, and a harness would exist mostly to test Supabase.
- **The two guards live in the database, not the actions.** "Only accepted Connections are groupable" and "only into your own groups" need subqueries, so they are `before insert or update` triggers rather than check constraints — and firing on update too is what stops a row being edited into a state the insert would have refused. `setGroupMembership` deliberately doesn't re-check either; adding a TypeScript copy would just be a second place to get it wrong.
- **Membership is keyed by Connection, not by the friend's user id.** Unfriending therefore cascades the grouping away with it. A grouping of someone you are no longer connected to would otherwise sit there still granting visibility.
- **`resolveVisibilityByConnection` is driven by the friends list, not by the membership rows**, so an ungrouped friend gets an explicit `none` rather than a missing key. A caller reading "absent" as "unknown" instead of "no access" is the failure mode worth designing out.
- **Level pickers are native `<select>`s** (`visibility-select.tsx`), not the shadcn one. Every form on the page posts to a Server Action and works with no JavaScript; a native control keeps that true.
- **Every write selects its row back** and treats zero rows as a failure. RLS turns "that isn't yours" into an empty result, not an error, so a delete or an update naming someone else's group otherwise returns a cheerful `{ ok: true }` for something that never happened. The one deliberate exception is clearing a visibility override, where no row is the state the User asked for.
- **The member picker shows `Name (@username)`, not just the name.** Found by the browser tests, not by review: with two "Ben Backhand"s in the local data, an `<option>` carrying only the display name gives no way to tell which friend you are adding. `personOptionLabel` exists for one-line contexts where `PersonName`'s second line doesn't fit.
- **Beyond the ticket, on purpose**: groups can be deleted (a group you can't get rid of is a trap), names are unique per owner case-insensitively and capped at 60 characters (two "Tuesday crew"s are indistinguishable in the member picker), and the three level names were coined here and written into [CONTEXT.md](CONTEXT.md) — the issue asked for "a default visibility level" without saying what the levels are.

## Phase 4 — Org + Booking ([#7](https://github.com/AdrianLuk/juice-bros/issues/7), [#18](https://github.com/AdrianLuk/juice-bros/issues/18))

**Redesigned mid-session on 2026-08-14: an Org is Google Place-backed, not free text.** The reasoning, the alternatives rejected, and the consequences are in [adr/0005-orgs-identified-by-google-place-id.md](docs/adr/0005-orgs-identified-by-google-place-id.md); the vocabulary is in [CONTEXT.md](CONTEXT.md) under **Place** and **Org**. Read both before starting — the steps below are the shape, not the argument.

- [x] 4.1 Schema: `orgs` (owner_id, google_place_id nullable, name nullable, exactly one set), `bookings` (org_id, owner_id, court_label, starts_at, ends_at, time_zone), `place_cache` (place_id pk, name, formatted_address, latitude, longitude, fetched_at)
- [x] 4.2 🔴 Test: an Org is either Place-backed or hand-named, never both and never neither → 🟢 check constraint
- [x] 4.3 🔴 Test: the same owner cannot add the same Place twice → 🟢 unique index on `(owner_id, google_place_id)`
- [x] 4.4 🔴 Test: `createBooking` requires an existing Org owned by the caller → 🟢 implement (trigger — the rule needs a subquery, and RLS does not cover it: the insert is on `bookings`, a table the User may write, and nothing in that policy looks at whose Org they named)
- [x] 4.5 🔴 RLS test: querying `bookings` or `orgs` directly as a non-owner returns no rows — including as an *accepted Connection*, since a Booking reaches a friend only through an attached Slot → 🟢 implement coarse policy
- [x] 4.6 🔴 RLS test: `place_cache` is readable by any authenticated User and writable by none → 🟢 select-only grant, writes via `service_role`
- [x] 4.7 🔴 Test: Place lookup — search returns candidates, picking one caches it, a stale row is refreshed → 🟢 implement — shipped in [#18](https://github.com/AdrianLuk/juice-bros/issues/18), see its notes below
- [x] 4.8 "Powered by Google" attribution on the Org picker and anywhere a Place's address is shown. Required by Google's terms, not optional. — shipped in [#18](https://github.com/AdrianLuk/juice-bros/issues/18)

Beyond the numbered steps, #7 also shipped the UI its acceptance criteria ask for: `/booking-buddy/orgs` (add a hand-named place, see your places, remove one) and `/booking-buddy/bookings` (log a court reservation, see them soonest-first, remove one), both linked from the dashboard.

### Both decisions settled (2026-08-14)

1. **The Places integration is split out — #7 lands the schema, [#18](https://github.com/AdrianLuk/juice-bros/issues/18) brings the picker.** 4.1–4.6 plus the hand-named Org path ship in #7; 4.7–4.8 are #18. The schema is the expensive-to-change part and it's what unblocks Phase 5, and shipping the picker inside #7 would have blocked the whole ticket on a Google Maps API key that doesn't exist yet (see "Outstanding — needs a human"). The consequence is stated in #7 and worth repeating: until #18 ships, the only Orgs creatable through the UI are hand-named ones. `google_place_id`, its uniqueness rule and `place_cache` all land here and are pgTAP-covered, so the shape is settled — but no User-facing path writes a `place_id` yet.
2. **Place search runs server-side, not as a client autocomplete.** A Server Action calls Places Text Search, renders the candidates, and the User picks one in a second step. Every Booking Buddy form posts to a Server Action and works with JavaScript off — that property is why the visibility pickers are native `<select>`s — and a keystroke-driven autocomplete gives it up. It also keeps the API key server-side by construction rather than by discipline. Two steps instead of type-ahead is the accepted cost. Recorded on #18, which is where it gets built.

### Notes carried out of Phase 4

- **The migration and its pgTAP test were rewritten in place**, not stacked on with an `alter table` — they had been applied locally before the redesign but never pushed, so there was no history worth preserving. Most of the original survived: `bookings`, the `assert_booking_coherent` trigger, the `ends_at > starts_at` constraint, the coarse RLS policies and the explicit grants. What changed is `orgs` and the addition of `place_cache`. Rewriting in place is why the local database needed `supabase db reset` — `migration up` cannot undo a version of a migration that no longer exists on disk.

- **`orgs.google_place_id` is deliberately not a foreign key to `place_cache`.** The cache is a cache: a row can be missing or stale, and ADR 0005 names the cache miss as a failure mode the read path has to cope with anyway. An FK would make an Org uninsertable until the server had fetched the Place, coupling the table to the Places integration rather than to the Place itself. The consequence to know about: PostgREST has no relationship to embed across, so `listOrgs` reads the cache in a second query — a `select("*, place_cache(...)")` will fail here, the same way it does on `connections`.

- **A failed cache read is not a failed page.** `listOrgs` logs it and renders every Org without its Place's name, which is the degradation ADR 0005 asks for. `orgDisplayName` returns "Facility details unavailable" rather than a placeholder that reads like a real name. This is the one read in Booking Buddy that tolerates its own failure instead of throwing, and the reason is that the User's own rows are all still there.

- **`time_zone` on `bookings` was not in the original plan and earns its place.** `starts_at` is an instant; rendering it back as the wall-clock time on the facility's own booking screen needs to know which clock that was, and without it the server renders in its own zone — UTC, in production, which turns a 6pm court booking into 10pm. The form sends an IANA zone, Postgres does the DST-aware conversion itself (`'2026-08-20 18:00:00 America/Toronto'::timestamptz`), and the trigger rejects a zone Postgres doesn't recognise. `e2e/bookings.spec.ts` logs a booking in `Asia/Tokyo` and asserts it reads back as the same six o'clock.

- **The time zone is a visible control, not a hidden field.** A hidden input filled in by script would have been simpler and would have broken with JavaScript off, which every other Booking Buddy form survives. Instead the native `<select>` carries the full zone list, and script only *preselects* the browser's zone — read through `useSyncExternalStore` rather than `useEffect`, which the `react-hooks/set-state-in-effect` lint rule refuses. The list is passed down from the server so both renders agree on it: Node's ICU and the browser's are free to disagree about which zones exist, and that would be a hydration mismatch on a 600-option list.

- **`npm run seed:users` now seeds Connections too**, because it turned out not to be true that `db reset` + `seed:users` restores a working local environment — the friendships the browser tests assume were created by hand once and died with the reset, taking five unrelated tests with them. They are seeded as the Users themselves rather than with the service-role key: `connections` is granted to `authenticated` and nobody else, and widening a grant in production to make a local fixture convenient is the wrong trade. See [docs/local-test-accounts.md](docs/local-test-accounts.md) for which pairs and why.

- **Removing a place and removing a booking both sit behind a confirmation dialog**, and neither was in the ticket. Same reasoning as the Friend Group delete: an entry you cannot get rid of is a trap, and a mistyped one would sit in the Booking form's picker forever. The dialogs say plainly that this only changes what Booking Buddy knows — the actual court reservation is untouched.

**#18 (Google Places lookup for Orgs) is shipped**, PR opened against master from branch `google-places-org-lookup`. 4.7 and 4.8 above are it. No schema change and no new pgTAP — #7 already landed everything this ticket writes to.

### Notes carried out of #18

- **Places API (New)**, not the legacy `maps.googleapis.com/maps/api/place/*` surface — `POST /v1/places:searchText` for search, `GET /v1/places/{id}` for the Details refresh, both under `https://places.googleapis.com`. Google's current recommendation for a fresh key; if the wrong product turns out to have been enabled in Cloud Console, it's a one-file swap (`src/lib/booking-buddy/google-places-client.ts`).
- **One `PLACE_CACHE_TTL_DAYS` (30), for the whole `place_cache` row, not just coordinates.** ADR 0005 gives a hard number only for coordinates and no caching exception at all for name/address; using the stricter number everywhere means one staleness check instead of two that could drift apart.
- **Refresh happens at two touchpoints, never on every render** — the ADR 0005 constraint that a cache-miss page still has to render. `pickPlace` (`src/lib/booking-buddy/actions/places.ts`) refreshes synchronously, since the User is already waiting on a network round trip. `listOrgs` schedules a refresh for whatever's stale via `after()` (`next/server`) — runs after the response is already sent, so a Google outage can never slow down or fail the Orgs/Bookings pages. Both funnel through `ensureFreshPlaceCache` in `src/lib/booking-buddy/place-cache.ts`.
- **A failed refresh drops coordinates rather than leaving them stale past 30 days.** Satisfies "refreshed or dropped" literally: success overwrites the whole row; failure on a row already past the TTL nulls just `latitude`/`longitude` (via the new `service_role` admin client, `src/lib/booking-buddy/supabase/admin.ts` — the second use of that key, after `place_cache`'s own writes in #7). Name/address stay, since nothing renders coordinates yet (that's #20) and `orgDisplayName` still needs something to show.
- **A `place_id` that stops resolving (`not_found`) is handled differently depending on who's asking.** At pick time it's a blocking, actionable error ("search again") — the id came from a live search moments earlier, so a 404 is worth surfacing. In the background refresh of an existing Org it's silent: the last-known name/address stays, same as any other cache-miss degradation.
- **The trust boundary on the pick form is `place_id` only.** `place_cache` is shared across every User, so a candidate's name/address is never trusted back from a hidden field — the server re-derives it itself (cache or a fresh Details call), the same reasoning ADR 0005 gives for why the table isn't User-writable.
- **The hand-typed `CreateOrgForm` moved into a `<details>` disclosure** on the Orgs page ("Can't find your club?"), still reachable with JavaScript off — a native element rather than a client-side toggle, same posture as the rest of Booking Buddy's forms.
- **Mocking a server-side `fetch` in Playwright needs its own seam**, since `page.route()` only intercepts requests the *browser* makes. `GOOGLE_PLACES_API_BASE_URL` (server-only, optional, defaults to the real API) lets `playwright.config.ts`'s `webServer.env` point a freshly-started dev server at a local fixture (`e2e/support/google-places-mock.ts`, fixed port so the URL can be baked into config ahead of time). That `env` block only takes effect when Playwright starts the server itself — `reuseExistingServer: true` means a dev server already running on :3000 keeps its own `.env` (the real Google host). CI always gets the override since nothing is already listening; a local run of `e2e/places.spec.ts` only gets it if `npm run dev` wasn't already up when `npm run test:e2e` started.
- **The mock's writes to `place_cache` are real, committed rows in the local database** — and that broke a pgTAP assertion (`orgs_and_bookings.test.sql`, "a User with nothing of their own can still read the Place cache") that assumed the table was otherwise empty, since nothing had ever populated it before this ticket. `e2e/places.spec.ts` now sweeps every `place_id` its mock cached via a direct `service_role` delete (`deleteCachedPlaces` in the mock support file) in its own `afterAll`, using the same published local demo key `scripts/seed-booking-buddy-users.mts` already uses — the app itself has no such action (ADR 0005: nothing evicts a cached Place on purpose), so this is test-only cleanup, not a UI path. Worth remembering for any future ticket that writes real `place_cache` rows from a browser test.

Verified: 171 `node --test` tests, 86 pgTAP tests, and 33 Playwright browser tests all pass; typecheck, lint and `npm run build` are clean.

## Phase 4.5 — Availability Window

Inserted ahead of Slot so Slot's `calendar`-visibility RLS (5.6) enforces against real data instead of a stub. See [adr/0006-availability-layered-precedence.md](docs/adr/0006-availability-layered-precedence.md) and [CONTEXT.md](CONTEXT.md) under **Availability Window**. Numbered 4.5 rather than renumbering every phase after it.

- [x] 4.5.1 Schema: `availability_windows` (owner_id, type: open|busy, starts_at, ends_at) — no uniqueness/overlap constraint (ADR 0006)
- [x] 4.5.2 🔴 Test: `resolveAvailability(ownerId, at)` — a Booking or confirmed Slot covering `at` returns busy regardless of any Availability Window (pure function over pre-fetched rows) → 🟢 implement
- [x] 4.5.3 🔴 Test: `resolveAvailability` — with no covering Booking/confirmed Slot, the most recently *created* Availability Window covering `at` wins → 🟢 implement
- [x] 4.5.4 🔴 Test: `resolveAvailability` — a moment covered by neither returns unspecified → 🟢 implement
- [x] 4.5.5 🔴 Test: creation order, not edit order, decides precedence — editing an older window's time range/type doesn't change which window wins an overlap it didn't already win → 🟢 implement
- [x] 4.5.6 🔴 RLS test: querying `availability_windows` directly as a User with less than `calendar` Visibility into the owner (including no Connection at all) returns no rows → 🟢 implement coarse policy (nuanced precedence stays app-layer per ADR 0003, same as Slot)

Rendering the User's own resolved Availability on a calendar grid is **not** built here — it's folded into [#23](https://github.com/AdrianLuk/juice-bros/issues/23) (Dashboard calendar + upcoming bookings list), which already builds that grid for Bookings. Viewing a *friend's* resolved Availability (through `calendar` Visibility) has no surface at all yet and no ticket — deferred until scoped.

4.5.1–4.5.6 are filed as [#28](https://github.com/AdrianLuk/juice-bros/issues/28), now shipped.

### Notes carried out of Phase 4.5

- **This is the first table whose read policy isn't pure ownership.** Every RLS policy through Phase 4 was "yours and nobody else's" (ADR 0003's example verbatim); `availability_windows` is gated on `calendar`-level Visibility specifically, per CONTEXT.md's Availability Window entry, so the coarse policy has to know the resolved level, not just who owns the row. `has_calendar_visibility(owner_user, viewer_user)` (the migration) replicates the override-then-group-default precedence `visibility.ts` already implements — but only as far as answering "is it `calendar`", since `calendar` is the top of `visibility_level`'s order and there's nothing more permissive to reduce over. The `resolveAvailability` pure function still owns the actual layered read (Booking/Slot busy-wins, then most-recently-created window); nothing about *that* moved into SQL.
- **`throws_ok` is the wrong assertion for an RLS-filtered write.** An `update`/`insert` that RLS blocks matches zero rows rather than raising — the same lesson Phase 4's notes already recorded for reads. First draft of the pgTAP test asserted a calendar-visible friend's `update` with `throws_ok` and it failed for the right reason (no exception, because the row was just silently unmatched); fixed by asserting the row's value is unchanged instead.
- **No `resolveAvailability` caller exists yet.** There's no Booking/confirmed-Slot data source wired to it (Slot doesn't exist until #8) and no UI (that's #23). The function takes pre-fetched `busyIntervals`/`windows` arrays rather than an `ownerId` — matching `resolveVisibility`'s shape, where the owner-scoping is the caller's query, not the pure function's job.

Verified: 183 `node --test` tests, 100 pgTAP tests pass; typecheck, lint and `npm run build` are clean.

## Phase 5 — Slot (poll → confirmed lifecycle, ADR 0001)

*Split across two issues, not a clean 1:1*: 5.1 (the `slots` half)/5.2/5.6 are [#8](https://github.com/AdrianLuk/juice-bros/issues/8) (bare-proposal Slot creation + Slot RLS); 5.1 (the `slot_bookings` half)/5.3/5.4/5.5 are [#9](https://github.com/AdrianLuk/juice-bros/issues/9) (attaching Booking(s) + Capacity).

- [x] 5.1 Schema: `slots` (owner_id, proposed_start, proposed_end, rotation_buffer default 0), `slot_bookings` (slot_id, booking_id) join table for multi-Booking Slots (Q5d) — the `slots` half only; `slot_bookings` is #9's
- [x] 5.2 🔴 Test: `createSlot` with no Booking creates a bare proposal → 🟢 implement
- [x] 5.3 🔴 Test: `attachBookingToSlot` links a Booking; only the Slot owner can attach → 🟢 implement — covered at the database seam (`slot_bookings.test.sql`) and the browser seam (`slots.spec.ts`), not as a pure-function test; the action itself is glue
- [x] 5.4 🔴 Test: `computeCapacity` — base capacity sums attached Bookings' court capacities, plus rotation buffer (pure function) → 🟢 implement
- [x] 5.5 🔴 Test: `computeCapacity` — a Slot with zero Bookings returns unbounded/null (nothing to enforce yet) → 🟢 implement
- [x] 5.6 🔴 RLS test: querying `slots` directly as a User with no Connection to the owner and no valid Slot Link token returns no rows → 🟢 implement coarse policy (nuanced precedence stays app-layer per ADR 0003 — this only proves the coarse boundary). Shipped as `has_slot_visibility` + `can_access_slot` — the Slot Link token half of this test doesn't exist yet (#10); today's boundary is Connection-and-Visibility only.

## Phase 6 — Response

*Also split*: 6.1/6.2/6.3 are [#8](https://github.com/AdrianLuk/juice-bros/issues/8) (Responses on a bare-proposal Slot); 6.4/6.5 are [#9](https://github.com/AdrianLuk/juice-bros/issues/9) (the over-capacity signal, since it needs Capacity from that ticket).

- [x] 6.1 Schema: `responses` (slot_id, user_id nullable, guest_name nullable, answer: yes/no/maybe, created_at) with a check constraint requiring exactly one of user_id/guest_name
- [x] 6.2 🔴 Test: `respondToSlot` records yes/no/maybe for a Connection with resolved visibility into the Slot → 🟢 implement — **deviation from "calling `resolveVisibility` as a guard"**, see the notes below
- [x] 6.3 🔴 Test: `respondToSlot` rejects a User with no visibility into the Slot → 🟢 implement
- [x] 6.4 🔴 Test: `respondToSlot` succeeds even when confirmed "yes" Responses already exceed Capacity — no hard block (documents the ADR 0001 consequence explicitly) → 🟢 implement (no blocking logic needed, but the test proves it) — see the notes below: there is no such test, because there is nowhere for one to fail
- [x] 6.5 🔴 Test: `isOverCapacity` returns true once "yes" Responses exceed `computeCapacity`'s result (pure function, drives the organizer-facing warning) → 🟢 implement

### Notes carried out of #8

**Issue #8 (Slot as a poll, with Responses) is shipped.** All six of its acceptance criteria are met: a bare-proposal Slot can be created with no Booking; a visible Connection can respond and change their response; a Connection without visibility is rejected; Responses are visible to the owner and other visible Connections; the `slots` RLS boundary is pgTAP-tested; and the one dedicated UI-seam test (tapping a response shows an optimistic state before the server confirms it) is in `e2e/slots.spec.ts`, built on TanStack Query per CLAUDE.md's "Slot Responses" carve-out.

- **`respondToSlot` doesn't call `resolveVisibility` in TypeScript, and can't.** The plan's 6.2 line assumed it would, but `resolveVisibility` needs the Slot owner's Friend Group memberships and overrides, and those tables are owner-only per Phase 3's own RLS (`visibility_overrides`/`friend_groups` are readable only by their owner) — the *responder* can never read them to compute the answer themselves. What actually enforces "no visibility, no response" is `can_access_slot`, a SQL mirror of `resolveVisibility`'s override-then-group precedence exposed as a `security definer` function (the same pattern Phase 4.5 already established for `has_calendar_visibility`, extended here to "at least `slots`" via `has_slot_visibility`). `respondToSlot` relies on the `responses` insert/update policies gating on it, and reads an RLS-filtered zero-row write as "no permission" — the same convention every other write in this app already follows.
- **A Slot's own RLS is not pure ownership**, same exception `availability_windows` set for `calendar` Visibility in Phase 4.5. `has_slot_visibility` is the "at least `slots`" version — `slots` isn't the top of `visibility_level`'s order the way `calendar` is, so it checks the override/group level is `in ('slots', 'calendar')` rather than equals a single value.
- **A bare-proposal Slot has no Org to read a clock off**, unlike a Booking (issue #20). It carries its own `time_zone` column instead — the same shape `bookings.time_zone` used to have before that ticket moved it to `orgs` — and, like every hand-named path in this app, defaults silently to `America/Toronto` rather than asking (`DEFAULT_HAND_NAMED_TIME_ZONE`, reused from `orgs.ts`) per the precedent set when the picker came back out of `CreateOrgForm`. No time-zone field appears anywhere in the Slot form.
- **Date/half-hour-time parsing moved out of `bookings.ts` into a new `datetime.ts`** the moment a second caller (Slots) needed the identical rules — `isRealDate`, `isHalfHourTime`, `HALF_HOUR_TIMES`, `formatTimeLabel`. `bookings.ts` re-exports `HALF_HOUR_TIMES`/`formatTimeLabel` so nothing importing from it had to change.
- **The `responses` unique index on `(slot_id, user_id)` is deliberately not partial** (not `where user_id is not null`, even though only non-null rows need the uniqueness — Guest rows via issue #10 don't). Postgres treats every `null` as distinct for uniqueness, so a plain index already imposes no limit on Guest rows for free, and staying plain is what lets `respondToSlot`'s `upsert(...).onConflict("slot_id,user_id")` name it as the arbiter — a partial index's predicate isn't expressible through supabase-js's upsert, and referencing one at all raises "no unique or exclusion constraint matching the ON CONFLICT specification" at runtime.
- **An INSERT that fails `with check` raises; an UPDATE filtered by `using` doesn't.** `slots.test.sql` needed both flavors in the same file: Ben inserting a Response on Amy's behalf, or Cal inserting one at all with no Visibility, are `throws_ok` (42501) — but Ben changing his own existing Response is silent, ordinary `is()`. The Phase 4/4.5 lesson ("check the row, never the status") is about the second case, not the first; conflating them was the first draft's bug.
- **`e2e/support/slot-cleanup.ts` signs in as Amy rather than using the service-role key** the way `deleteCachedPlaces` does for `place_cache`. `service_role` has no grant on `slots` at all — only `authenticated` does — and every Slot the spec creates is Amy's, so her own RLS delete policy is enough; there is no genuine production need to bypass RLS here, unlike the Places refresh job. First draft used the service-role key anyway, out of habit copied from `google-places-mock.ts`, and failed silently (`deleteSlots` didn't check the response), leaving stray rows that broke a later run's locator with a strict-mode "resolved to 2 elements" failure — worth remembering: an un-checked cleanup `fetch` can pass its own test while quietly not doing its job.
- **`getSlotResponses` is split out of `getSlotDetail`** as its own exported function/query key, not folded into one page-data call — `ResponseButtons`' optimistic mutation refetches and cache-patches just the responses half, per CLAUDE.md's "Slot Responses" TanStack Query carve-out. `getSlotDetail` composes it rather than duplicating the query.
- **Beyond the ticket's acceptance criteria**: `/booking-buddy/slots` lists both the caller's own Slots and every friend's they can see (RLS alone decides which rows come back — there's no separate "visible friends" filter in application code), and the dashboard links to it now that there's something real to link to.

**#8 is not "Slots done"** — Phase 5's `slot_bookings`/`attachBookingToSlot`/`computeCapacity` (5.3–5.5) and Phase 6's over-capacity signal (6.4–6.5) are issue #9's, which already lists #8 as a blocker (now closed). #10 (Slot Link + Guest) and #11 (Email Reminders) both build on Slots existing too.

### Follow-up: no past Slots or Bookings

Not part of #8's acceptance criteria, added afterward by request: a Slot cannot be proposed, and a Booking cannot be logged, for a date/time that's already passed. Both a Slot and a Booking represent something happening going forward — the same reasoning ADR 0002 already gives for why a Booking has no "intended" state, extended one step further to "and not a past one either."

- **Two layers, same shape as every other rule in this app**: a `before insert` trigger on each table (`slots_not_in_the_past`, `bookings_not_in_the_past`) is the authority, comparing the already-converted `timestamptz` instant against `now()` — no time-zone math needed there, Postgres already did it. A coarse, calendar-day-only pre-check (`isPastDate`/`todayInZone`, `datetime.ts`) catches the obvious case in JS first, for a friendlier error without the round trip; a same-day-but-already-passed-hour proposal slips past that pre-check on purpose and is caught by the trigger instead, translated back into the same friendly message by `slotWriteMessage`/`bookingWriteMessage` reading the exception text (`error.message?.includes("in the past")`) — the same "message says which rule fired" pattern `bookingWriteMessage` already used for org-ownership failures.
- **Scoped to `insert`, not `insert or update`, on purpose.** Neither table has an edit path in the app today, and a rule meant to stop someone *posting* something in the past has no business retroactively blocking some unrelated future update to a row whose start time has since passed (attaching a Booking to a Slot in #9, for instance, is an insert into the separate `slot_bookings` join table, not an update of `slots` itself — so this doesn't even brush up against that).
- **Where the JS pre-check runs differs between the two, and has to.** `parseNewSlotProposal` (pure function) resolves its own `timeZone` before this check ever runs, since a bare-proposal Slot has no Org to borrow one from — so the check lives right there. `parseNewBooking` has no zone available to it at all (the Org's zone isn't known until `createBooking` reads it from the database), so the equivalent check runs in the action, right after that read, not in the pure parser. Not an inconsistency — the zone genuinely isn't known in the same place for both.
- **Code review caught a real bug before it shipped**: both triggers apply to the *existing* pgTAP fixtures too, and `orgs_and_bookings.test.sql`/`slots.test.sql` had hardcoded Booking/Slot dates only 5–7 days out from "today" (2026-08-15). Nothing enforced "not in the past" before this, so those dates were fine; adding the trigger turned them into a near-term time bomb that would've started failing within the week, for every future session, on shed unrelated to this feature — the DST wall-clock instant check in particular, which depends on one of those exact rows. Fixed by moving the threatened fixture dates out to 2031, matching the distance `e2e/slots.spec.ts` already uses for its own fixtures. Worth remembering for the next rule that adds a `now()`-relative constraint to an existing table: **grep the test fixtures for dates that rule would now touch, not just the ones in the diff.**

Verified: 211 `node --test` tests, 116 pgTAP tests pass; typecheck, lint and `npm run build` are clean. `e2e/slots.spec.ts` and `e2e/bookings.spec.ts` each cover the friendly-error path.

### Notes carried out of #9

**Issue #9 (Confirm a Slot) is shipped.** All five acceptance criteria are met: the Slot owner can attach one or more of their Bookings; Capacity is the courts plus the rotation buffer; a Slot with no Bookings has no Capacity; "yes" Responses are never blocked; and the organizer sees an over-capacity signal once they exceed Capacity. This closes Phase 5 (5.3–5.5) and Phase 6 (6.4–6.5).

- **Capacity is per-Booking data, not a flat constant** — [ADR 0008](docs/adr/0008-court-capacity-is-per-booking-data.md), revised mid-ticket before shipping. The first draft made "a court holds four" a constant and left singles unrepresented; revisited on request, because singles and informal drilling (a court shared by 3+ players rotating through drills, more spots than a strict doubles reading would show) are common enough that folding them entirely into the organizer's manual read of the rotation buffer was the wrong simplification. `bookings.format` (`singles` | `doubles`, defaulting to `doubles`) now drives each attached court's own share; `computeCapacity` sums the list rather than multiplying a count.
- **`slot_bookings.format` is a deliberate copy of `bookings.format`, not a fresh read on every query.** A friend can read `slot_bookings` (gated on `can_access_slot`) but not `bookings` (owner-only), so without the copy a friend's Capacity would silently omit whatever singles courts are attached. `assert_slot_booking_coherent` writes the copy from the Booking itself — never from what the client's insert claims — the same trigger that already checked ownership; pgTAP proves a tampered `format` in the insert gets overwritten. Safe against drift only because Bookings have no edit path today; an editable Booking would need this revisited.
- **The Capacity a friend sees is the same number the organizer sees, and RLS is what makes that safe.** `slot_bookings` is readable by anyone who can read the Slot (`can_access_slot`), so the court count and formats travel; `bookings` stays owner-only, so where and which court do not. `getSlotCapacity` doesn't branch on the viewer for that — it asks for the Booking details and a friend simply gets none back, which is a harder thing to get wrong than a filter someone could forget.
- **The over-capacity signal reads the same query cache the response buttons write to.** `SlotCapacityPanel` and `ResponseButtons` share one `useQuery` key (`slotResponsesQuery`), so an optimistic "yes" moves the count and the signal together. Two independent reads of the same thing would have let the count and the ceiling disagree for a beat, which is exactly the moment the signal matters.
- **6.4 asks for a test that `respondToSlot` succeeds over Capacity — the honest place for it turned out to be pgTAP, not the action.** The action never reads Capacity, so a test at that seam proves nothing about code that doesn't exist. What can regress is the database: someone adding a "helpful" trigger later. `slot_bookings.test.sql` therefore has five Users each RSVPing "yes" to a one-court Slot and asserts all five rows land.
- **The Booking-side ownership check cannot live in RLS.** The insert is on `slot_bookings`, and the insert policy only sees the Slot; nothing in it looks at whose Booking was named. `assert_slot_booking_coherent` (security definer) is what closes that, the exact shape `assert_booking_coherent` already uses for Org ownership — and the reason attaching someone else's Booking raises `23514` rather than being silently filtered.
- **A code-review pass caught a real gap before shipping**: the first `attachable` list offered every Booking the owner had ever logged, with no relation to the Slot's own date — attaching a wildly unrelated reservation would misreport Capacity with nothing to catch it. `bookingOverlapsSlot` now filters the picker to Bookings whose window actually overlaps the Slot's proposed one; not a hard block (nothing in this app hard-blocks attach/detach), just the picker not offering nonsense. The same pass also caught `slot_bookings`' primary key permitting one Booking to back two different Slots at once (double-counted Capacity) — closed with `slot_bookings_booking_unique` — and an e2e test that submitted a Booking-creation form without waiting for it to land before navigating away, which could leave the next step with nothing to select.
- **`addPlace`/`logBooking`/`removePlace` moved out of `bookings.spec.ts` into `e2e/support/places.ts`** the moment the Slots spec needed a real Booking as a fixture — the same "extract when the second caller appears" move `datetime.ts` was in #8. `logBooking` grew an optional `format` param for the same reason.

Verified: 234 `node --test` tests, 134 pgTAP tests pass; the full Playwright suite (40 tests, including `e2e/slots.spec.ts` and `e2e/bookings.spec.ts`) passes; typecheck, lint and `npm run build` are clean.

## Phase 7 — Slot Link + Guest ([#10](https://github.com/AdrianLuk/juice-bros/issues/10))

- [x] 7.1 Schema: `slot_links` (slot_id, token unique, created_at)
- [x] 7.2 🔴 Test: `generateSlotLink` creates an unguessable token tied to one Slot → 🟢 implement (crypto-random token)
- [x] 7.3 🔴 Test: `guestRespondViaLink` records a Response keyed by `guest_name`, bypassing the Connection/visibility check when a valid token is presented → 🟢 implement
- [x] 7.4 🔴 Test: `guestRespondViaLink` logs ip/user-agent/timestamp for the abuse-detection audit trail (Q7) → 🟢 implement
- [x] 7.5 🔴 Test: `guestRespondViaLink` does not create a Connection row → 🟢 implement (assert no row added)
- [x] 7.6 🔴 Test: repeated guest RSVPs from the same IP against one Slot Link past a soft threshold get flagged/logged, not blocked (Q7) → 🟢 implement

### Notes carried out of #10

**Issue #10 (Slot Link & Guest RSVP) is implemented.** All five acceptance criteria are met: the Slot owner can generate a unique, unguessable Slot Link (`/booking-buddy/slots/[id]`'s new "Invite link" section); anyone holding it can view the Slot at `/s/[token]` with no sign-in; a Guest RSVPs by name alone and no Connection is created; every Guest RSVP is logged with IP, user agent and timestamp in `guest_rsvp_log`; and repeated attempts from the same IP past `GUEST_RSVP_SOFT_THRESHOLD` (3) are flagged in that log, not blocked.

- **The Guest path runs entirely through the admin (`service_role`) client, not a new `anon`-role RLS policy.** A Guest never gets a Supabase session, so there is no `auth.uid()` for a policy to gate on — the alternative (an `anon`-callable RPC keyed by a token read from a header or claim) would make `slot_links` a surface an attacker could hammer directly against Postgres, bypassing whatever logging or threshold the app wants to enforce. Instead `getSlotByToken`/`guestRespondViaLink` (`src/lib/booking-buddy/actions/guest-rsvp.ts`) do the token lookup themselves via `createAdminClient()` — the exact `service_role` precedent `place_cache` already set (ADR 0003) — before touching `slots`, `slot_bookings`, `responses` or `profiles`, each of which needed its own new `service_role` grant in this ticket's migration (table grants and RLS bypass are independent checks, the same lesson the `place_cache` migration's comment already carries). `slot_links` itself and the new `guest_rsvp_log` audit table stay `authenticated`-only (owner-managed) and `service_role`-only respectively — no User, including a Slot's own owner, has a read path to another Guest's IP today.
- **One Slot Link per Slot**, not one per invite. `slot_links_one_per_slot` is a unique index on `slot_id`, and `generateSlotLink` reuses the existing row rather than minting a second — CONTEXT.md already speaks of "its Slot Link", singular, and a second live token per Slot would just be a second thing to remember was shared.
- **`getSlotResponses` (Phase 6, issue #8) had been filtering Guest rows out on purpose**, with a comment pointing straight at this ticket (`user_id !== null`). That filter is gone: `SlotResponse` gained an `id` (the response row's own, since a Guest response has no `userId` to key React's list on) and `displayName` now falls back to `guest_name` when `userId` is null. `ResponseButtons`' optimistic-update path (`withResponse`) got the same `id` field, using the signed-in responder's own `viewerId` as a stand-in until the next refetch replaces it — nothing else about that TanStack Query carve-out changed.
- **The soft threshold counts all-time prior attempts from the same IP against the same link**, not a rolling window — CLAUDE.md's Guest RSVP abuse-handling decision asks for logging and soft-threshold flagging, not a rate limiter with real time semantics, so the simpler count is the honest read of what was actually specified. A missing/unparseable `X-Forwarded-For` (`clientIp` returns `null`) skips the threshold check entirely rather than guessing — an RSVP still gets logged and still succeeds, just unflagged, since there's nothing to attribute a "repeat" to.
- **A failed audit-log write does not fail the Guest's RSVP.** The `responses` insert is the one write that must succeed for the acceptance criteria to hold; `guest_rsvp_log`'s insert (and the count read before it) are best-effort, logged to the server console on failure — the same posture `listOrgs` already takes toward a failed `place_cache` read: the User-facing action succeeded, and a logging hiccup shouldn't read to a Guest as their RSVP having failed.
- **`/s/[token]` needed no proxy or layout change.** `routes.ts`'s `requiresSession` only gates paths under `BOOKING_BUDDY_ROOT`, and `/s` was never nested under it — confirmed by a test already sitting in `routes.test.ts` before this ticket started (`"the public Slot Link route is reachable without a session"`), which suggests the route's placement was decided back when Phase 9 was originally scoped. The page carries its own `robots: { index: false }`, since a Slot Link is meant for whoever holds it, not for search engines.
- **No Guest RSVP update path.** A Guest resubmitting the form inserts a second `responses` row rather than changing their first — there is no unique constraint on `(slot_id, guest_name)` the way signed-in Users get on `(slot_id, user_id)`, deliberately: two different Guests can share a name, so that constraint would either block a legitimate second Guest or require a mechanism (a per-Guest token, a browser-stored id) nothing in the ticket asked for. Out of scope here, not forgotten.
- **pgTAP (`slot_links.test.sql`) covers the `slot_links` RLS boundary — owner-only, and not reachable even through the most permissive Visibility level — and confirms `guest_rsvp_log` is unreachable through `authenticated` at all**, but doesn't (and can't usefully) exercise `service_role` itself: it bypasses RLS by Supabase's own platform guarantee, the same reason `place_cache`'s writes were never pgTAP-covered either.
- **Verified in full review, Docker available this time.** `npm run test:rls` passes (144 pgTAP tests, up from 134, across 11 files — `slot_links.test.sql`'s 10 assertions included); `npm run test:e2e` passes (43 Playwright tests, up from 40 — `e2e/slot-links.spec.ts`'s 3 specs included, no regressions elsewhere). `npm test` (245 pure-function tests), `npm run lint`, `npx tsc --noEmit`, and `npm run build` all clean too. Nothing needed fixing.

**#11 (Email Reminders) is done too** — see "Notes carried out of #11" below, under Phase 8.

**[#36](https://github.com/AdrianLuk/juice-bros/issues/36) (Booking Window Reminder) is done too** — filed and shipped in the same session, by request, ahead of #12/#13. Not in the original plan above; see "Notes carried out of #36" under Phase 8.

**Start the next session with [#12](https://github.com/AdrianLuk/juice-bros/issues/12)** (Web push + PWA installability) or [#13](https://github.com/AdrianLuk/juice-bros/issues/13) (Hardening pass) — both are unblocked. #23 (dashboard calendar) remains independent of either and still fully spec'd and ready whenever it's picked up instead.

## Phase 8 — Reminder

*Split by channel*: 8.1 (`notification_preferences`/`reminder_sends`)/8.2/8.4/8.5 (email half) are [#11](https://github.com/AdrianLuk/juice-bros/issues/11); 8.1 (`push_subscriptions`)/8.3/8.5 (push half) are [#12](https://github.com/AdrianLuk/juice-bros/issues/12), alongside PWA installability.

- [x] 8.1 Schema: `notification_preferences` (user_id, email_enabled default true, push_enabled default false), `push_subscriptions` (user_id, endpoint, keys), `reminder_sends` (slot_id, user_id, channel, sent_at) for idempotency — `notification_preferences`/`reminder_sends` shipped with #11; `push_subscriptions` shipped with #12
- [x] 8.2 🔴 Test: `getReminderRecipients` returns Users with a "yes" Response on a confirmed Slot only (has ≥1 Booking) — excludes bare proposals and Guests → 🟢 implement, matching `CONTEXT.md`'s Reminder definition exactly
- [x] 8.3 🔴 Test: `sendReminder` skips the push channel for a User with `push_enabled: false` → 🟢 implement, respecting preferences — proven as `shouldSendReminder`, a pure decision function; push delivery wired up by #12
- [x] 8.4 🔴 Test: `sendReminder` is idempotent — calling it twice for the same Slot+User+channel sends once → 🟢 implement using `reminder_sends` as a dedupe log
- [x] 8.5 Wire actual delivery: Resend for email (reuse existing integration from the marketing site), `web-push` npm package + VAPID keys for push. Scheduling via a Vercel Cron Job hitting a route handler, firing at each Slot's configured reminder offset. — both channels now shipped, email with #11 and push with #12, in the same `send-reminders` route

### Notes carried out of #11

**Issue #11 (Email Reminders) is shipped.** All four acceptance criteria are met: a Slot owner sets a reminder offset (minutes before start, defaulting to 60) right on the Slot's own detail page; only Users with a "yes" Response on a *confirmed* Slot (≥1 Booking attached) ever get one — bare proposals and Guests get nothing; delivery goes through the existing Resend integration; and `reminder_sends`' unique `(slot_id, user_id, channel)` index makes a repeat send a no-op, not a duplicate.

- **The send job (`/api/booking-buddy/send-reminders`) runs entirely through `service_role`, not a User's own session** — the same posture issue #10's Guest RSVP path established, and the one `env.ts`'s own comment predicted back in Phase 0 ("PROGRESS.md expected Phase 8's Reminder job to need this first, but writing the Place cache beat it there"). It reads across every User's Slots, Responses and preferences at once, which no single User's RLS grant would ever allow. Fired by a **Vercel Cron Job** hitting that route on a schedule (`vercel.json`), not Supabase `pg_cron` → Edge Function — this repo has no Supabase Edge Functions infrastructure yet, and staying inside the Next.js app keeps the whole feature testable the same way everything else here is (`node --test`, pgTAP, Playwright), with nothing to deploy or debug in a second runtime.
- **`isReminderDue`'s window is deliberately wide, not a narrow "this exact minute" check.** It's due from the moment `now` reaches the configured offset before `proposedStart`, and stays due all the way up to the Slot's own start. That width is what makes the job tolerate *any* cron cadence without a code change — `reminder_sends` is what actually prevents a duplicate send, not the window's width.
- **`vercel.json` is set to `"0 13 * * *"` (once daily), because that's the actual constraint**: Vercel's Hobby plan allows at most 2 Cron Jobs, each invoked at most once a day — confirmed against the account this ships to, not assumed. Corrected after first shipping this at `*/15 * * * *`, which Hobby cannot run as configured. **Upgrading later is a one-line change** — edit the schedule string back to something like `*/15 * * * *`; nothing in the route or `reminders.ts` needs to change, since the due-window design above already tolerates it. Documented in the route's own header comment, so the instruction lives next to the code it applies to.
- **Daily-only cron has a real, not merely cosmetic, consequence for short offsets.** A Reminder's due-window is only as wide as its own configured offset (as narrow as 15 minutes, per `REMINDER_OFFSET_PRESETS`). If that window opens and fully closes between two daily runs, the Reminder is silently never sent — not late, *missed*, since `isReminderDue` requires `now < proposedStart` and there's no retroactive send once a Slot has started. Worth knowing before picking a short offset today: a 1-day or 2-day offset is the only reliably-delivered choice until the cron itself runs more often than the offset being configured.
- **`shouldSendReminder` already branches on a `push` channel that sends nothing yet** (8.3's acceptance test). `notification_preferences.push_enabled` and the channel parameter both exist now so issue #12 only has to wire up delivery, not revisit this decision function.
- **A missing `notification_preferences` row means the defaults, not a signup trigger.** Unlike `profiles`, there's no `handle_new_user`-style insert wired to `auth.users` for this table — `getNotificationPreferences` (the User's own read) and the send job's batch read both resolve a missing row to `email_enabled: true` themselves. One fewer trigger to keep in sync with the signup flow, at the cost of every reader needing to know the default — worth revisiting if a third reader ever needs it independently.
- **Recipient email addresses come from the GoTrue Admin API (`service_role`'s `auth.admin.getUserById`), not a table column.** No table in this schema — `profiles` included — has ever carried a User's email; granting `service_role` a new column just for this job would duplicate what Auth already owns. Fetched per recipient rather than in bulk, since a Slot's own attendee list is small.
- **The email template (`formatReminderEmail`) is pure and unit-tested** — HTML-escaped, same discipline the contact form's `renderContactEmail` already established — but the send itself (the actual Resend call, and the cron trigger end to end) has the same coverage gap `guest_rsvp_log`'s `service_role` writes and the contact form's own Resend integration already carry: nothing here is Playwright-tested, since that would mean either mocking a third-party mail API this codebase has no fixture for, or sending a real email on every CI run. Verified instead with a manual smoke test against the real route: unauthenticated and wrong-secret requests both 401, and an authenticated request runs the full query pipeline (admin client, `slots`/`slot_bookings`/`responses`/`notification_preferences`/`reminder_sends` reads) cleanly against the local stack, returning `{ ok: true, checked: 0, sent: 0, failed: 0 }` since no near-term confirmed Slot existed in the seed data to trigger an actual send.
- **`CRON_SECRET` and `REMINDER_FROM_EMAIL` are new required env vars**, documented in `.env.example`. Neither is set on Vercel yet — **needs a human**: generate a `CRON_SECRET` and set it (plus `REMINDER_FROM_EMAIL`, reusing the already-verified Resend domain or a new `reminders@` address) on the Vercel project for Production, the same way `RESEND_API_KEY`/`CONTACT_*` already are. Until then the deployed cron job 500s safely rather than sending anything — it refuses to run unconfigured rather than falling back to an open, unauthenticated endpoint.

- **The reminder-timing control is a preset `<select>`, not a free-typed number of minutes** — changed after the first pass shipped, by request. Nobody thinks in minutes past an hour or two, so `REMINDER_OFFSET_PRESETS` (15/30 min, 1/2/4 hr, 1/2 days) and `reminderOffsetLabel` (renders any whole-minute value in the largest clean unit it divides into) replace the raw number field. The column and `parseReminderOffsetMinutes` still accept any value in `[0, 10080]` — `ReminderOffsetForm` adds the Slot's current value to the option list when it isn't one of the presets, the same "don't discard what's already there" move `TimeZoneSelect` makes for a detected zone the list doesn't carry, so a value set before this list existed still renders correctly rather than silently jumping to the nearest preset.

Verified: 261 `node --test` tests, 155 pgTAP tests pass; the full Playwright suite (46 tests, including the two new reminder specs in `slots.spec.ts`/`settings.spec.ts`) passes; typecheck, lint and `npm run build` are clean. `supabase migration up --local` applied cleanly; `supabase db push` to the hosted project is still pending — do it before considering this fully deployed.

### Notes carried out of #36

**[#36](https://github.com/AdrianLuk/juice-bros/issues/36) (Booking Window Reminder) is shipped**, filed and built in the same session #11 shipped in, by request — not part of the original plan above, so it's tracked as its own ticket rather than a Phase 8 sub-item. All five of its acceptance criteria are met: an organizer can set an **Intended Org** on a still-bare-proposal Slot; an Org can carry an optional **Booking Window** (days-before + time-of-day, both or neither); the organizer gets an email once that window opens, only while the Slot has no Booking attached; sending is idempotent (`booking_window_reminder_sends`, unique per Slot); and it's governed by its own preference, `booking_window_email_enabled`, independent of #11's `email_enabled`.

- **A second, distinct Reminder, not a variant of #11's.** Different recipient (the organizer alone, not every "yes" Responder), different trigger (a fact about the *facility*, not the Slot's own start time), different audience state (a Slot that's *still unbooked*, the opposite of #11's "confirmed only"). Kept as its own migration, its own pure-logic file (`booking-window.ts`), its own cron route (`send-booking-window-reminders`) and its own preference column — mirroring how `slot-links.ts`/`guest-rsvp.ts` stayed separate action files despite being related, rather than growing #11's code to do two jobs.
- **The trigger is a database view, not JS.** `slot_booking_windows` computes `window_opens_at` in Postgres — convert the Slot's own start to its own local calendar date, subtract the Org's lead time, combine with the Org's own opening time and time zone, let Postgres do the DST-aware conversion — the same posture every other wall-clock conversion in this app already takes (`createSlot`'s `"${date} ${time}:00 ${timeZone}"` trick). The consequence: unlike #11's `isReminderDue` (a JS predicate over a bounded fetch), the send job's query *is* the due-check (`window_opens_at <= now()`), with no JS-side window logic at all.
- **The view is deliberately not `security_invoker`.** First draft made it one, on the theory that RLS should evaluate as the querying role — reasonable-sounding, and wrong here: `service_role` has no direct grant on `orgs` (it's never needed one anywhere else), so a `security_invoker` view returned `42501 permission denied for table orgs` the moment the route actually queried it, caught by the manual smoke test before this ever reached a real deployment. Dropping `security_invoker` makes the view run as its owner — the same "security definer" posture `has_slot_visibility`/`can_access_slot` already use — so only the view's own grant (`service_role` only) gates access, not the underlying tables' grants. Worth remembering: a view over RLS-protected tables needs the *default* (definer) semantics unless the querying role is separately granted on every table the view touches.
- **A bare-proposal Slot has no Org otherwise**, so `slots.intended_org_id` is a new, separate hint — not a reservation, and untouched by attaching a real Booking. `assert_slot_intended_org_coherent` (a trigger, mirroring `assert_slot_booking_coherent`) is what stops it pointing at someone else's Org; RLS alone can't see across both tables in one policy.
- **The Booking Window lives on `orgs`, is the first update path that table has ever had.** Every prior Org action was create-or-delete only (`createOrg`/`pickPlace`/`deleteOrg`); `updateBookingWindow` needed no new RLS or grants — the table's existing owner-only `for all` policy already covered `update`, just never had a caller.
- **A separate preference, per explicit request mid-design.** The first draft reused #11's `email_enabled`; revised before implementation to a new `booking_window_email_enabled` column and its own Settings-page toggle, since wanting to know a game is on and wanting to be nagged about booking logistics are genuinely different asks. `shouldSendReminder` (already generic over "an emailEnabled boolean") needed no change to serve both.
- **`booking_window_reminder_sends` is one row per Slot, not per-channel like `reminder_sends`** — there's exactly one recipient (the owner) and one channel (email) for this Reminder, so a plain `unique(slot_id)` is the whole idempotency story; no `channel` column to check, and no need to alter #11's `reminder_sends` constraint.
- **Recipient/facility resolution reuses existing pure functions rather than re-deriving them**: `orgDisplayName` (place-backed vs. hand-named vs. "unavailable", from `orgs.ts`) resolves the Org's name in the email exactly the way the Orgs page already does; the owner's email still comes from the GoTrue Admin API, same as #11.
- **Same coverage gap as #11's Resend integration, same reasoning**: the actual email send is untested by Playwright (no fixture for a third-party mail API, and CI shouldn't send real mail). Verified with the same manual smoke test shape: unauthenticated/wrong-secret requests 401, and — after catching and fixing the `security_invoker` grant issue above — an authenticated request runs the full query pipeline cleanly, returning `{ ok: true, checked: 0, sent: 0, failed: 0 }` against the local stack's seed data.
- **A known, documented simplification**: changing a Slot's intended Org *after* its Booking Window Reminder already fired doesn't reset eligibility — `booking_window_reminder_sends` is keyed per-Slot, not per-Org. The same "out of scope, not forgotten" posture #10 already used for a couple of its own edge cases.

- **A second cron entry, same file, same corrected daily schedule as #11's** (`vercel.json`'s `"0 13 * * *"` — see #11's own notes above for why: Hobby caps Cron Jobs at once a day, confirmed against the account this ships to). Upgrading is the same one-line `vercel.json` edit for both routes. This job carries much less of #11's "missed entirely" risk for a short offset, though — its due-window runs from `window_opens_at` all the way to the Slot's own start, which is typically days wide rather than minutes, so a once-daily check is far more likely to land inside it. See the route's own header comment for the one configuration (`booking_window_days_before: 0` on a same-day Slot) where that width narrows.

Verified: 270 `node --test` tests (9 new, `booking-window.test.ts`), 168 pgTAP tests (13 new, `booking_window_reminders.test.sql`) pass; the full Playwright suite (50 tests, including a new booking-window spec in `bookings.spec.ts`, an intended-org spec in `slots.spec.ts`, and two new preference specs in `settings.spec.ts`) passes; typecheck, lint and `npm run build` are clean. `supabase migration up --local` applied cleanly (after a `supabase db reset` once the view fix above landed, to pick it up cleanly rather than hand-patching a live view). `supabase db push` to the hosted project is still pending, same as #11's.

### Notes carried out of #12

**Issue #12 (Web push + PWA installability) is shipped.** All four acceptance criteria are met: `/booking-buddy` carries a manifest (`public/booking-buddy.webmanifest`) and a registered service worker (`public/booking-buddy-sw.js`), so the section meets Chrome's current installability criteria; a signed-in User can opt into push from `/booking-buddy/settings` (`PushNotificationsForm`), which subscribes via `PushManager` and persists the subscription; `send-reminders` now delivers both channels, each gated by its own `notification_preferences` flag exactly as `shouldSendReminder` (#11) already decided; and a User with no subscription or `push_enabled: false` is silently skipped on the push channel, no error, same shape email already had for `email_enabled: false`.

- **`push_subscriptions` is one row per device, not one per User** — unlike `notification_preferences` (an owner's single opinion, upserted), a User can install the PWA on a phone and a laptop and reasonably expect a Reminder on both. `endpoint` carries the table's own uniqueness (issued once per browser registration), so re-subscribing on the same device is an upsert onto that column, never a duplicate row. RLS is the same owner-managed `for all` shape every other Booking Buddy table starts from (ADR 0003); `service_role` gets `select`/`delete` only (never `insert` — subscribing stays the User's own action through their session), the `delete` there specifically for pruning subscriptions the push service reports as gone.
- **The Settings-page toggle reads its checked state from the browser's own subscription, not from `notification_preferences.push_enabled`.** That column is account-wide ("is push on for this User at all"); the checkbox is asking "is push on for *this device*." Trusting the DB flag directly would show a false positive on a second device that never subscribed. `savePushSubscription` still flips `push_enabled` on as a side effect of a successful subscribe (subscribing *is* opting in, not a separate step), and `removePushSubscription` only flips it back off once it was the User's last device — the send job's per-User gate and the per-device subscription list are deliberately two different questions.
- **No offline caching in `booking-buddy-sw.js`, unlike `pickle-point-pal-sw.js`.** That service worker's whole job is surviving flaky court wifi for a single self-contained scorekeeper, where a stale cached shell is harmless. Booking Buddy is auth-gated and per-User; caching a navigation response risks serving one User's page to whoever signs in next on a shared device, so this service worker only handles `push`/`notificationclick` and does nothing on `fetch`. Also **no `fetch` handler at all**, deliberately — Chrome dropped the fetch-handler requirement from its installability criteria in 108 (mobile) / 112 (desktop); Chrome's own engineering blog now calls an empty pass-through handler added just to satisfy the old checklist an anti-pattern; adding one back for a checklist that no longer checks it would be worse than not having one.
- **VAPID env vars (`NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`) are optional, not required, in `send-reminders`.** Unlike `RESEND_API_KEY`/`REMINDER_FROM_EMAIL` (missing either 500s the whole route), a deploy that hasn't provisioned VAPID keys yet still sends email Reminders — the push channel is just skipped for the run, checked once up front rather than failing per-recipient. Generating a pair needs no external account (`npx web-push generate-vapid-keys` is pure local crypto), so this is a "needs a human to set the env var" gap, not a "needs a human to provision a third-party credential" one like Google Maps' key was.
- **Actually subscribing was verified manually, not by Playwright, and that's a deliberate scope call, not an oversight.** `PushManager.subscribe()` calls through to the browser vendor's real push service (Chrome talks to Google's FCM) to mint an endpoint — there's no local fixture for it the way `google-places-mock.ts` stands in for Google Places, and this session's sandbox has no outbound network to reach it at all (confirmed: subscribing throws `AbortError: Registration failed - push service not available` here specifically for that reason, not a code defect — a `chromium.launchPersistentContext` with `notifications` permission granted gets past sign-in, permission grant, and service worker registration cleanly, and only the actual FCM round trip fails). Same coverage shape #11/#36 already carry for Resend: pure logic (`formatReminderPush`) is unit-tested, the RLS boundary is pgTAP-tested (`push_subscriptions.test.sql`), and the two things true regardless of network — the toggle renders unchecked for a fresh device, and the unsupported-browser fallback renders when `PushManager` is missing — are what `e2e/push-notifications.spec.ts` actually covers.
- **The 7-magic-number VAPID key conversion (`urlBase64ToUint8Array`) needed an explicit `ArrayBuffer`, not `Uint8Array.from`.** `Uint8Array.from(...)` types as `Uint8Array<ArrayBufferLike>`, which `lib.dom`'s `BufferSource` (what `applicationServerKey` wants) refuses — `ArrayBufferLike` also admits `SharedArrayBuffer`, which `BufferSource` doesn't. Building the backing buffer by hand (`new ArrayBuffer` → `new Uint8Array(buffer)` → fill) sidesteps the mismatch. Worth remembering if this pattern shows up again: it's a `lib.dom` typing quirk, not a real runtime concern.

Verified: 271 `node --test` tests (1 new, `formatReminderPush`) pass; 176 pgTAP tests (8 new, `push_subscriptions.test.sql`) pass; the full Playwright suite passes except `e2e/places.spec.ts`'s 5 specs, which fail for a pre-existing, documented reason unrelated to this ticket: `testing.md` already warns a reused dev server skips `GOOGLE_PLACES_API_BASE_URL`'s mock override and hits the real Google API on this machine's real `GOOGLE_MAPS_API_KEY` — this session's sandbox has no outbound network at all (confirmed separately: a direct request to Google's own push service host also failed to connect), so those 5 were never going to pass here regardless of this ticket's changes, which never touch Places/Google code. `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `supabase migration up --local` applied cleanly. `supabase db push` to the hosted project, and setting the three VAPID env vars (plus generating a real production key pair) on Vercel, are both still pending — **needs a human** for the Vercel env vars specifically, same posture as #11's `CRON_SECRET`/`REMINDER_FROM_EMAIL`.

**#13 (Hardening pass) is done.** See "Notes carried out of #13" below, under Phase 10.

**[#23](https://github.com/AdrianLuk/juice-bros/issues/23) (Dashboard calendar + upcoming bookings list) is shipped.** All eleven acceptance criteria are met: `/booking-buddy` now renders a hand-rolled Month/Week/Agenda calendar (Week default) of the User's own Bookings, a persistent upcoming-bookings sidebar, a floating "+ Add booking" sheet reusing the existing `CreateBookingForm`, and the User's own resolved Availability layered in per ADR 0006 — a Booking always wins as busy over an overlapping Availability Window in the grid itself, not just in `resolveAvailability`. `/booking-buddy/bookings` is untouched. This closes out Phase 9.1 and leaves the graph with only the parent epic (#2) and the two items Phase 9 already tracked separately (9.2's TanStack-everywhere line and 9.3's optimistic-Yes test, both superseded/pending as noted there) open.

- **Two new pure-logic modules, both `node --test`-only**: `calendar.ts` (Week/Month grid-range math, the Week view's overlap-column layout for same-time Bookings, day-local grouping, the upcoming-Bookings filter) and an addition to the existing `availability.ts` — `resolveAvailabilitySegments`, which turns `resolveAvailability`'s single-moment answer into the list of open/busy blocks a calendar grid actually draws over a visible range. Neither imports React, Next.js, or Supabase, matching every other file in this seam.
- **`resolveAvailabilitySegments` never emits a segment for a Booking-covered span at all**, rather than emitting a "busy" segment that would sit underneath the Booking block. That's deliberately how the UI acceptance criterion ("renders as the Booking only, never both") is satisfied: the calendar draws a real Booking block for that span already, so a second, merely-corroborating Availability block would be redundant at best. The function still resolves through the exact same `resolveAvailability` precedence for the spans a Booking doesn't cover.
- **The Popover trigger's position has to be the styled/positioned element itself, not a wrapper around a positioned child.** First draft put the Week view's `position: absolute` styling on a `<div>` nested inside `PopoverTrigger`'s (a static-flow `<button>`) children; the button rendered with zero size — its only content had been pulled out of flow — even though the visually-positioned child still painted in the right place. Found by Playwright, not by eye: `toBeVisible()`/`.click()` reported the trigger `hidden`/unclickable, while a plain screenshot looked completely correct. Fixed by having `DashboardBookingPopover` accept `className`/`style` and apply them straight to the trigger button; every caller (Week, Month, Agenda) positions the button itself now, not a div inside it. Worth remembering for any future absolutely-positioned trigger built the same way.
- **The Week view is a plain sweep-based layout, not a fixed time-slot grid.** `layoutDayEvents` sorts a day's Bookings by start time and greedily assigns each the lowest free column among the events still open at that moment, closing a "run" (and stamping its column count) once nothing from it is still in progress — the same interval-graph-coloring approach a calendar library would use internally, sized down to what this app actually needs (no drag/resize, no cross-day events). A User's own Bookings rarely overlap, but the layout doesn't assume they don't.
- **A Booking never spans two Week-view day columns, on purpose, even though the viewer's local render of an instant range technically could straddle local midnight** if a facility's own time zone differs enough from the viewer's browser zone. `clampToDay` in `dashboard-week-view.tsx` clips a Booking's rendered block to the day it starts in rather than splitting it across two columns — a known, deliberate simplification (documented in the component) rather than building a general multi-day-event splitter for what both the app's constraints and this issue's own examples suggest is a rare case (courts are booked same-day, same-zone, in hour-or-two chunks).
- **Availability rendering treats `open`/`busy` as distinct in both color *and* pattern**, not color alone: `open` is a dashed, tinted outline (echoing the accent color already used for CTAs elsewhere), `busy` is a diagonal hatch (the same device Google Calendar uses for declared-busy time) rather than the app's destructive-red, which stays reserved for actual delete affordances. A Booking block is the only solid-filled one of the three. Month view's per-day availability summary is a pair of small dot indicators (outline for open, filled for busy) next to the day number rather than repeating the Week view's block treatment, since a day cell has no time axis to place a block against.
- **`Booking` gained a `timeZone` field** (`org?.timeZone ?? "UTC"`, mirroring how `when` was already resolved) so the dashboard's popover can show the facility's own clock explicitly, not just bake it into the pre-formatted `when` string. `createBooking`/`deleteBooking` also now `revalidatePath(BOOKING_BUDDY_ROOT)` alongside the existing `BOOKINGS_PATH` — both routes render Bookings now, and the quick-add sheet posts from the dashboard itself without navigating away.
- **No new tables, migrations, or RLS** — this ticket is entirely a read of what Phase 4 (`bookings`) and Phase 4.5 (`availability_windows`) already expose, so `npm run test:rls` needed no new assertions; a fresh `src/app/booking-buddy/error.tsx` was added, though, since `getDashboardPageData` is the dashboard's first read that can actually throw (same `DataError`/`readFailed` convention as `bookings/error.tsx`).
- **`e2e/support/availability.ts` is a new direct-PostgREST fixture helper**, in the same spirit as `guest-rsvp-log.ts` — but signs in as the real User rather than using the service-role key, since `availability_windows` (unlike `guest_rsvp_log`) carries no `service_role` grant at all (owner-only, per its own migration). There is still no create/edit UI for Availability Windows (out of scope for this ticket, per its own acceptance criteria), so a direct write is the only way a test can get one into the database.

Verified: 296 `node --test` tests (10 new — 5 in `calendar.test.ts`, 5 added to `availability.test.ts`), 176 pgTAP tests (unchanged — no schema touched) pass; the full Playwright suite (58 tests, 5 new in `dashboard.spec.ts`) passes when Playwright starts its own dev server (needed for `places.spec.ts`'s Google mock, same pre-existing caveat `testing.md` already documents for a reused one); typecheck, lint and `npm run build` are all clean.

**#23 was the last ticket in the graph besides the parent epic (#2).** Nothing is currently queued — check GitHub Issues for what's next before starting new work.

## Phase 9 — UI wiring (scoped per agreement — business logic already covered above)

- [x] 9.1 Routes: `/booking-buddy` (dashboard/calendar — shipped as [#23](https://github.com/AdrianLuk/juice-bros/issues/23), Booking-only for now since Slots don't exist yet), `/booking-buddy/friends`, `/booking-buddy/slots/[id]`, `/booking-buddy/settings`, and the public guest route `/s/[token]` (outside the auth-gated layout) — the last four had already shipped incrementally across earlier phases; #23 was the one still open
- [ ] 9.2 TanStack Query hooks wrapping each Server Action, with mutation-driven query-key invalidation; initial data fetched server-side and hydrated (Q5)
- [ ] 9.3 🔴 The one agreed high-stakes UI test: tapping "Yes" on a Slot shows an optimistic "yes" state immediately, before the server responds → 🟢 implement (tooling for this — RTL/jsdom vs. Playwright — to be decided when we reach this step)
- [x] 9.4 PWA: `manifest.json`, service worker, install prompt/nudge tied to enabling push notifications (Q8) — see [#12](https://github.com/AdrianLuk/juice-bros/issues/12)'s notes above

## Phase 10 — Hardening pass ([#13](https://github.com/AdrianLuk/juice-bros/issues/13))

- [x] 10.1 Cross-check every table introduced in Phases 1-8 has at least the coarse default-deny RLS policy its phase specified
- [x] 10.2 Confirm the guest-abuse soft-threshold logging (7.6) is actually wired into the production `guestRespondViaLink` path, not just the test

### Notes carried out of #13

**Issue #13 (Hardening pass) is done.** Both acceptance criteria hold, and this was genuinely a verification sweep, not new behavior — no application code changed.

- **10.1, done by cross-checking, not by writing new policies.** Every `create table public.<name>` across all 21 migrations (18 tables total: `profiles`, `connections`, `friend_groups`, `friend_group_members`, `visibility_overrides`, `orgs`, `bookings`, `place_cache`, `availability_windows`, `slots`, `responses`, `slot_bookings`, `slot_links`, `guest_rsvp_log`, `notification_preferences`, `reminder_sends`, `booking_window_reminder_sends`, `push_subscriptions`) has a matching `alter table ... enable row level security` — an exact 18-for-18 match, checked with a grep diff, not by eye. `push_subscriptions` (added after this ticket was first scoped, per the prior session's note) is in the count and is pgTAP-covered like everything else. The `service_role`-only tables (`guest_rsvp_log`, `reminder_sends`, `booking_window_reminder_sends`) carry no `authenticated`/`anon` policies at all, which *is* the coarse default-deny posture ADR 0003 describes for that shape — ratified by `throws_ok` `42501` assertions in their own pgTAP files, not just a comment claiming it. Ran `npm run test:rls` fresh against the local stack (Docker was available this session, unlike the one that shipped #10): all 176 pgTAP tests across all 14 test files pass. `supabase migration list` still shows all 21 migrations in sync between local and the hosted project — nothing pending to push.
- **10.2 turned up a real gap: the logic was already live in production, but nothing had ever proven it.** Reading `guestRespondViaLink` (`src/lib/booking-buddy/actions/guest-rsvp.ts`) confirms the count-then-flag-then-log sequence runs unconditionally on every real Guest RSVP, with no test-only branch or flag gating it — so the concern the ticket's title raises ("not just in tests") was already false in the sense of "gated behind a test double," but true in the sense that *nothing had ever exercised it end to end*: `slot-links.test.ts` only covers the pure parsing/message helpers, and `slot_links.test.sql` only proves the RLS boundary — it can't exercise `service_role` business logic at all, since `service_role` bypasses RLS by Supabase's own platform guarantee (the same limitation already noted for `place_cache`'s writes). Closed that gap with a new spec, `e2e/guest-rsvp-abuse.spec.ts`: a Guest browser context with a fixed `x-forwarded-for` header (Playwright's `extraHTTPHeaders`, standing in for what a real reverse proxy sets) drives `GUEST_RSVP_SOFT_THRESHOLD + 1` real RSVPs through the real dev server against one Slot Link, then reads `guest_rsvp_log` directly via the service-role REST key (`e2e/support/guest-rsvp-log.ts`, the same direct-PostgREST pattern `deleteCachedPlaces` already established for a `service_role`-only table) and asserts the first three attempts land `flagged: false` and the fourth lands `flagged: true` — proving the threshold logs and flags, and never blocks the RSVP itself, against the actual production code path rather than a description of it.
- **The spoofed IP is `198.51.100.42`, TEST-NET-2 (RFC 5737)** — a block reserved for documentation and examples, guaranteed never to resolve to a real client, the same reasoning that already picks fixture data (place IDs, dates) that can't collide with anything real.
- **Cleanup reuses `deleteSlots`, not a new sweep.** `slot_links` and `guest_rsvp_log` both cascade away with their Slot (the same fact `slot-links.spec.ts`'s own comment already documents), so no separate `guest_rsvp_log` cleanup was needed.

Verified: 271 `node --test` tests pass (no new ones — nothing here is pure-function logic); 176 pgTAP tests pass (no new ones — no new tables or policies, this ticket only cross-checked existing ones); the full Playwright suite (53 tests, 1 new — `guest-rsvp-abuse.spec.ts`) passes; `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `supabase migration list` confirms local and hosted are already in sync — this ticket added no migration.

**[#61](https://github.com/AdrianLuk/juice-bros/issues/61) (Friend calendar view) is shipped.** All ten acceptance criteria are met: a Connection resolved to `open_time`/`calendar` Visibility gets a "View calendar" action on `/booking-buddy/friends`, one with less gets none; `/booking-buddy/friends/[username]/calendar` renders that friend's busy/open time on the same Month/Week/Agenda shell the owner's own dashboard uses; a Booking's popover reads "Booked at [Facility]" plus start/end only; a bare-proposal Slot never renders as busy (it was never a Booking, so it was never a candidate); Availability Windows render exactly as they do on the owner's own dashboard; navigation is bounded at today *and* nothing before today ever renders, even within the currently-displayed week/month; there are no creation affordances; a viewer without the grant gets zero rows back from the route directly, server-enforced; the new view is pgTAP-covered proving both the zero-row boundary and that `court_label` isn't selectable through it; and [ADR 0010](docs/adr/0010-friend-calendar-reads-bookings-through-a-view.md) documents the reversal of Phase 4's "a Booking reaches a friend only through a Slot" invariant. A multi-angle code review ahead of merge caught two real gaps in the first pass (the past-time leak into the current week/month, and a misleading self-view) plus several smaller reuse/naming issues — both fixed and re-verified before shipping; see the notes below for what changed and why.

### Notes carried out of #61

- **The Week/Month/Agenda views became generic over what an "event" is, rather than growing a second copy of each.** `DashboardWeekView`/`DashboardMonthView`/`DashboardAgendaView` now take `T extends CalendarEvent` (`calendar.ts`'s new alias for `TimeSpan & { id: string }`, so the constraint lives in one place) plus a `renderEvent` (or per-view `renderWeekEvent`/`renderMonthEvent`/`renderAgendaEvent`) prop, and the popover mechanics moved into a new generic `CalendarEventPopover` (`calendar-event-popover.tsx`) — the grid math, scroll sync, and month/agenda layout (already generic in `calendar.ts` since #23: `layoutDayEvents`, `groupByLocalDay`, `layoutMultiDaySpans`) needed no duplicate. The three chip shells' own Tailwind classes (`WEEK_EVENT_CLASS`/`MONTH_EVENT_CLASS`/`AGENDA_EVENT_CLASS`) live once in `calendar-event-popover.tsx` too, so the owner's and a friend's chips can't drift apart visually without both call sites being touched. Two thin, non-generic client components sit on top: `OwnerDashboardCalendar` (extracted byte-for-byte from the old `DashboardCalendar`/`DashboardBookingPopover`, just expressed as render props now) and the new `FriendDashboardCalendar`. Both call the same generic `DashboardCalendar` shell — this is what "full reuse of the shell" in the ticket's own body meant, verified by screenshot: the owner's dashboard renders pixel-identical to before this ticket, popover included.
- **Render-prop functions can't cross the Server→Client boundary, which is why there are two wrapper components and not one `variant` prop.** A Server Component page can only pass serializable data to a Client Component, never a closure — so `page.tsx` (Server) can't hand `DashboardCalendar` (Client) a `renderWeekEvent` function directly. `OwnerDashboardCalendar` and `FriendDashboardCalendar` are each `"use client"` themselves, receive only data props from their Server Component pages, and define their own render-prop closures internally before calling the shared generic shell — Client → Client function props are fine, only Server → Client isn't.
- **`restrictToFuture` is a boolean, not a `minDate: Date` prop, on purpose.** The shell already resolves "today" itself, in a `useEffect` after mount, specifically to dodge an SSR/hydration mismatch near local midnight (`dashboard-calendar.tsx`'s own long-standing comment). A second, independently-computed "today" handed down from a caller could disagree with it. `restrictToFuture` reuses the shell's own resolved `now` instead of asking anyone else for a second opinion.
- **Bounding navigation at today turned out not to be enough on its own — caught by review, not by the first pass of manual testing.** The Week/Month views always render a full grid, so clamping only where the anchor can *move to* still let a friend's real past Bookings/Availability Windows show up on any day before today that falls inside the *currently displayed* week or month (Sunday–Tuesday of the week "today" sits in, or the 1st through today in Month view) — directly contradicting the page's own "nothing before today" copy the first time it was checked against a friend with real history instead of an empty one. Fixed at the data layer, not the grid: `notEndedBefore` (`calendar.ts`, pgTAP-adjacent pure logic with its own `node --test` coverage) filters both `events` and `availabilityWindows` to "hasn't fully ended before the floor" before either ever reaches the Week/Month/Agenda views, using the exact same `minAnchor` the navigation clamp already computes. A day cell's own "go to this week" button (Month view's day number, Week view's day header) is separately disabled via a new `minDay` prop threaded into both views, so a click on an already-past day does nothing rather than silently landing on today's week — the clamp alone made that landing safe, but not honest about why the click didn't go where its own `aria-label` said it would.
- **Visiting your own Username's calendar is treated as "not found," not rendered.** `friend_visible_bookings` never matches a self-pair (`has_open_time_visibility` has no Connection row to match against yourself), but `availability_windows`' read policy is `owner OR open-time-visible friend`, so a self-view would otherwise render your own real Availability Windows next to zero Bookings — a confusing, half-real page for a route whose entire premise is "someone else's calendar." `getFriendCalendarPageData` now checks `profile.id === session.userId` alongside the existing "unknown/no-Connection" check, collapsing all three into the same `null` → `notFound()`.
- **`friend_visible_bookings` (the new view) resolves `facility_name` in SQL, not by handing the caller `orgs`/`place_cache` rows to resolve themselves** — mirrors `orgDisplayName` (hand-typed name, else the cached Place's, else "Facility details unavailable") so a friend's calendar can't drift from what the owner's own Orgs page would show for the same Booking. See [ADR 0010](docs/adr/0010-friend-calendar-reads-bookings-through-a-view.md) for why this is a view rather than widening `bookings`/`orgs` RLS directly.
- **A friend's popover reads times in the *viewer's* browser-local clock, not the facility's — a deliberate divergence from the owner's own popover** (`formatInstantDateAndTime` with the Booking's own `timeZone`), flagged by review and kept after reconsidering rather than "fixed": `friend_visible_bookings` doesn't carry `time_zone` at all, on purpose (not in the ticket's own column list), and the calendar grid itself already positions every chip in the viewer's local time regardless of whose calendar it is. A friend checking "is my friend busy" cares what that means on *their own* clock, which local time already answers consistently with where the chip sits on the grid; the facility's own clock is what the *owner* needs to actually show up, which isn't this viewer's problem. Documented inline (`friend-dashboard-calendar.tsx`) rather than left as a silent gap. One consolidated `formatTimeLabelFromMs` (`datetime.ts`) now backs every clock label across both dashboards — three near-identical `Intl.DateTimeFormat`/`getHours()` formatters (one per new file, plus the pre-existing Week view one) collapsed into that single export during the same review pass.
- **`open_time_visible_owners(owner_users uuid[])` batches the friends-list gate in one round trip, and now runs alongside the profile lookup rather than after it.** The friends page needs to know, for every friend at once, whether *they* grant the caller `open_time` — the reverse direction from `resolveVisibility`'s existing caller (`getGroupsPageData`, which resolves what *the caller* grants *their own* friends) — and the caller has no read access to another User's Friend Groups/overrides to compute it client-side, so this can only be answered by a database function built for exactly this question; no new predicate logic beyond `has_open_time_visibility` either place. First pass called it as a second, sequential step after `listConnections()` fully resolved (including its own profile fetch) — an avoidable extra round trip, since the RPC only needs the friends bucket's ids, known right after grouping. `connections.ts` now shares one `fetchGroupedConnections` helper between `listConnections()` (untouched for its other caller, `getGroupsPageData`) and a new `getFriendsPageData()`, which runs the profile fetch and the visibility RPC via `Promise.all` instead.
- **`friend_visible_bookings`'s own row type is `FriendVisibleBooking`/`bookings`, not `FriendBusyBlock`/`busyBlocks`** — the first draft's naming, changed after review flagged it against CONTEXT.md's own Availability Window entry, which explicitly lists "Block" under `_Avoid_`. Renamed to mirror the view's own name instead, keeping it unambiguous next to the real `AvailabilityWindow` type sitting right beside it on the same component.
- **Verified in a real browser at every stage, not just pgTAP/`node --test`**, since a Playwright-driven session was available this session: signed in as one seeded account, granted a second one `open_time` Visibility via a Friend Group, logged a future Booking and a future Availability Window, then signed in as the second account and walked the whole path — the "View calendar" action appearing, the Week/Month views rendering the Booking and the Availability Window, the popover reading "Booked at [Facility]" with no court/format, the "Previous" button disabling once the visible month/week already contains today, and no quick-add affordance anywhere on the page. A third account (connected, but in no Visibility-granting group) hit the route directly by URL and got a normal page with no Bookings and no "Booked at" text anywhere in it. After the review pass above, re-verified specifically against the two bugs it found: a Booking and an Availability Window dated *yesterday* (inside the current displayed week) no longer render at all in Week or Month view once `restrictToFuture` is on, Month view's past-day cells render visibly disabled and a click on one is a genuine no-op, and the owner visiting their own Username's calendar route gets a real 404. All fixture data was inserted directly against Postgres (temporarily disabling `bookings_not_in_the_past` for the one fixture that needed a past date, matching the app's own domain rule that a Booking can never be created in the past through any other path) and removed again before running `npm run test:rls`, the same "test-only, cleaned up after" posture `docs/testing.md` already documents for `place_cache`.
- **`friends.spec.ts` is not extended with a "View calendar" browser spec, and neither is a dedicated `friend-calendar.spec.ts`.** In scope for a follow-up, not this session: the existing seeded local accounts (`docs/local-test-accounts.md`) have no Friend Group set up between them by default, and building one from scratch inside a spec (facility → booking → group → membership, four separate forms, one of them needing the past-dated-Booking workaround above) would be a heavier fixture than any existing Booking Buddy spec sets up for one assertion. The manual browser verification above exercises the identical code path, twice over (once before and once after the review fixes), but a dedicated Playwright spec is straightforward to add once there's a lighter fixture path (e.g. a seed-script extension mirroring `seed:users`'s own Connection seeding) — worth doing before this ships to production, not before merging this ticket.
- **Known, pre-existing, and deliberately not touched by this ticket**: `calendar.ts`'s `addMonths` doesn't clamp day-of-month on rollover (`setMonth` semantics), so navigating Month view forward/back from an anchor on the 29th–31st can skip a short month entirely — noticed while reviewing this ticket's own `goForward`/`goBack`, but the bug predates it (both functions call `addMonths` exactly as they did before this ticket, on both the owner's and now the friend's calendar) and isn't part of the friend-calendar feature. Worth its own ticket, not a scope-creep fix here.

Verified: 318 `node --test` tests pass (2 new — `notEndedBefore` in `calendar.test.ts`; `getFriendsPageData`/`getFriendCalendarPageData` stay untested at that seam, same as `getDashboardPageData` — they're glue, per "Seams under test"); 197 pgTAP tests pass (14 new, `friend_visible_bookings.test.sql`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` was not re-run in full this session (no changes to any existing spec, and the new surface has no dedicated spec yet — see the note above). `supabase migration up --local` applied cleanly; `supabase db push` to the hosted project is still pending.

**[#72](https://github.com/AdrianLuk/juice-bros/issues/72) (stale e2e locators) is fixed.** `npm run test:e2e` had drifted to 28 failing tests with nothing in this branch's own diff to explain it — every failure traced to a UI change from an *earlier*, already-merged session that never got a matching test update, discovered while shipping #62 (whose own branch first noticed the suite wasn't green) and diagnosed/fixed as its own ticket rather than folded into #62.

- **Three separate, unrelated causes, not one.** (1) The "warm visual redesign" (#43) renamed "place"/"club" copy to "facility" (`orgs.tsx`, `place-search.tsx`) and reworded both reminder checkboxes (`reminders.tsx`) — this was the original diagnosis. (2) A live rerun turned up a second layer: the Court field had become numbers-only (`type="number"`) at some earlier point, so fixtures filling `"Court 3"` were rejected outright — fixtures now fill just the number, since `formatCourtLabel` already prepends "Court " on display. (3) A third rerun turned up the deepest one: the duration-picker rework (#57) replaced a pickable End `<select>` with a field computed from Start + a Duration radiogroup (1/2/3 hours or Custom) and disabled/read-only otherwise — every Booking-logging call site needed a real rewrite (click a Duration radio, not select an End option), not a text swap. Each layer was invisible until the one in front of it was fixed and the suite could get far enough to hit it — three consecutive full runs, not one.
- **One test's entire premise stopped being constructible through the UI.** "A booking cannot end before it starts" set Start after End directly — impossible now that End is *derived* from Start + Duration and can never be earlier by construction. Flagged to the user rather than silently reinterpreted: kept as a rewritten test of the current equivalent invalid case (a custom duration that runs past midnight), not deleted, so the form keeps a negative-path e2e test. The server-side `endTime <= startTime` guard in `parseNewBooking` is unreachable from this form now but is still directly unit-tested (`bookings.test.ts`), so this is a UI-reachability change, not a coverage loss.
- **One genuine test bug, unrelated to any of the three causes above**: `friend-groups.spec.ts`'s "Remove member" flow clicked "Remove" once and asserted the member was gone — but that confirm dialog's own submit button shares its trigger's exact accessible name ("Remove"), so the single click only ever opened the dialog. Reproduced identically across two independent full runs (ruling out flake), fixed by scoping the confirm click to `getByRole("alertdialog")`.
- **A separate, real stray-data issue surfaced mid-investigation and wasn't a code bug at all**: after the first two fix layers, `friend-groups.spec.ts` still failed on a group that showed "1 friend" immediately after creation — traced to accumulated `Playwright`-prefixed fixture rows left behind by this same investigation's own earlier interrupted runs (dev server kills, mid-run cancellations), not anything the app or the tests got wrong. `supabase db reset --local` + `npm run seed:users` cleared it; worth remembering that a long debugging session against a live local stack can pollute its own evidence.

Verified: `npm run test:e2e` full suite green, 59/59, run three times from a freshly-reset local database as each layer was found and fixed. `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. No application/UI code changed — every fix is inside `e2e/`.

**[#62](https://github.com/AdrianLuk/juice-bros/issues/62) (Connect/disconnect Gmail — Mailbox Link) is shipped.** All nine acceptance criteria are met: `mailbox_links` exists with owner-only RLS (pgTAP-covered); `refresh_token` is encrypted at rest and never leaves the server unencrypted; "Connect Gmail" runs a real Google OAuth redirect requesting `gmail.readonly`, and on success stores a Mailbox Link and shows the connected account; "Disconnect" removes it; Gmail's OAuth flow is fully mocked in Playwright (`e2e/support/gmail-mock.ts` + `GMAIL_API_BASE_URL`); a server-only allowlist gates the Settings section (absent, not disabled, for anyone unlisted); `connectGmail`/the callback route both re-check the allowlist authoritatively, not just the page render; an empty/unset allowlist rejects everyone; and the allowlist check is `node --test`-covered as a pure function. This is #59's first slice — parsing, the actual sync/review flow, and cancellation handling are #63/#64/#65, still open.

- **Not #59's Server Actions module shape.** #59's own "Implementation Decisions" sketched one `actions/email-sync.ts` with `connectGmail`/`disconnectGmail`/`syncFromEmail`/`confirmImportCandidate`/`dismissImportCandidate` together; this ticket only needed the first two; the other three arrive with the tickets that actually have candidates to act on (#64/#65). `actions/email-sync.ts` today holds `connectGmail`, `disconnectGmail`, `getMailboxLink`, and `isEmailSyncAllowedForCaller` — the last is exported for the Settings page to call directly, since it's the same "is this User approved" question `connectGmail` re-checks authoritatively, and duplicating that logic between the page and the action would be exactly the drift ADR-0009's addendum is trying to avoid.
- **The refresh-token encryption decision #59 left open is resolved as app-level AES-256-GCM, not `pgcrypto`.** `token-encryption.ts` (pure, `node --test`-covered — round-trip, wrong-key, and tampered-ciphertext all fail closed) encrypts/decrypts entirely in Node with a new `MAILBOX_LINK_ENCRYPTION_KEY`, so the key never has to enter Postgres at all — a `pgcrypto` approach would mean handing the key to the database on every decrypt, which is a wider blast radius for the one secret #59 explicitly asked to be treated as sensitively as `SUPABASE_SERVICE_ROLE_KEY`. `mailbox_links.encrypted_refresh_token` stores `iv.authTag.ciphertext`, each part base64; every read of the table in app code hand-picks columns (never `select("*")`), the same discipline that column already gets in principle even though nothing yet needs to select it into a client-facing response.
- **`gmail-client.ts` needed three real Google hosts, not Places' one** (`accounts.google.com` for the authorize redirect, `oauth2.googleapis.com` for the token exchange, `www.googleapis.com` for the userinfo lookup that resolves which account got connected) — `readGmailApiBaseUrl`/`GMAIL_API_BASE_URL` collapses all three onto one local mock host by appending a distinguishing path (`/authorize`, `/token`, `/userinfo`) when the override is set, still mirroring `GOOGLE_PLACES_API_BASE_URL`'s single-var shape. `prompt=consent` is set on the authorize request on purpose: without it, a User re-authorizing an already-granted scope can get a token response with no `refresh_token` at all, which this feature has no use for.
- **`gmail-mock.ts` also has to stand in for the browser's own redirect through Google's consent screen, not just two JSON endpoints like `google-places-mock.ts`.** There's no real human to click "Allow" in CI, so `/authorize` simulates instant consent — it reads `redirect_uri`/`state` off the query string and 302s straight back with a one-time code, which `/token` then checks was actually issued by this run (not a stale/replayed one) before returning fixture tokens. `registerTokenFailure` exists so `email-sync.spec.ts` can exercise the "couldn't connect" path without a real outage.
- **A real bug caught only by actually running the browser test, not by `tsc`**: the first draft exported a `GMAIL_OAUTH_STATE_COOKIE` string constant straight out of `actions/email-sync.ts`. `"use server"` files may only export async functions — Next's compiler accepts a *type*-only export from one (`export type { ActionResult }` already relied on that) but rejects a runtime `const`, and this only surfaces as a build error in the dev server's own output, not from `tsc --noEmit`. Fixed by moving the constant into its own tiny non-`"use server"` module, `gmail-oauth.ts`, imported by both the action and the callback route.
- **The OAuth callback is a Route Handler (`src/app/booking-buddy/settings/gmail-callback/route.ts`), separate from `.../auth/callback`**, exactly as #59 specified — it re-checks the allowlist itself (not just trusting that `connectGmail` already did, since a User could be de-allowlisted mid-flow) before ever calling Google's token endpoint, and never touches Supabase Auth or the session cookie: a Mailbox Link is a second, independent OAuth grant, not a sign-in.
- **A stray already-running `next dev` server on :3000 had to be stopped before `email-sync.spec.ts` could run at all** — confirmed with the user first, since it's exactly the kind of process a person might have open for their own reasons. Once stopped, Playwright started its own server and picked up `GMAIL_API_BASE_URL`/the new OAuth env vars from `webServer.env`, the same caveat `testing.md` already documents for `places.spec.ts`.
- **`GOOGLE_OAUTH_CLIENT_ID`/`GOOGLE_OAUTH_CLIENT_SECRET` and a real `MAILBOX_LINK_ENCRYPTION_KEY`/`EMAIL_SYNC_ALLOWLIST` are not set on Vercel yet — needs a human**, same posture as every other real external credential in this file (Google Maps' key, the Google OAuth *sign-in* provider itself). Provisioning the OAuth client also means adding this app's production/preview redirect URIs in Cloud Console and, per ADR-0009, adding each initially-approved User as a Cloud Console test user separately from `EMAIL_SYNC_ALLOWLIST`. Until then the feature is simply invisible in production (nobody is in `EMAIL_SYNC_ALLOWLIST`), not broken.

- **A code review pass ahead of shipping caught four real issues, all fixed before merge**: (1) the "your account isn't approved" error was unreachable — `connectGmail`'s redirect on a disallowed User set `?error=email_sync_not_allowed`, but the Settings page only ever rendered `GmailSyncSection` (the only place that error was displayed) when the caller *was* allowed, so the message could never actually show; fixed by moving that one error to a standalone banner the page renders outside the allowlist gate. (2) `requireMailboxLinkEncryptionKey()`/`requireGoogleOAuthClientId()` threw uncaught on a missing env var, unlike every other failure branch in the callback route and `connectGmail`, which redirect to `?error=gmail_connect_failed`; both now catch and redirect the same way — relevant precisely because these vars are the ones flagged "not set on Vercel yet" below. (3) the Settings page fetched the caller's own profile twice per load (once directly, once again inside `isEmailSyncAllowedForCaller`); fixed by having the page call the pure `isEmailSyncAllowed` directly against the profile it already has, reserving the wrapper for callers (like `connectGmail`) that don't. (4) the "build an absolute URL from the request's own host" snippet had drifted into a third near-identical copy (`auth.ts`'s `callbackUrl`, `slot-links.ts`'s `absoluteSlotLinkUrl`, and this ticket's `gmailCallbackUrl`); extracted to one shared `request-origin.ts` (`absoluteAppUrl`), with `auth.ts`/`slot-links.ts` updated to call it too — a behavior-preserving extraction, reverified by rerunning `slot-links.spec.ts` (which exercises `absoluteSlotLinkUrl`'s only real caller) alongside `email-sync.spec.ts` after the change.
- **A pre-existing, unrelated e2e regression was found while running the full suite, not caused by this ticket**: 28 tests across `bookings.spec.ts`, `dashboard.spec.ts`, `places.spec.ts`, `settings.spec.ts`, `friend-groups.spec.ts` and `slots.spec.ts` time out on `getByLabel`/`getByRole` lookups whose text no longer matches the app. Traced to the "warm visual redesign for adoption" PR (#43, `041d721`) renaming copy — e.g. `orgs.tsx`'s "Place name"/"Add place" became "Facility name"/"Add facility", `reminders.tsx`'s "Email me a reminder before slots I'm in" became "...I've said yes to, so I don't forget to show up" — without updating the specs that still look for the old strings. Confirmed unrelated to #62 two ways: `git diff origin/master` on every affected component/spec file is empty (this branch never touched them), and the same failures reproduce running those specs alone, fresh, against a clean dev server. Worth its own ticket — a straight-through find-and-replace of stale copy in `bookings.spec.ts`/`dashboard.spec.ts`/`places.spec.ts`/`settings.spec.ts`/`friend-groups.spec.ts`/`slots.spec.ts` against the redesigned components, verified test-by-test. `email-sync.spec.ts` (this ticket's own spec) uses only current copy and is unaffected — all 5 of its tests pass, including a full mocked-OAuth connect/disconnect run in a real browser.

Verified: 344 `node --test` tests pass (16 new — 9 in `email-sync-allowlist.test.ts`, 7 in `token-encryption.test.ts`); 211 pgTAP tests pass (14 new, `mailbox_links.test.sql`); `npx tsc --noEmit` and `npm run lint` are clean. `npm run test:e2e` run in full against a freshly-started dev server: `email-sync.spec.ts`'s 5 new tests all pass; 28 pre-existing failures elsewhere in the suite are a stale-copy regression from #43, unrelated to this ticket (see above) — not fixed here, flagged for a follow-up ticket instead of expanding this one's scope. `supabase migration up --local` applied cleanly (verified twice: once mid-run, once after a full `supabase db reset` that also confirmed a pre-existing, unrelated `place_cache` row-count failure in `orgs_and_bookings.test.sql` was stray local data, not something this ticket caused). `supabase db push` to the hosted project is still pending.

**[#63](https://github.com/AdrianLuk/juice-bros/issues/63) (CourtReserve email parser + matcher) is shipped.** All ten acceptance criteria are met: `courtreserve-email.ts` parses a CourtReserve confirmation into facility/date/time/court/format/players and a cancellation into the same facility/date/time/court shape; a waitlist/membership email is recognized and excluded as `not_a_booking` rather than mis-parsed; malformed HTML for an email whose subject *did* claim to be a confirmation/cancellation comes back `unparseable`, never throws; `buildCourtReserveSearchQuery` scopes to `notifications@courtreserve.com` and the last 90 days; `email-sync-matching.ts` covers exact-name Org matching, Org+court+date/time duplicate detection, Connection name matching, and past-date filtering (reusing `datetime.ts`'s `isPastDate`). No user-visible behaviour yet, per the ticket's own framing — both modules are pure logic with no caller, wiring is #64's job.

- **No real CourtReserve email was available this session** — every fixture (confirmation/cancellation HTML, subject-line phrasing) is this session's own best-guess construction of "a labelled table of fields" per #59's framing, not a captured real sample. Flagged directly in `courtreserve-email.ts`'s own header comment as something to re-verify once #64 actually runs this against a live inbox, rather than presented as validated. This is the honest ceiling on how far a review pass can harden pattern-matching logic with no ground truth to check it against.
- **A four-round code-review cycle, each round finding real bugs the previous round's fixture set didn't cover** — worth recording for the shape it took, not just the count. Round 1: an incomplete/case-folded HTML entity table, a naive time-range dash-split silently downgrading a malformed end time to "no end time" instead of failing, and a global label/value regex that backtracked across a lone unpaired `<td>`. Round 2: the same dash-only time-range gap re-surfaced for the word "to" as a separator, `<br>`-joined player names concatenating without a separator, and uppercase-accented entities (`&Eacute;`) folding onto their lowercase counterpart. Round 3/4: a subject-classification tension with no clean answer without real data — a bare "cancel" mention in ordinary confirmation hedge text ("you may cancel free of charge...") needed excluding, but exclude too eagerly and a genuine cancellation phrased as a noun ("Reservation Cancellation") stops matching, and a naive global `<td>` regex mispaired a row that had a leading icon/spacer cell. Resolved by requiring the verb/noun *suffix* ("cancelled"/"cancellation", not bare "cancel") combined with a narrow "cancellation polic(y)" phrase exclusion for the first, and by rewriting field extraction to be row-scoped (`<tr>` then its cells, last two as label/value) for the second — the combination that satisfies every constructed test case, though see the note above about the ceiling on that confidence. Declined to keep chasing the tail past round 4 (a semicolon-less HTML entity, explicitly flagged by the reviewer itself as "plausible but not certain") — diminishing returns against self-invented fixtures, documented as a known gap rather than chased into a fifth round.
- **`extractFields` ended up row-scoped (`<tr>` then its `<td>` cells, last two as label/value) rather than a flat global regex pairing any two adjacent `<td>`s** — the latter's first version mispaired a row with a leading icon/spacer cell (a common real email-template pattern) by consuming cells 1+2 instead of 2+3, silently losing the real field. Row-scoping also subsumed an earlier, narrower fix (blocking backtrack across a lone unpaired `<td>`) for free, since a single-cell row is now just skipped rather than needing its own regex exclusion.
- **`parseFormat` was changed mid-review to call `capacity.ts`'s existing `isBookingFormat` guard instead of hand-checking `=== "singles"`** — the module's own comment already cited `parseNewBooking`'s "default rather than error" precedent but hadn't actually reused its validation, so a third `BookingFormat` value would have updated `capacity.ts` and silently stayed stale here.
- **Two near-identical "zero-padded local Y-M-D from a `Date`" blocks** (`parseHumanDate`'s dash-joined date, `buildCourtReserveSearchQuery`'s slash-joined one) **collapsed into one `formatLocalDate(date, sep)` helper.**

Verified: 367 `node --test` tests pass (30 new in `courtreserve-email.test.ts`, 17 new in `email-sync-matching.test.ts`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. No pgTAP/Playwright coverage — no schema and no UI in this ticket. `npm run test:e2e` not run for this ticket (unaffected — see #72 above, fixed and verified separately in this same session).

**#63's real-email gap (flagged above) closed the same session it was raised**, once two real CourtReserve emails (a confirmation, a cancellation) were captured via Gmail's "Show original" and handed over. The best-guess reconstruction turned out to assume the wrong template shape entirely, not just wrong field wording:

- **CourtReserve's real body has no labelled `<td>` rows at all.** Each field group is an `<h4>` heading ("Details", "Player(s)", "Court(s)", or "Cancellation Details" for a cancellation) immediately followed by an `<h5>` value — `extractFields`'s whole row-scoped `<tr>`/`<td>` design (round 1–2's hardening work, notes above) was scanning for a shape that doesn't exist in a real email. Replaced with `extractSection`, matching an `<h4>` heading to its following `<h5>`.
- **The confirmation subject never contains the word "reservation."** It's "Booking Confirmation for Friday, 2026-08-21 2:00 PM - 4:00 PM" — `CONFIRMATION_SUBJECT_PATTERN` required "reservation" *and* "confirm" jointly, so a real confirmation would have silently fallen through to `not_a_booking` before ever reaching the body parser. Now accepts "booking" alongside "reservation", still paired with "confirm" rather than matching either word alone.
- **The facility name isn't a labelled field anywhere in the body** — it's the template's own logo `<img alt>`. `extractFacilityName` reads the first non-empty `alt`, skipping the empty-`alt` tracking pixel in the footer.
- **A cancellation's body has no Court(s) section at all** — its one "Cancellation Details" block bundles the player's own name in front of the same format/date/time lines a confirmation's "Details" block carries, with no separate court field. `CourtReserveCancellation.courtLabel` is `null` for every real cancellation, not just a facility that doesn't label its courts.
- **The body date ("Friday, 8-21-2026") is a non-ISO format `Date`'s own free-text parsing isn't spec-guaranteed to handle consistently across engines** (ECMA-262 only promises ISO 8601). `parseHumanDate` now parses the known `M-D-YYYY` suffix with a regex instead of handing the string to `new Date()`.
- **Both real emails, with the facility/player names swapped for the same placeholders the rest of the test file uses** (this repo is public), are now regression fixtures in `courtreserve-email.test.ts` — byte-for-byte the real nested-doctype/divider-table/footer/tracking-pixel structure, not a simplified stand-in. The synthetic fixture builders (`confirmationHtml`/`cancellationHtml`) were also rewritten to the real `<h4>`/`<h5>` shape; a few tests that only made sense under the old `<td>`-row model (a leading icon cell, a label with a space before its colon, a `<strong>`-wrapped label) were removed rather than adapted, since headings in the real template are always plain text.

Verified again: 366 `node --test` tests pass (29 in `courtreserve-email.test.ts`, replacing the prior 30 — net one fewer after removing table-row-specific tests and adding two real-fixture regression tests); `npx tsc --noEmit`, `npm run lint`, and `npm run build` (TypeScript/lint stage only — prerendering `/booking-buddy` needs Supabase env vars this worktree doesn't have) are all clean. `email-sync-matching.ts` untouched — it consumes `CourtReserveConfirmation`/`CourtReserveCancellation` by shape, not by how they're parsed, and that shape didn't change.

**[#64](https://github.com/AdrianLuk/juice-bros/issues/64) (Sync, review, and confirm new Bookings) is shipped.** All ten acceptance criteria are met: `processed_gmail_messages` exists with owner-only RLS (pgTAP covered), no update/delete grant since an outcome is recorded once and never revisited; "Sync from Email" (now living on the Bookings page, not Settings — see below) runs a live search and shows a review screen of candidates; a facility-name exact match prefills the (now-shared) Org picker, otherwise it's left for the User; matched co-players are shown for reference only; confirming creates a real Booking with the parsed format/court; dismissing records the outcome so a later sync skips it; a duplicate or past-date/time candidate is filtered out before the review screen ever renders; an expired Mailbox Link — discovered either already-expired or mid-sync, when Google's own refresh grant itself fails — shows a reconnect prompt instead of a raw error; `syncFromEmail`/`confirmImportCandidate`/`dismissImportCandidate` each re-check #62's allowlist authoritatively. Cancellations are #65's job — a `cancellation`-kind parse is simply skipped here (never recorded in `processed_gmail_messages`), same as `not_a_booking`/`unparseable`.

- **`gmail-client.ts` grew a refresh-token exchange and the two Gmail message endpoints #63's parser had no caller for yet.** `refreshAccessToken` distinguishes `invalid_grant` (Google's own code for a dead/revoked refresh token — ADR-0009's 7-day Testing-mode expiry) from a generic `unreachable`, which is exactly the fork `syncFromEmail` needs to tell "reconnect Gmail" apart from "Google had a bad minute." `searchGmailMessages`/`fetchGmailMessage` mirror the real `users.messages.list`/`.get` shape (not paginated — the query's own fixed 90-day window comfortably fits one page for a hobby-app inbox); `fetchGmailMessage` walks a (possibly multipart) message payload for its `text/html` part rather than assuming it's the top-level body, base64url-decoding it into what `parseCourtReserveEmail` expects.
- **The Org `<select>` extracted out of `bookings.tsx` into `OrgSelect`, exactly as the ticket asked, and it needed no interactivity of its own** — no "use client", since it's plain enough to render from either a client or server tree. `CreateBookingForm` now calls it too, a behavior-preserving swap (same markup, same `defaultValue`/`required` props).
- **A CourtReserve email's own Court(s) text ("Court #6 - Hard", per #63's real fixture) isn't the same shape `court_label` already stores** — the manual Booking form's court field is bare ("3"), with `formatCourtLabel` re-adding "Court " at display time; feeding a candidate's raw courtLabel straight through would have rendered "Court Court #6 - Hard" once confirmed. `stripCourtLabelPrefix` (`bookings.ts`, node-test covered) strips exactly the leading "Court" word CourtReserve's own template always prepends, keeping whatever free text follows it ("#6 - Hard") — round-trips back through `formatCourtLabel` to the original.
- **`createBooking`'s org-ownership check, past-date check, insert, and error translation moved into a shared `insertValidatedBooking(ownerId, parsed)`**, called by both `createBooking` (form-parsed) and `confirmImportCandidate`. `confirmImportCandidate` itself takes the laziest-correct path: the candidate card's Confirm form posts the exact same field names (`org_id`, `format`, `date`, `start_time`, `end_time`, `court_label`) `CreateBookingForm` does, plus `gmail_message_id`, so it reuses `parseNewBooking`'s validation as-is rather than trusting the already-parsed candidate fields a second time. A candidate whose parsed time isn't on-the-hour (CourtReserve emails captured so far always are) would surface `parseNewBooking`'s existing "Pick a start and end time" message on Confirm — not specifically handled, since every real captured sample so far is on-the-hour and the ticket doesn't call this case out.
- **The review screen ended up on the Bookings page instead of Settings** — Settings stays the connect/disconnect pipe (#62's own scope), and Bookings is where a synced candidate actually becomes visible once confirmed, right next to the manual "Log a booking" form it's an alternative to. Originally gated behind `orgs.length > 0` (mirroring "Log a booking"'s own gating, since confirming genuinely needs an existing Org to point at), moved out of that gate after the expired-Mailbox-Link Playwright case exposed the real problem with it: a User syncing for the first time, with zero Orgs yet, still deserves to see a reconnect prompt (or a "no new bookings" result) without being blocked by an unrelated empty state. The Org *picker* inside a candidate card still has nothing to offer with zero Orgs — that half of the gating was never wrong, only gating the whole section on it was.
- **`syncFromEmail` reuses `getBookingsPageData()` and `listConnections()`** rather than querying `bookings`/`connections` a second, narrower way — both were already exactly the shape needed (`Booking.timeZone` per row for the zone-aware duplicate/past-date check via `todayInZone`/`clockInZone` against an arbitrary instant, not just "now" despite the name; `listConnections().friends` for player matching, filtered to a non-null `displayName` since `matchPlayerNamesToConnections`' own `ConnectionCandidate` type doesn't allow one).
- **`e2e/support/gmail-mock.ts` grew message list/get endpoints and a `refresh_token`-grant branch on `/token`**, gated behind `grant_type` rather than reusing the `authorization_code` path's `#issuedCode` check (a refresh carries a `refresh_token`, not a one-time code) — without this, every refresh attempt in a test would have failed as an unmatched code regardless of `registerTokenFailure`. `registerMessages`/`MockGmailMessage` seed a fixture inbox both new endpoints read back from.
- **A genuine Playwright-only bug, not a product one**: the confirm test's candidate-card locator (`getByRole("listitem").filter({ hasText: facility })`) started also matching the new `bookings` row once confirmed, since `BookingRow` renders the same facility name — scoped instead to `.filter({ has: page.getByRole("button", { name: "Confirm" }) })`, which only the review card ever has.

Verified: 385 `node --test` tests pass (3 new, `stripCourtLabelPrefix` in `bookings.test.ts`); 226 pgTAP tests pass (13 new, `processed_gmail_messages.test.sql`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` run for `email-sync.spec.ts` (all 8 — the 5 pre-existing plus 3 new for this ticket), `bookings.spec.ts`, `dashboard.spec.ts`, and `settings.spec.ts`: all green against a freshly-started dev server, no regressions from the `OrgSelect`/`insertValidatedBooking` extractions or the Bookings-page layout change. `supabase migration up --local` applied cleanly.

**[#79](https://github.com/AdrianLuk/juice-bros/issues/79) (Add Gender to profile) is shipped.** All five acceptance criteria are met: `profiles.gender` exists, nullable, constrained to `male`/`female`, no backfill/RLS changes needed (the table's existing owner-only policies already cover it); Settings has a Gender field — a radiogroup ("Prefer not to say"/Male/Female), same interaction pattern `DurationPicker` (bookings.tsx) already established — saved through its own `updateGender` action, independent of `UsernameForm`'s; leaving it unset is the default and is never treated as an error or nagged about; `getOwnProfile()` returns it.

- **Filed as two tickets, not one, once the actual ask turned out to be "a gender-aware capacity *signal*," not a hard sign-up cap.** The originating conversation asked for both a Gender field and "the ability to cap it," which read at first like reversing [ADR 0001](docs/adr/0001-optional-booking-on-slot.md)'s explicit "yes' Responses are never blocked, no automatic waitlist" rule for a Slot's Capacity. Confirmed with the user before writing anything: the cap should stay a soft signal (ADR 0001 unchanged), just broken down by gender instead of one flat number, and mixed doubles is a fixed 2-male/2-female split rather than organizer-configurable. This ticket is the field only; the actual signal logic is #80, `needs-triage` and blocked on this one (native GitHub issue dependency, not just a body link) since it has more open surface (where Division lives, how an "unspecified" gender Responder folds into the display) worth a look before an agent just wings it.
- **`parseGender` never has an error branch, unlike `parseUsername`.** A tampered/stale value falls back to `null` (unset) rather than refusing the write — the same "default rather than error" posture `capacity.ts`'s own format parsing already takes, and defensible here specifically because the form only ever offers three real choices (unset/male/female) the User can't mistype; there's no free-text path where a person could plausibly hit this and deserve an explanation.
- **A real Playwright gotcha, not a product bug**: `getByRole("radio", { name: "Male" })` matched *both* the Male and Female buttons, since Playwright's accessible-name matching is substring-by-default and "Male" is literally inside "Female." Fixed with `exact: true` everywhere the Male radio is targeted — worth remembering for any other radiogroup with a similarly-nested label pair.

Verified: 391 `node --test` tests pass (6 new, `gender.test.ts`); 233 pgTAP tests pass (7 new, `profile_gender.test.sql`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` run for `settings.spec.ts` (all 13 — the 10 pre-existing plus 3 new for Gender): green against a freshly-started dev server. `supabase migration up --local` applied cleanly.

**[#80](https://github.com/AdrianLuk/juice-bros/issues/80) (Gender-aware Capacity signal for mixed/men's/women's Slots) is shipped.** All six acceptance criteria are met: `slots.division` exists, `not null default 'open'`, constrained to `open`/`mixed`/`mens`/`womens` (pgTAP-covered); the Slot creation form offers a Division picker; `computeGenderedCapacity`/`isGenderBucketOverCapacity` (`capacity.ts`) grow the gender-aware equivalents of `computeCapacity`/`isOverCapacity`, returning `null` — today's plain-count behaviour, unchanged — for an `open` Slot or one with no Capacity yet; `SlotCapacityPanel` renders a per-gender breakdown (`"Male: 3 of 2 spots taken"`, `"Female: 1 of 2 spots taken"`) for a non-`open` Slot with a real Capacity, and is byte-for-byte unchanged otherwise; a Responder with no Gender set (or, for `mens`/`womens`, the *other* gender) falls into a visible "unspecified" count rather than being folded into either bucket; both `node --test` and a Playwright spec cover it.

- **The ticket's own acceptance criteria said Division should be "constrained to valid values for the Slot's own Format," but there is no per-Slot Format to constrain against.** Format lives on `bookings`/`slot_bookings` (ADR 0008), copied per attached Booking specifically *because* a Slot can carry several Bookings of different formats at once (a multi-court game) — and Division is chosen at Slot creation, before any Booking is attached at all, so there's no Format in scope yet even if one existed. Resolved by not adding a database constraint tying the two together: `division`'s check constraint covers only its own four values, the creation form always offers all four regardless of Format, and "mixed only really makes sense for doubles" stays an unenforced application-level nuance — consistent with `has_slot_visibility`'s own precedent of keeping nuanced logic out of a CHECK constraint. Documented directly in the migration's column comment so the gap doesn't look like an oversight to the next reader.
- **`mixed` is a fixed even split of the Slot's own Capacity, not always literally "2 and 2."** The user's own answer while scoping this ("fixed 2M + 2F for any mixed Slot") describes the common case — one doubles court, Capacity 4 — but Capacity can be any number once a rotation buffer or multiple courts are involved. `computeGenderedCapacity` generalizes it as `Math.floor(capacity / 2)` male / the remainder female, which reduces to exactly 2/2 for the case the user described and degrades sensibly (not by refusing) for anything else, e.g. a rotation-buffered Capacity of 5 splits 2/3.
- **`mens`/`womens` is one bucket against the Slot's whole Capacity, and a "yes" from the other gender is not silently counted toward it.** Not explicitly spelled out in the acceptance criteria (which only calls out the no-Gender-set case), but the same reasoning extends naturally: an unaccounted-for "yes" is exactly what the "unspecified" bucket exists to hold, whichever reason it's unaccounted for. `computeGenderedCapacity`'s three-branch shape (`open`/no-capacity → `null`, `mixed` → two buckets, `mens`/`womens` → one bucket) keeps this a single pure function rather than special-casing the single-gender divisions' "everyone else" logic separately.
- **The Capacity display's per-bucket over-capacity banner replaces the flat one entirely for a non-`open` Slot, rather than showing both.** A mixed Slot at "4 of 4, full" by the old flat math can still be genuinely over on one side and under on the other (the ticket's own worked example) — showing the flat banner alongside the gendered one would either contradict it or double up on the same information stated two ways. `SlotCapacityPanel` branches once on `computeGenderedCapacity`'s result: `null` still renders exactly the pre-#80 flat markup (verified via the existing `attaching a booking gives a proposal real capacity...` spec, unmodified and still green), a real breakdown renders only the new gendered markup.
- **The optimistic "yes" a `ResponseButtons` click writes into the query cache always carries `gender: null`, same as the existing `displayName: null`.** `ResponseButtons` isn't handed the viewer's own Gender (`viewerName` passed to it today is hardcoded `null` too, pre-dating this ticket), so a fresh "yes" briefly shows as unspecified in the breakdown until `onSettled`'s refetch brings back the server-computed value — never miscounted onto a side, just transiently uncategorized for the length of one round trip. Threading the real Gender through would mean a new prop and a new profile fetch on the Slot detail page for a few hundred milliseconds of cosmetic accuracy; not worth it against the existing precedent right next to it.
- **A real bug in this ticket's own Playwright spec, the same class as #79's**: `getByText("Male: 0 of 1 spots taken")` matched *two* elements — its own line and `"Female: 0 of 1 spots taken"` — because Playwright's text matching is a case-insensitive substring by default, and "male: 0 of 1 spots taken" is literally inside "female: 0 of 1 spots taken". Fixed with `exact: true` on every Male/Female breakdown assertion, the same fix #79's retrospective already flagged for radio buttons — worth remembering as a general rule for this app's gendered UI, not just radiogroups.
- **A second, this-test-only bug**: the over-capacity assertion originally reset Ben2's Gender back to unset (test cleanup) *before* asserting the breakdown — but the breakdown reads each Responder's *current* Gender at fetch time, not the Gender they had when they answered, so resetting first erased the very thing under test. Fixed by asserting first, resetting after, inside the same `try` block.
- **Known, pre-existing, and unrelated to this ticket, surfaced only by a full local `supabase db reset` + `npm run seed:users`**: `slots.spec.ts`'s `"a Connection with no slots Visibility cannot see or reach the slot"` now fails, reproducibly, in isolation. Traced to #76 (`335415d`, already merged): accepting a Connection now seeds `calendar`-level `visibility_overrides` on both sides automatically, so the seeded Amy↔Ben2 Connection this test relies on having *no* override no longer exists once seeded fresh — the test's premise ("a friend with no group and no override has no access") is still true as a rule, but the seed data it points at no longer satisfies "no override." Not fixed here — no code in this ticket touches Connections, visibility, or the seed script, and re-scoping this ticket to fix an unrelated older test would be exactly the scope creep CLAUDE.md warns against. Worth its own ticket.

Verified: 406 `node --test` tests pass (15 new — 5 in `division.test.ts`, 8 in `capacity.test.ts`, 2 in `slots.test.ts`); 240 pgTAP tests pass (7 new, `slot_division.test.sql`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` run for `slots.spec.ts` (all 8 — the 7 pre-existing plus 1 new for the gendered breakdown) and `settings.spec.ts` (all 13, unaffected): green against a freshly-started dev server and a freshly-reset local database, aside from the pre-existing #76 regression noted above (reproduces identically with or without this ticket's changes checked out). `supabase migration up --local` applied cleanly.

**[#83](https://github.com/AdrianLuk/juice-bros/issues/83) (stale "no slots Visibility" e2e test after #76) is fixed.** Root-caused to `scripts/seed-booking-buddy-users.mts`, not any test file: #76's `connections_seed_visibility_on_accept` trigger auto-grants a `calendar`-level `visibility_overrides` row on both sides the moment a Connection is accepted — real, correct, and already pgTAP-covered (`connections.test.sql`) — but the seed script accepts its two seeded friendships through that same real flow, so every fresh `supabase db reset` + `npm run seed:users` now leaves the `@amyace`↔`@benbackhand2` pair with an override several specs were written assuming doesn't exist.

- **First attempt was a per-test workaround in `slots.spec.ts`** — clear the override via the Groups page's "Each friend" picker at the top of the one failing test, restore it in `finally`. Reverted before committing: rerunning the fuller suite turned up the identical root cause independently breaking two tests in `friend-groups.spec.ts` (`"a per-friend override beats the group default, and clearing it restores"`, `"two groups resolve to the most permissive of them"`) that also assume this pair starts with no override. A per-test patch would have had to be copied into a second file today and a third the next time something in this area shipped — the actual bug is in the fixture, not in any one spec.
- **Fixed at the source instead: `connect()` now clears both sides' own `visibility_overrides` row for the seeded connection right after accepting it**, every run (not just the run that first accepts) — covers a fresh reset and a re-run against an already-seeded database identically. Still done as the Users themselves (`asUser`, matching the accept step right above it), since `visibility_overrides` is `authenticated`-only, same reasoning the script's own header comment already gives for not using the service-role key here.
- **Deliberately not "fixed" by leaving the auto-granted override in place and updating the specs instead.** The override is genuinely correct new behavior for a *real* friendship; these two seeded pairs predate that trigger by design and several specs (this file among them) depend on them staying at the lattice's bottom specifically so "a friend with no group and no override" has something real to test against. Changing the specs to expect `calendar` by default would just be re-testing #76's own trigger a second time, worse, from the browser.
- **Verified against a fully fresh cycle**: `supabase db reset` (wipes `auth.users`/`connections`/`visibility_overrides`), `npm run seed:users`, then a direct Postgres query confirming zero `visibility_overrides` rows remain for either seeded pair. `slots.spec.ts`, `friends.spec.ts`, `friend-groups.spec.ts`, and `settings.spec.ts` — 30 tests total, including every test either implicated or adjacent — all green in one run, including the specific two `friend-groups.spec.ts` tests this investigation newly found broken.

Verified: `node --test` (406) and `npm run test:rls` (240 pgTAP) both still pass, unaffected — this ticket's only production-relevant file is a local dev script, no application or schema code changed. `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` for `slots.spec.ts` + `friends.spec.ts` + `friend-groups.spec.ts` + `settings.spec.ts`: 30/30 green against a freshly-reset local database and a freshly-started dev server.

**[#65](https://github.com/AdrianLuk/juice-bros/issues/65) (Cancellation sync) is shipped.** All five acceptance criteria are met: a parsed cancellation matching an existing Booking appears as a candidate on the review screen (the Bookings page, same as #64's confirmation candidates); confirming it removes the matched Booking; a cancellation with no matching Booking shows a distinct "no matching booking found" notice instead of being silently dropped; the notice's own Dismiss button (reusing #64's `dismissImportCandidate` as-is) records the outcome so it doesn't resurface; Playwright covers both the matched- and unmatched-cancellation paths.

- **Cancellation matching is Org + date/start-time only, deliberately *not* + court like #63's `isDuplicateBooking`.** #63's own header comment already documents that a real CourtReserve cancellation email carries no Court(s) section at all — `CourtReserveCancellation.courtLabel` is always `null` — so matching on it too would mean a cancellation could never match a Booking that has a real court label logged, which is the common case. `matchCancellationToBooking` (`email-sync-matching.ts`) is a new, narrower sibling to `isDuplicateBooking` rather than a reuse of it, built around what the data actually supports.
- **`processed_gmail_messages.outcome` grew a third value, `cancelled`, distinct from `confirmed`.** #64's own migration comment ties `confirmed` to a Booking being *created*; confirming a cancellation candidate does the opposite (removes one), so reusing `confirmed` would misdescribe what happened even though the mechanics (record an outcome, never revisit it) are identical. Dismissing a cancellation candidate — matched or the "no match found" notice — still uses the existing `dismissed` outcome and `dismissImportCandidate` action unchanged, since that path really is identical either way.
- **`confirmCancellationCandidate` is a new action, not a variant of `confirmImportCandidate`** — the two candidates create/remove opposite things (a real Booking vs. a `bookings.delete`), so there's no shared validation to factor out beyond the allowlist re-check every action in this module already does. `bookingId` comes from a hidden form field the review screen fills from what `syncFromEmail`'s own `matchCancellationToBooking` resolved server-side — never something the User (or a tampered request) picks — and the delete is scoped by `id` alone, RLS turning "isn't yours" into the same empty-result case `deleteBooking` already handles.
- **A cancellation and a confirmation are parsed in the same `syncFromEmail` loop, branching on `parseCourtReserveEmail`'s own `kind`**, rather than a second pass over `unseenIds` — one Gmail fetch per message either way, so there's no reason to walk the list twice.
- **`getBookingsPageData()`'s existing Bookings list grew an `id` field on the plain-data shape `syncFromEmail` builds for matching** (`BookingIdentity & { id: string }`) — #64 never needed the id itself, only the identity fields, so this is additive rather than a reuse of a differently-shaped read.
- **A code review pass ahead of shipping caught two real issues, both fixed before merge.** (1) `matchCancellationToBooking`'s first draft picked the first match on Org+date/start-time with `.find()` — silently wrong the moment a User has two Bookings at the same Org/date/start-time (e.g. two courts reserved for one group session), since a cancellation email carries no court field to disambiguate with; a `confirmCancellationCandidate` submit would then delete whichever Booking happened to come first, not necessarily the one actually cancelled. Fixed by switching to `.filter()` and treating more than one match the same as zero — the "no match found" notice now covers "ambiguous" as well as "nothing on file," refusing to guess rather than risk deleting the wrong, still-valid Booking. (2) `confirmCancellationCandidate` had re-implemented `deleteBooking`'s exact delete-by-id/RLS-empty-result/error-message/revalidate sequence inline; extracted into a shared `deleteOwnedBooking(bookingId)` (`bookings.ts`) both actions now call, the same shared-helper shape `insertValidatedBooking` already established for the create side.
- **An unrelated, concurrent change landed in the same working tree while this ticket was in progress** (`matchOrgByExactName` → `matchOrgByName`, made case/punctuation-fuzzy, plus an "Add facility"-era copy tweak in `org-select.tsx`) — not part of #65's own scope, kept as-is per the user's own call rather than split out, and reverified compatible with this ticket's cancellation-matching code (which only reads `matchOrgByName`'s resolved `orgId`, not how the name matched).

Verified: 415 `node --test` tests pass (7 new for this ticket, `matchCancellationToBooking` in `email-sync-matching.test.ts`; a further handful from the unrelated concurrent change noted above); 241 pgTAP tests pass (1 new, `cancelled` accepted as an outcome in `processed_gmail_messages.test.sql`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` run in full (73/73 green) against a freshly-reset local database and a freshly-started dev server, including `email-sync.spec.ts`'s two new cancellation tests (matched → removes the Booking, unmatched → dismissible notice that doesn't resurface). `supabase migration up --local` applied cleanly. A stray already-running `next dev` on :3000 had to be stopped before Playwright would launch its own server with the Gmail mock's own env vars wired in — same caveat #62's own retrospective already flagged.

**[#88](https://github.com/AdrianLuk/juice-bros/issues/88) (Reconcile confirm/cancel email chains during Sync from Email) is shipped.** All five acceptance criteria are met: a confirm→cancel→confirm→cancel→confirm chain for the same Org/date/start-time nets to a single Import Candidate carrying the last confirmation's own fields, with no leftover confirm candidates or "no matching booking found" notices for the netted-out pairs (reported directly against real captured CourtReserve emails); a lone cancellation with no in-batch confirmation to net against is unaffected, still falling through to #65's existing matched/unmatched behavior; two ambiguous simultaneous confirmations (same facility/date/start-time, different courts) aren't guessed between by an incoming cancellation, same refusal `matchCancellationToBooking` already has for real Bookings; reconciliation orders by the email's own `internalDate`, not Gmail search/fetch order; covered by both `node --test` (the pure reconciliation function) and Playwright (the full five-email chain on the Bookings review screen).

- **Reconciliation is a new pure function, `reconcileCourtReserveEvents`, in `email-sync-matching.ts`** — grouped by facility name (case/punctuation-folded, the same `normalizeFacilityName` `matchOrgByName` already uses) + date + start-time, replayed in `receivedAt` order within each group: a cancellation with exactly one still-active confirmation ahead of it nets both away; zero or more than one active confirmation leaves the cancellation untouched, for `syncFromEmail`'s existing per-email logic to resolve against real Bookings exactly as before. Deliberately generic over the confirmation payload's own shape (`ReconciliationEvent<TConfirmation>`) rather than importing `CourtReserveConfirmation` into the matching module, keeping it as free of the parser's own types as `isDuplicateBooking`/`matchCancellationToBooking` already are.
- **`syncFromEmail` is now a two-phase pipeline**: phase 1 parses every unseen Gmail message into a flat list of `ReconciliationEvent`s (skipping `not_a_booking`/`unparseable`/no-end-time exactly as before, so those stay invisible to reconciliation too); phase 2 runs the *exact same* Org-matching/past-date/duplicate-check/cancellation-matching logic that existed before this ticket, just over `reconcileCourtReserveEvents`'s surviving confirmations/cancellations instead of the raw per-message stream. No behavior changed for anything that doesn't net against another email in the same sync.
- **`fetchGmailMessage` (`gmail-client.ts`) grew a `receivedAt` field** (Gmail's own `internalDate`, epoch ms) on its returned email — reconciliation has to order a chain by when each email actually arrived, and neither `searchGmailMessages`'s result order nor this repo's own fetch-loop order is documented as chronological. Missing/unparseable falls back to `0` (oldest possible) rather than "now," so a message with no timestamp can't masquerade as the most recent event in its own chain and win a netting it shouldn't.
- **Netted-away message ids are deliberately never written to `processed_gmail_messages`.** Only the surviving candidate's own `gmailMessageId` gets recorded when the User confirms/dismisses it, same as before this ticket. The superseded emails stay "unseen" and get re-fetched on every later sync — wasteful but not incorrect, since replaying the same chain always re-nets to the same empty result; recording them would need new plumbing (`confirmImportCandidate`/`dismissImportCandidate` currently take one id, not a list) for a cost this ticket judged not worth it. Matches the existing posture the module's own header comment already documents for `not_a_booking`/`unparseable` parses: "nothing actionable to remember either way."
- **`gmail-mock.ts`'s `MockGmailMessage` grew an optional `receivedAt`**, echoed back as the fixture message resource's own `internalDate` — `email-sync.spec.ts`'s new chain test registers all five messages with out-of-registration-order timestamps specifically to prove reconciliation sorts by `receivedAt`, not array/search order.

Verified: 421 `node --test` tests pass (6 new, `reconcileCourtReserveEvents` in `email-sync-matching.test.ts`); no pgTAP changes (no schema touched — this ticket is pure application logic); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` run for `email-sync.spec.ts` (11/11 green, including the new five-email chain test) and `bookings.spec.ts` (6/6 green, unaffected) against a freshly-started dev server and the existing local Supabase stack.

**[#91](https://github.com/AdrianLuk/juice-bros/issues/91) (Reservation Update sync during Sync from Email) is shipped.** CourtReserve's "Reservation Update Notice" — sent when a logged reservation's format, players, or court changes — previously matched neither the confirmation nor cancellation subject pattern, so `parseCourtReserveEmail` classified it `not_a_booking` and "Sync from Email" silently dropped it. All three acceptance criteria are met: a Confirmation + Update Notice for the same slot in one sync nets to a single candidate carrying the update's own fields; an Update Notice alone, matching a logged Booking, appears as a candidate whose "Apply update" edits that Booking's format/court in place rather than creating a new one; an Update Notice with no matching Booking shows a distinct, dismissible "no matching booking found" notice, same as an unmatched cancellation.

- **Prompted by an actual real captured email** (Gmail's "Show original" on a genuine "Reservation Update Notice"), fetched before any parsing code was written — this repo's own history (courtreserve-email.ts's header comment) already documents a first version built from a best-guess reconstruction turning out wrong, and this ticket wasn't going to repeat that. The real body confirmed the design difference from a confirmation: an `<h4>Reservation Details</h4>` heading (not "Details") whose `<h5>` value bundles the court label in as a fourth `<br>`-joined line, with no separate `Court(s)` section at all.
- **Scope was deliberately both same-sync netting *and* cross-sync matching, not just the former.** The two emails (original confirmation, later update) will often land in different sync clicks in practice — a User who confirms promptly gets the Update Notice alone on a later sync — so netting-only would have left the exact gap this ticket exists to close for the common case. Mirrors how #65 (cancellation-to-Booking matching) and #88 (in-batch reconciliation) already work together as two halves of one feature, rather than introducing a third pattern.
- **`reconcileCourtReserveEvents` (#88) grew a third branch inside its existing replay loop**, not a second function: an `update` event with exactly one active confirmation *replaces* it (fields and `gmailMessageId` both become the update's own, same "only the surviving id gets recorded" posture #88 already established for a netted cancel/confirm pair); zero or more than one active confirmation leaves it in a new `updates` bucket, the same refusal-to-guess shape an unresolved cancellation already has.
- **`matchUpdateToBooking` is Org + date/start-time only, *not* + court — a deliberate divergence from `isDuplicateBooking`'s stricter match, even though (unlike a cancellation) an update genuinely carries a court label.** Requiring court equality would mean a real court change could never be matched at all, which defeats the point of an "apply update" action that's supposed to be able to write exactly that kind of change to the Booking on file. New, narrow sibling to `matchCancellationToBooking` rather than a shared helper, same precedent #65 already set for not merging these.
- **Applying a matched update only ever writes `format`/`court_label`, never `starts_at`/`ends_at`.** The match itself is date/start-time-scoped, so the slot is already known unchanged by construction; players are reference-only display data, same as a confirmation candidate's `matchedPlayers` — a Booking has never stored who's playing, so an update whose only real change is the player list correctly has nothing to write, just an outcome to record so the notice stops resurfacing.
- **`processed_gmail_messages.outcome` grew a fourth value, `updated`, distinct from `confirmed`/`cancelled`/`dismissed`** — same one-migration-per-new-outcome pattern #65 established, and same reasoning: reusing `confirmed` would misdescribe an edit-in-place as a fresh Booking.
- **Facility/player names in the real-fixture unit test were swapped for placeholders before commit** (this repo is public), continuing the convention `courtreserve-email.test.ts`'s own file header already documents for the confirmation/cancellation fixtures.

Verified: 436 `node --test` tests pass (15 new — 5 in `courtreserve-email.test.ts` for update parsing plus the real-fixture regression test, 10 in `email-sync-matching.test.ts` for `matchUpdateToBooking` and update-netting); 242 pgTAP tests pass (1 new, `updated` accepted as an outcome in `processed_gmail_messages.test.sql`); `npx tsc --noEmit`, `npm run lint`, and `npm run build` are all clean. `npm run test:e2e` run for `email-sync.spec.ts` (17/17 green, including 3 new: same-sync netting, cross-sync matched update, cross-sync unmatched notice) and `bookings.spec.ts` (6/6 green, unaffected) against a freshly-started dev server and the existing local Supabase stack. `supabase migration up --local` applied cleanly.

**[#93](https://github.com/AdrianLuk/juice-bros/issues/93) (Booking name) is shipped, in three tickets.** A Booking's calendar block only ever showed the facility it was at, so two unrelated games at the same place looked identical at a glance, and CourtReserve's own confirmation emails already state a name for the reservation ("Singles", "Men's Partners Play 3.5 and up Tourney Style") that Sync from Email threw away.

- **[#94](https://github.com/AdrianLuk/juice-bros/issues/94) (schema, manual entry, display) is shipped** as [#105](https://github.com/AdrianLuk/juice-bros/pull/105). Nullable `bookings.name text` with a `char_length <= 60` check constraint, mirroring `court_label`'s own pattern; `parseNewBooking` trims, folds blank to `null`, rejects over-length. Renders on its own line *above* the facility name everywhere the owner's calendar already shows the facility — week/month chips, agenda row, Bookings list row, detail popover, dashboard "Coming up" sidebar — nothing extra renders when a Booking has none, and no name is fabricated for a Booking logged before the field existed. The friend-facing calendar (`friend_visible_bookings`) is untouched, same as every other owner-only column. Verified: 439/439 `node --test`, 245/245 pgTAP (3 new constraint tests), full e2e green.
- **[#95](https://github.com/AdrianLuk/juice-bros/issues/95) (name from the Booking Confirmation template) is shipped** as [#106](https://github.com/AdrianLuk/juice-bros/pull/106). `CourtReserveConfirmation` grows a `name` field — the Details section's own first line, the same raw text `format` was already derived from — threaded through `syncFromEmail`/the confirm form/`ImportCandidateCard` the same way `format`/`courtLabel` already were. Verified: 440/440 `node --test`.
- **[#96](https://github.com/AdrianLuk/juice-bros/issues/96) (Registration Confirmation template) is shipped** as [#107](https://github.com/AdrianLuk/juice-bros/pull/107). CourtReserve's *second* confirmation template — used for program/tournament sign-ups, subject "Registration Confirmation - ..." rather than "Booking Confirmation for ..." — reads `Event` in place of `Details` and `Registered Team(s)` in place of `Player(s)` (stripping that section's own "Team:" label before splitting names), producing the identical `CourtReserveConfirmation` shape the first template already does, so nothing downstream needed to change. An Event name that isn't literally "Singles"/"Doubles" (e.g. "Men's Partners Play 3.5 and up Tourney Style") still falls back to the doubles default; there is still no cancellation counterpart for this template, since no real sample exists to build one against. Per-kind section headings moved from nested ternaries to typed lookup tables so a third template variant is a one-line addition. Verified: 446/446 `node --test`; a multi-angle `/code-review` pass caught and fixed two real issues before merge — the "Team:" strip leaking onto ordinary Player(s) parsing, and the nested-ternary heading selection losing exhaustiveness checking.

**[#104](https://github.com/AdrianLuk/juice-bros/issues/104) (document the Onboarding domain model) is shipped**, docs-only ahead of #103's implementation: the "Onboarding" glossary entry in `booking-buddy/CONTEXT.md` and [adr/0012](docs/adr/0012-onboarding-surfaces-gender-proactively.md), which reverses the prior "don't nag about Gender" stance specifically for this one surface — the field stays optional, only its visibility changes.

**[#103](https://github.com/AdrianLuk/juice-bros/issues/103) (Onboarding modal for first Facility + optional Gender) is shipped** as [#108](https://github.com/AdrianLuk/juice-bros/pull/108). A dismissible `OnboardingModal` on the dashboard, shown whenever the signed-in User currently has zero Facilities — computed live from the Org count on every load, no stored "seen it" flag, so it resurfaces for any zero-Facility User and stops the moment they have one. Composes the existing Place-search flow, hand-typed fallback, and Gender form unchanged; lets a User add several Facilities in one sitting, closed via an explicit Done. `createOrg`/`pickPlace` now also revalidate the dashboard route so a Facility added from the modal shows up immediately in the Booking form's picker. The Gender fetch that seeds the modal only runs when the caller has zero Facilities, so onboarding-complete Users don't carry a permanent extra read on the app's most-visited route. Verified: 440 `node --test`, 245 pgTAP, full 86-test Playwright suite green including a new 8-test `e2e/onboarding.spec.ts`.

**[#97](https://github.com/AdrianLuk/juice-bros/issues/97) (Edit an existing Booking) is shipped** as [#109](https://github.com/AdrianLuk/juice-bros/pull/109). Until this ticket, a wrong court number or time meant deleting and relogging the whole Booking. `updateValidatedBooking`/`updateBooking` mirror `insertValidatedBooking`'s org-ownership re-check, past-date check, and `bookingWriteMessage` translation, reusing `parseNewBooking` as-is rather than a second parser — no new migration, since `bookings_coherent` already fires on update and RLS already turns "isn't yours" into an empty result. The org-ownership/past-date validation is shared between insert and update via a new `resolveValidatedOrg` helper so the two paths can't drift apart. `EditBookingForm` reuses `CreateBookingForm`'s field set (extracted into `BookingFieldSet`); "Edit" sits next to "Remove" in the Bookings list row and the calendar popover.

- **A known, deliberate gap, called out in the PR itself**: editing a Booking's `format` after it's already attached to a Slot did *not* re-derive the frozen `slot_bookings.format` copy — confirmed by reading the migrations, since `slot_bookings_coherent` only fired `before insert`, never `before update`. This is exactly the gap ADR 0008 had flagged and ADR 0011 was supposed to resolve, but the trigger the ADR's own "Consequences" section claimed existed didn't — a documentation error, caught while shipping this ticket, corrected by #102 below rather than silently patched here.

Verified: 446 `node --test`, 245 pgTAP (unaffected, no migration), full `e2e/bookings.spec.ts`/`dashboard.spec.ts` (14 tests) including a new edit-from-the-list-row test.

**[#98](https://github.com/AdrianLuk/juice-bros/issues/98) (Player names on Bookings) is shipped, in three tickets**, closing the gap where Sync from Email already parsed and matched player names for the review screen's "With: ..." display, then threw the match away the instant a Booking was confirmed.

- **[#99](https://github.com/AdrianLuk/juice-bros/issues/99) (schema, matching, manual entry) is shipped** as [#110](https://github.com/AdrianLuk/juice-bros/pull/110). New `booking_players` (`booking_id` FK cascade, `name` not-blank/≤40 chars mirroring `court_label`, `connection_user_id` nullable FK `on delete set null`); RLS mirrors `bookings`' owner-only posture via a subquery on the parent's `owner_id` — no coherence trigger needed, since the RLS `with check` alone already covers "only your own Booking." **`matchPlayerNamesToConnections` changes behavior for real, per [ADR 0011](docs/adr/0011-booking-players-matched-once-not-live.md)**: a name matching more than one Connection now resolves *unlinked* rather than "first match wins," because the result is now persisted (a standing misattribution nobody reviews again) rather than the disposable email-review hint it originally served. `parseNewBooking` gains a comma-separated, trimmed, blank-filtered `players` field, sharing a new `splitPlayerNames` helper with `courtreserve-email.ts`'s own player parsing rather than duplicating it. Manually verified against two real Connections both named "Ben Backhand": both names landed on the Bookings row, and the DB confirmed the ambiguous one stored `connection_user_id: null` instead of guessing. Verified: 450/450 `node --test`; 262 pgTAP across 20 files, 17 new in `booking_players.test.sql` covering both cascade behaviors and the RLS boundary; `bookings.spec.ts`/`dashboard.spec.ts` (14) unaffected.
- **[#100](https://github.com/AdrianLuk/juice-bros/issues/100) (Sync from Email wiring) is shipped** as [#111](https://github.com/AdrianLuk/juice-bros/pull/111). `confirmImportCandidate` now carries a candidate's parsed player names through as a hidden field, reusing #99's matching/write path — and re-matches against the caller's *current* Connections at confirm time (the real add-time, per ADR 0011), not whatever was true when the review screen first rendered. `ImportCandidateCard` stays unchanged in shape; `matchedPlayers` is still read-only display. Names are truncated to the max length before submit defensively — a parsing quirk on an over-long name shouldn't block confirming the whole Booking, a real issue found and fixed during review. Verified: 450/450 `node --test`.
- **[#101](https://github.com/AdrianLuk/juice-bros/issues/101) (included in Edit Booking) is shipped** as [#112](https://github.com/AdrianLuk/juice-bros/pull/112). `EditBookingForm` gets the same Players input, pre-filled from the Booking's existing rows. `updateBooking` replaces Players as a **row-level delta**, not a wholesale delete+reinsert, via new `diffBookingPlayers` (pure, in `email-sync-matching.ts`): a name that pairs one-for-one with an existing row is left completely untouched — its stored Connection link survives, per ADR 0011 — and only an added/edited name gets re-matched, only an orphaned row gets deleted. Verified: 458/458 `node --test` (new `diffBookingPlayers` coverage), 262 pgTAP (no schema change), `e2e/bookings.spec.ts` (9/9) including a new test adding/editing/removing Players through Edit.

**[#102](https://github.com/AdrianLuk/juice-bros/issues/102) (harden Booking edit against stale Slot Capacity) is shipped** as [#113](https://github.com/AdrianLuk/juice-bros/pull/113), closing the gap #97's own notes flagged and correcting the ADR 0008/0011 documentation error found while shipping it. New migration `20260820090000_sync_slot_booking_format_on_edit.sql`: an `after update on bookings` trigger, `sync_slot_booking_format`, re-derives an already-attached `slot_bookings` row's copy of `format` the moment the Booking it mirrors is edited. A dedicated trigger function rather than retargeting `assert_slot_booking_coherent` — that function's contract is "validate + stamp `NEW` on the row being *inserted*," which has nothing to do on a `bookings` UPDATE. `security definer`, the same reason `seed_visibility_overrides_on_accept` needs it: `slot_bookings` deliberately carries no UPDATE policy for `authenticated`. ADR 0008 updated to record the gap as actually closed this time. Verified: 264/264 pgTAP (new: editing an attached Booking's format updates the mirrored `slot_bookings.format`; `bookings_not_in_the_past` confirmed still insert-only), 458/458 `node --test` (no app code touched).

### A run of smaller, UI-focused tickets followed

Mostly single-PR fixes, no schema, no new domain vocabulary:

- **Mobile dialog scrolling, two separate real bugs.** First ([#116](https://github.com/AdrianLuk/juice-bros/pull/116)): a tall dialog (e.g. Edit booking) had no height cap on mobile and grew past the viewport, pushing the close button off-screen — `DialogContent` now caps at `100dvh - 2rem` and scrolls its content internally while the close button stays pinned to the popup's own corner; verified with a throwaway Playwright script showing the close button move from off-screen (y:-70) to reachable (y:24). Second ([#122](https://github.com/AdrianLuk/juice-bros/pull/122), filed after the first fix didn't fully hold): the outer Popup used CSS `grid`, whose default row track grows to fit content instead of shrinking to the height cap, so the inner `overflow-y-auto` div never actually got constrained and just got visually clipped — fixed by switching the Popup to `flex flex-col`, whose default `flex-shrink: 1` actually shrinks the inner div and activates its scroll. Confirmed via a throwaway script asserting `scrollHeight > clientHeight` and that scrolling moves `scrollTop`, which it didn't before this fix.
- **[#114](https://github.com/AdrianLuk/juice-bros/pull/114) right-aligns every modal's submit button** on mobile and desktop (`items-start` → `items-end`), across every Booking Buddy form.
- **[#115](https://github.com/AdrianLuk/juice-bros/pull/115) auto-scrolls the mobile dashboard Week view to today's column** — below `lg`, day columns keep a minimum width wider than a phone screen, so "today" could be off-screen with no indication a sideways scroll was needed; now centers today's column on mount/week-change whenever the row actually overflows.
- **[#117](https://github.com/AdrianLuk/juice-bros/pull/117) hides past Bookings behind a collapsed "History" accordion** on the Bookings page — only upcoming (not-yet-ended) Bookings show under "Booked" by default, consistent with the `notEndedBefore` reasoning the dashboard calendar already uses; **[#123](https://github.com/AdrianLuk/juice-bros/pull/123)** then dropped the past-Bookings count next to the History trigger.
- **[#119](https://github.com/AdrianLuk/juice-bros/pull/119) prefixes the shortened weekday onto email-imported candidate dates** (`Wed 08-19-2026` instead of a bare `MM-DD-YYYY`) on the Sync from Email review cards.
- **[#120](https://github.com/AdrianLuk/juice-bros/pull/120) and [#121](https://github.com/AdrianLuk/juice-bros/pull/121) move and then merge per-friend Visibility into the friends page.** The "Each friend" override control moved off `/booking-buddy/groups` onto `/booking-buddy/friends` (`getGroupsPageData` no longer resolves overrides, a new `getFriendVisibilityList()` does), then the resulting two back-to-back lists on the friends page ("Your friends" and "Each friend," same people twice) were merged into one row per friend carrying both the visibility control and the calendar-link/remove actions — dropping a redundant `connections` read in the process.
- **[#124](https://github.com/AdrianLuk/juice-bros/pull/124) adds the ability to delete a Slot.** Owner-only `deleteSlot`, following the same "select the deleted row back, empty result = not yours" convention as `deleteFriendGroup`/`deleteOrg`/`deleteOwnedBooking`; cascades to Responses, attached courts, the Invite link, and Reminder sends via existing FKs, but leaves any attached Bookings themselves untouched — the same "app record vs. real reservation" distinction `detachBookingFromSlot` already draws. The detail page 404s after deletion, so the action redirects to the Slots list instead of revalidating in place.
- **[#125](https://github.com/AdrianLuk/juice-bros/pull/125) and [#126](https://github.com/AdrianLuk/juice-bros/pull/126) rework the mobile nav FAB** — a menu icon instead of the brand logo (so it reads as a menu trigger, not a home link) with the dashboard quick-add buttons pushed clear of it, then the nav sheet itself changed from a full-screen drawer to a compact panel anchored to the bottom-right corner near the FAB, with staggered corner-anchored item animation.
- **[#127](https://github.com/AdrianLuk/juice-bros/pull/127) adds a Booking details modal**, shared between the dashboard's "Coming up" sidebar (click a row — previously did nothing) and a new "View" button on every Bookings-list row, both opening one `BookingDetailsModal` that also surfaces the existing Edit/Remove actions inline.
- **[#128](https://github.com/AdrianLuk/juice-bros/pull/128) reorders both the Bookings and Slots pages**: Sync from Email now sits directly above Log a booking (was at the very top); Post a slot moved to the bottom of the Slots page, after Your slots and From your friends — matching the log-form-at-the-bottom pattern the Bookings page already used.

**[#129](https://github.com/AdrianLuk/juice-bros/issues/129) (privacy policy page) is shipped** as [#129](https://github.com/AdrianLuk/juice-bros/pull/129). `/booking-buddy/privacy` covers account data, Bookings/friends/Slots data, push notifications, sharing, security, and retention/deletion, plus a dedicated "Sync from Email (Gmail)" section spelling out exactly what's read (`notifications@courtreserve.com` only, last 90 days, on-demand only), what's extracted vs. stored, how the refresh token is secured, and how to disconnect — that section only renders for a caller `isEmailSyncAllowed` passes, since most accounts don't have the feature enabled. Linked from every Booking Buddy page's footer, including sign-in, which is why it's also added to `routes.ts`'s public subpaths (reachable signed out, with its own routes test) rather than sitting behind the auth gate like the rest of the section. Verified: 459/459 `node --test`, clean build with the new route rendering dynamic like the app's other session-aware pages.

**[#130](https://github.com/AdrianLuk/juice-bros/issues/130) (pick a facility when posting a slot) is shipped** as [#130](https://github.com/AdrianLuk/juice-bros/pull/130). The "Post a slot" form gets an optional Facility field, reusing the existing `intended_org_id` mechanism (#36's Booking Window Reminder hint) instead of forcing a separate save on the Slot detail page afterward — no migration needed, since the column and its ownership-coherence trigger already existed and already fired on insert. `OptionalOrgSelect` extracted out of `IntendedOrgForm` so the create form and the detail-page "Planning to book at" form share one picker instead of two copies able to drift apart, the same reasoning `OrgSelect` was extracted for back in #64.

**[#131](https://github.com/AdrianLuk/juice-bros/issues/131) (optional notes field on Bookings and Slots) is shipped** as [#131](https://github.com/AdrianLuk/juice-bros/pull/131). One migration adds a free-text `notes` field (500 chars) to both `bookings` and `slots`, mirroring `bookings.name`'s not-blank/length constraint shape. Bookings: a notes textarea in Log/Edit, shown only in the Booking details modal (#127), never on the compact list row. Slots: a notes textarea on "Post a slot," plus a new `setSlotNotes` action and inline edit form for afterward — since a Slot has no general edit flow once posted, this mirrors the existing `rotation_buffer`/Intended Org pattern instead — shown on the Slot detail page, editable for the owner and read-only for friends. `booking-buddy/CONTEXT.md`'s Booking and Slot entries both updated in the same PR. Verified: 467/467 `node --test`, new Playwright coverage for both entities' create/edit/display paths.

**[#132](https://github.com/AdrianLuk/juice-bros/issues/132) (Sync from Email fails to parse a bare-ISO Registration Confirmation date) is fixed** as [#133](https://github.com/AdrianLuk/juice-bros/pull/133) — the first real-world parser bug caught after #96 shipped. A real captured "Registration Confirmation" email (a tournament sign-up via a facility's own CourtReserve-hosted booking site) has an Event section carrying a bare ISO date (`2026-08-25`) with no weekday prefix, unlike every fixture #96 was built against ("Weekday, M-D-YYYY," e.g. "Friday, 8-21-2026"). `parseHumanDate` rejected it outright, so the whole email came back `unparseable` and Sync from Email silently dropped it. `parseHumanDate` now checks for `YYYY-MM-DD` first, falling back to the existing weekday-prefixed shape; a regression test built from the real email's own Event block shape guards it. Verified: 468/468 `node --test`.

**[#200](https://github.com/AdrianLuk/juice-bros/issues/200) (`dashboard.spec.ts` fails from a clean DB) is fixed.** #176's onboarding-modal trigger change (`orgs.length === 0` → "no Booking **and** no Slot") meant a freshly-seeded AMY — accounts and friendships only, no Bookings or Slots — now trips the modal on the dashboard, and its intent-choice dialog intercepted every click in four `dashboard.spec.ts` tests (`:72`/`:165`/`:187`/`:214`). A full-suite run masked it because an earlier spec left AMY a stray Booking; the spec failed run first or alone. Fixed in the spec, not the seed script or the app: `beforeEach` now pre-sets the modal's own `localStorage` dismissal snooze (`bb-onboarding-snoozed-until`) via `addInitScript` before the first paint, rather than the app-state assumption (a Facility suppresses onboarding) that #176 invalidated. The two view-only tests that added a Facility purely to dodge the old modal trigger dropped that now-pointless fixture. Not "seed AMY a Booking" — that would ripple into every other spec's assumptions about her starting state. Verified: the 4 named tests plus the rest of `dashboard.spec.ts` green run first/alone against a clean `supabase db reset` + `npm run seed:users`.

### State as of now

**Every ticket in the graph is shipped — GitHub Issues has zero open issues** for this repo as of 2026-08-25. Booking Buddy is a complete, deployed friend-group scheduling app: auth, friend connections and visibility, Facilities (Google Places-backed and hand-named), Bookings (with Players, an editable name/notes, and full in-place editing), bare-proposal-to-confirmed Slots with gendered Capacity signals and Slot Links/Guest RSVP, Availability Windows, a dashboard Month/Week/Agenda calendar with a friend-calendar view, email/push Reminders plus a Booking Window Reminder, PWA installability, Sync from Email (CourtReserve confirmation/cancellation/update/registration parsing with cross-email reconciliation), and a privacy policy.

**Still deliberately not linked from the public site**: Booking Buddy has no entry in `src/data/apps.ts`, so it doesn't appear on `/tools` or in the sitemap — see the "Deferred follow-up" note under Phase 0. That was written when there was nothing usable to link to; that's no longer true, so whether to flip it on is now a product decision (soft-launch to real users, allowlist Gmail access for them, decide on a public/waitlisted framing) rather than an engineering blocker. Nothing to pick up here without a new ticket — check GitHub Issues first, the same standing instruction every earlier checkpoint in this file has repeated.

**[#179](https://github.com/AdrianLuk/juice-bros/issues/179) (onboarding funnel analytics) is shipped.** Custom Vercel Analytics events at each signup→first-value step, emitted server-side from a new `src/lib/booking-buddy/analytics.ts` via `@vercel/analytics/server` inside `after()`, each gated on a 0→1 database check (no client trust, naturally idempotent): `bb_signup` (set-once via new `profiles.funnel_signup_at`, fired from all three auth entry points), `bb_first_facility` (`createOrg` + `pickPlace`), `bb_first_booking` (`insertValidatedBooking`, so manual + Sync from Email both), `bb_first_slot`, `bb_first_friend` (accepter side only), `bb_slot_first_response` (`respondToSlot` + `guestRespondViaLink`, count-before so the upsert can't re-fire it). No payloads carry PII. `bb_onboarding_intent` is deferred to #176, which builds the intent selector it depends on. The read is a manual Vercel Analytics console activity — `docs/onboarding-funnel.md`. See [adr/0014](docs/adr/0014-onboarding-funnel-analytics.md) for the server-side/count-check rationale and the `bb_first_friend`/`bb_slot_first_response` attribution limits. No new `node --test`/pgTAP: the added logic is a count check plus a guarded external call, and the seam policy at the top of this file says don't build a harness for that.

**[#176](https://github.com/AdrianLuk/juice-bros/issues/176) (intent-branched onboarding modal) is shipped.** `OnboardingModal` now branches its body on a modal-local intent choice ("track my court bookings" vs "get my group on a time"), each firing `bb_onboarding_intent` client-side (completing #179's deferral). Track branch: add a Facility (search + hand-typed, equal weight — no more collapsed `<details>`), then log a Booking against it, then a "it's on your calendar" confirm. Coordinate branch: post a Slot pre-filled to next Monday 8-10pm (no facility needed), then a "send it to your group" step with the Slot Link + friend search; Gender moved here, collapsed, labelled with its real purpose. Both branches keep a persistent friend-search footer. **Trigger changed** from `orgs.length === 0` to "no Booking **and** no Slot" (`getDashboardPageData` gains `hasSlot`), plus a ~7-day `localStorage` dismissal snooze so it stops nagging. `createSlot` now returns the new Slot's id; `CreateSlotForm` gains `defaultDate` + `onPosted`. Every form is the real page form, reused unchanged. Invite link (#175) still open, so the share step is Slot Link + friend search only. Docs: `CONTEXT.md` "Onboarding" rewritten, [adr/0012](docs/adr/0012-onboarding-surfaces-gender-proactively.md) amended (Gender narrows to the coordinate branch), new [adr/0015](docs/adr/0015-onboarding-intent-branch.md). `e2e/onboarding.spec.ts` and `e2e/onboarding-empty-states.spec.ts` rewritten for the new flow. No migration.

**[#175](https://github.com/AdrianLuk/juice-bros/issues/175) (personal invite link) is shipped.** Every User carries a rotatable `profiles.invite_token` (24 URL-safe base64 chars, `security definer` `unique_invite_token()`), assigned automatically at signup the same way `username` is and backfilled onto existing rows — `handle_new_user` grew one column, its body otherwise carried over verbatim from `20260825120000`. Surfaced as `/booking-buddy/join/<token>` (`JOIN_PATH` in `routes.ts`, added to `PUBLIC_SUBPATHS` so it's reachable signed out, and `noindex` in `robots.ts` + the page's own metadata). See CONTEXT.md's new "Invite Link" entry.

- **The join route branches like `/booking-buddy` itself does.** Signed out: a `<Name> invited you` landing (owner resolved via the new `invite_link_owner(token)` definer function — granted to `anon`, name/handle only, the same "narrow definer, not a widened policy" shape as `search_users`) whose one button (`startInviteSignIn`) stashes the token in a short-lived `bb_invite` cookie and redirects into normal sign-in. Signed in: an explicit "send request" button (reusing `sendConnectionRequest` unchanged, duplicate-pair guard and all) for a stranger, or a friendly no-op line for an existing friend / your own link.
- **Token survives the auth round trip in a cookie, not `?next=`** — `safeRedirectTarget` only accepts *gated* Booking Buddy paths and would drop a public `/join` target. `consumeInviteCookie` (`invite-connection.ts`, `server-only`) reads and clears it, then creates the pending request; wired into all four first-authenticated entry points (auth callback route + password / Google / sign-up actions), inline rather than in `after()` since it clears a cookie. The auto-created request is always *pending* — a link never makes an accepted Connection silently.
- **Rotation** is `rotate_invite_token()` (`security invoker`, RLS scopes the UPDATE to the caller), behind an `AlertDialog` confirm in `InviteLinkPanel`. The panel (link + copy + reset) shows on the Friends page and in the onboarding friend-footer (`OnboardingModal` gains an `inviteUrl` prop, fetched only under the same no-Booking-no-Slot condition the modal itself needs).
- **Pure fn** `parseInviteToken` (`invite-links.ts`) extracted for the token-shape validation, mirroring the `invite_token_format` check. Tested in `invite-links.test.ts`.
- Verified: 560 `node --test` (6 new in `invite-links.test.ts`, 2 in `routes.test.ts`), 287 pgTAP (18 new in `invite_links.test.sql` — uniqueness, auto-assignment, rotation, the auto-request respecting `connections_unique_pair`), full Playwright suite incl. new `e2e/invite-links.spec.ts` (fresh signup through a link → pending request → accept → connected → no-op on re-open → own-link no-op; plus signed-in send-from-link and an invalid token). `npx tsc --noEmit`, `npm run build` clean. Migration applied local; since `db push`ed to the hosted project.

**[#195](https://github.com/AdrianLuk/juice-bros/issues/195) (group availability overlap — "Find a time") is shipped.** A read-only view at `/booking-buddy/overlap` — Plan's third child, label "Find a time" — that picks a set of friends and shows the days/times the viewer and all of them are free over a Week or Month range, each free day deep-linking into the Games form (`?date=&start=`, prefilled) via a new `id="post-a-game"` anchor.

- **"Free" = not busy, and unspecified counts.** The decision the user corrected mid-plan: a moment is free unless a Booking/confirmed Slot covers it or the Availability Window winning ADR 0006 precedence there is `busy` — a person who has declared nothing is treated as available, not unknown. So the view surfaces a time as soon as nobody is actually busy, rather than only when everyone has painstakingly marked open time.
- **Pure seam:** `resolveCommonOpenSegments` (`availability.ts`) — a boundary sweep like `resolveAvailabilitySegments`, but calls `resolveAvailability` per person per sub-interval and keeps a slice only where every person is `!== "busy"`. 8 new `node --test` cases. `resolveAvailabilitySegments` was the wrong primitive (it drops unspecified spans, which we now want).
- **No new schema, no new access path.** `getFriendAvailability` re-filters the client's id list through the existing `open_time_visible_owners` RPC, then `.in("owner_id", ...)` on `friend_visible_bookings` + `availability_windows` — both already gated by `has_open_time_visibility` (issue #61's view/policy). The viewer's own busy time is their `bookings` rows (a confirmed Slot's Booking is already one) + their windows, from `getOverlapPageData`. On TanStack Query per CLAUDE.md's interactive-route trigger (friends' data re-fetches on selection change; range + viewer data are local state).
- **Result list clips to daytime hours (07:00–23:00) for display only** — an unconstrained day would otherwise read as a midnight-to-midnight bar; the intersection math stays full-day, and a fully-free day renders "Any time".
- **One row per free window, not per day.** A day split by someone's midday busy stretch shows a window before it and a window after, each with its own "Propose a game" seeding *that* window's start time (`proposeHref(dateKey, block)`) — the earlier draft joined the windows into one line and the button always picked the first.
- Nav: `BB_SECTIONS` gains the third Plan child; `BbAppShell`'s dropdown and `BbSectionNav`'s pill row both already `.map` children, so no component change. `routes.test.ts` updated (the `siblingsForPath` Plan assertions now expect three labels).
- Range starts at the next whole hour from *now*, not local midnight, so today's gone hours aren't offered and a same-day "Propose a game" can't seed a past start time. The slots-page prefill only shape-checks the date (`isRealDate`) — the form's own submit guard + `slots_not_in_the_past` are the real gate, and a tighter check would need a time zone the deep-link doesn't carry. If every picked friend has since lost visibility, the result shows "no longer shares their open time" rather than a viewer-only overlap dressed up as a group one.
- Verified: 576 `node --test` (8 new in `availability.test.ts` for `resolveCommonOpenSegments`, 1 new + 2 amended assertions in `routes.test.ts`), no pgTAP change (no schema), `npx tsc --noEmit` / `npm run lint` / `npm run build` clean. New `e2e/overlap.spec.ts` (3 tests): a friend without `open_time` never appears in the picker; with a grant, A sees the range free and the deep-link opens the Games form with the date prefilled, then B blocks the whole range busy and A sees "No shared free time"; a friend busy 12:00–14:00 makes that day render two proposable windows with different start times.
- Landing page (`booking-buddy-landing.tsx`): a fourth `FeatureRow` ("Find a Time" / "See when the whole group is free") with an `OverlapPreview` mockup, plus a "How do I find a time that works for everyone?" FAQ that flows into the FAQPage JSON-LD. The `/overlap` route is auth-gated and non-indexable (and now `robots.txt`-disallowed alongside `/availability`, which #197 missed), so the landing is the feature's actual SEO/AEO surface.

**A Game shows its facility in the title even as a bare proposal.** Before, `Slot.facilityLabel` (the ` · <facility>` suffix on every game row and the detail-page `<h1>`) only appeared once a court was booked — it read `slot_bookings.org_name` alone. A proposal with an Intended Org set showed no facility anywhere a friend could see it, so "which facility is this proposal for?" had no answer without opening the owner-only edit form. Now `facilityLabel` falls back to the Intended Org when no court is attached, in both `listSlots` and `getSlotDetail`.

- **New migration `20260828120000_add_slot_intended_org_name.sql`** — a `slots.intended_org_name` snapshot column, the same friend-visible-resolved-text trick `slot_bookings.org_name` (`20260826100000`) uses, and for the same reason: `intended_org_id` points at owner-only `orgs`, so a friend who can read the `slots` row has nothing to join it against. `assert_slot_intended_org_coherent` is re-derived to stamp the resolved name (`orgDisplayName` shape: typed name → cached Place name → fallback) on every insert/update, and to clear it when `intended_org_id` goes null — including the FK's own `on delete set null`, which arrives as an update the trigger sees. No orgs-side re-sync trigger, matching `slot_bookings.org_name` (Orgs are created, never renamed).
- The booked-court facility still wins when both exist; the status badge (Proposal / Court booked) is what distinguishes the two cases, so the title itself doesn't need to say "planning to".
- **The Slot Link guest preview (`/s/[token]`) shows it too** — a guest arriving from a WhatsApp link should be able to see which facility the game is for. `getSlotByToken` already runs through the admin (service_role) client with the token as the whole authorization, so it just reads the same two friend-visible snapshot columns (`slot_bookings.org_name` / `slots.intended_org_name`) and applies the same `facilityLabel(...) ?? intendedOrgName` fallback; `orgs`/`bookings` stay closed. Before this the guest preview showed no facility at all, even for a booked court.
- Verified: 608/608 `node --test`, full pgTAP incl. 3 new assertions in `booking_window_reminders.test.sql` (column exists; setting `intended_org_id` stamps the name; clearing it clears the name), `npx tsc --noEmit` / `npm run lint` clean. `e2e/slots.spec.ts` extended: the facility shows in the row title and the detail `<h1>` for a still-bare proposal, both when picked at creation and when set afterward; `e2e/slot-links.spec.ts` extended: a guest sees the facility in the preview heading for a bare proposal. Migration applied local; since `db push`ed to the hosted project.

**[#228](https://github.com/AdrianLuk/juice-bros/issues/228) (friend-request email with one-click Accept / Decline) is shipped.** A pending friend request was silent — the addressee only found it on `/booking-buddy/friends`. Now they get an email the moment one is created (all three creation paths: Friends-page request, Invite Link "connect", first signup through an Invite Link), with **Accept** and **Decline** links that work in one click straight from the inbox, no sign-in. See CONTEXT.md's new "Connection Request Email" entry and [ADR 0017](docs/adr/0017-connection-request-email-is-session-less.md).

- **New migration `20260831120000_connection_request_emails.sql`.** `connection_request_links` — a `service_role`-only single-use token table, same posture as `slot_links`/`guest_rsvp_log` — plus an `after insert on connections when (new.status = 'pending')` `security definer` trigger that mints one per Connection (the "database trigger, not app bookkeeping" reasoning the default-facility mark uses, so no creation path has to remember). Plus `notification_preferences.connection_request_email_enabled` (its own opt-out, independent of the two Reminder toggles, default on) and `grant select, update, delete on connections to service_role` for the respond path.
- **Session-less by deliberate decision (ADR 0017).** `/connect/<token>` sits outside `BOOKING_BUDDY_ROOT` like `/s/<token>`, so `requiresSession` never gates it. The page only renders on GET; Accept/Decline is a POST to `respondToConnectionRequestAction` (`actions/connection-invites.ts`, no `verifySession`), which runs through the admin client — the RLS "only the addressee answers" policy still guards the signed-in Friends-page path. The token is valid only while `pending` and burned (`consumed_at`) on first use; the requester can never read it (no `authenticated` grant), so "mutual accept" is untouched.
- **Reuses the Reminder delivery stack, no new env var.** `connection-request-notify.ts` mirrors `send-reminders/route.ts`: `RESEND_API_KEY` + `REMINDER_FROM_EMAIL`, `supabase.auth.admin.getUserById` for the address, best-effort (every failure is a logged `return`, the whole body wrapped — it's always called from `after()`). The email copy (`connection-request-email.ts`) is pure and HTML-escaped, same discipline as `formatReminderEmail`; `escapeHtml` lifted into its own `escape-html.ts` so there's one copy.
- **Settings section "Reminders" renamed to "Notifications"** — a friend-request email isn't a reminder — with `ConnectionRequestPreferenceForm` added as a fourth row.

**Settings "Notifications" card: one Save button, push moved to the top.** The three email opt-ins (attendee Reminder #11, Booking Window Reminder #36, friend-request email #228) were three separate `<form>`s each with its own "Save" — now one `NotificationPreferencesForm` with a single Save that upserts all three columns in one write (`updateNotificationPreferences` replaces the three per-column actions). They stay independent settings: the form submits every checkbox's current state on each save, so flipping one leaves the others as they sit. "Push me a reminder on this device" (`PushNotificationsForm`, still its own toggle-saves-immediately control) now renders first in the card instead of last. `e2e/settings.spec.ts`: new test asserts the single Save + push-first ordering; the independence specs still pass unchanged.
- Verified: 615/615 `node --test` (5 new in `connection-request-email.test.ts`, 2 new in `routes.test.ts`), full pgTAP incl. 9 new assertions in `connection_request_links.test.sql` (trigger mints one token per pending insert; `authenticated` denied select/update; one-per-Connection; column default). `npx tsc --noEmit` / `npm run lint` / `npm run build` clean. New `e2e/connection-request-email.spec.ts` (accept from a logged-out browser connects both sides and the link is single-use; decline removes the request and the pair can retry; an unknown token shows a friendly dead end) and `e2e/settings.spec.ts` extended for the new toggle. Two `e2e/friends.spec.ts` specs fail on this machine for a pre-existing reason unrelated to this ticket — the suite's `disconnect` UI helper can't clear an accepted friendship through the base-ui confirm dialog here; verified identical failures with all of this ticket's changes stashed. Migration applied local; since `db push`ed to the hosted project.

**[#266](https://github.com/AdrianLuk/juice-bros/issues/266) (bookings and game proposals that cross midnight) is shipped.** Games run 9pm–midnight or 10pm–1am, and Booking Buddy refused any window whose end time wasn't strictly after its start on the same calendar day. Now a Booking or a Slot may span a single midnight: the form still takes one start date + start time + end time, and an end at or before the start reads as landing on the next day.

- **No migration.** The `booking_ends_after_start` / `slot_ends_after_start` check constraints compare instants, and the write path (`bookingInstants` in `actions/bookings.ts`, an inline branch in `createSlot`) now builds the end instant on `nextCalendarDate(parsed.date)` when the range crosses midnight, so the constraint is satisfied. The `*_not_in_the_past` triggers only look at the start, so they're untouched.
- **`crossesMidnight(startTime, endTime)`** (`datetime.ts`) — `endTime < startTime` — is the one predicate both actions and the duration picker share. `addHoursToTime` stopped returning `null` at `23:00` and now wraps (`22:00` + 3 → `01:00`); it still refuses a count of 24h or more, since a range can't lap its own start. `parseNewBooking` / `parseNewSlotProposal` now only reject `endTime === startTime` (a zero-length window), not `endTime <= startTime`.
- **UI:** the duration picker's "That runs past midnight" alert became a "more than a full day" alert (only reachable now with a hand-typed 24h+ count, which the `max={23}` input already blocks), and the computed End field shows a muted "Next day" line when the window wraps. `useDurationInput` exposes `endCrossesMidnight`. `EditBookingForm` derives its initial duration from the wrapped gap (`rawHours > 0 ? rawHours : rawHours + 24`) so a past-midnight Booking pre-fills "Custom" with e.g. 4 hours, not -20.
- Verified: 697/697 `node --test` (new cases in `datetime.test.ts` for the wrap + `crossesMidnight`; the `bookings.test.ts` / `slots.test.ts` "cannot end before it starts" tests split into a past-midnight-is-fine case and a zero-length-is-refused case). `npx tsc --noEmit` (booking-buddy files clean; the pre-existing `PageProps` errors in `podcast/[slug]` and `tools/[slug]` need a `next build` to generate `.next/types` and are unrelated) / `npm run lint` clean. `e2e`: the two "duration that would run past midnight is refused" specs (`bookings.spec.ts`, `dashboard.spec.ts`) became "can be logged" specs; new past-midnight-game spec in `slots.spec.ts`; `selectDuration` helper handles the wrap. Full `test:e2e` not run this session.

**Connection Accepted Email — the requester learns their friend request went through.** The friend-request email (#228) covered the addressee; the person who sent the request still had to keep checking `/booking-buddy/friends` to see if it landed. Now they get an email the moment it's accepted, on either accept path.

- **New migration `20260901140000_connection_accepted_email.sql`** — one column, `notification_preferences.connection_accepted_email_enabled` (its own opt-out, independent of `connection_request_email_enabled` and the two Reminder toggles, default on). No new table or trigger: the Connection already exists and already carries `requester_id`.
- **`notifyConnectionAccepted(connectionId)`** in `connection-request-notify.ts`, next to `notifyNewConnectionRequest` and built the same way — admin client, `getUserById` for the requester's address, preference check, best-effort (every exit a logged `return`, whole body wrapped, always called from `after()`). Wired into both accept paths: `acceptConnectionRequest` (signed-in Friends page) and `respondToConnectionRequestAction` (session-less `/connect/<token>` link — `respondToConnectionRequest` now also returns `connectionId`). The shared `loadRequesterLabel` helper is now `loadPersonLabel` since it resolves either party.
- **Email copy** `connection-accepted-email.ts` — pure, HTML-escaped, same table layout as the other two BB emails. One plain link to the Friends page; nothing to action from the inbox.
- **Settings:** a fourth checkbox in `NotificationPreferencesForm` ("Email me when someone accepts a friend request I sent…"), `updateNotificationPreferences` now upserts all four columns.
- Verified: `node --test` green (3 new in `connection-accepted-email.test.ts`), `connection_request_links.test.sql` extended (+2 assertions: column exists, defaults on), `e2e/settings.spec.ts` extended (defaults enabled; turning it off sticks independently). `npx tsc --noEmit` clean for booking-buddy files (the pre-existing `PageProps` errors in `podcast/[slug]` / `tools/[slug]` are unrelated, need `next build`). Migration applied local; since `db push`ed to the hosted project.

**[#284](https://github.com/AdrianLuk/juice-bros/issues/284) (Sync CourtReserve mail from an Outlook inbox — spec #280's third slice) is shipped.** "Sync from Email" now works end to end for an Outlook / Hotmail Mailbox Link, behaving identically to the Gmail path — same review screen, same confirm / dismiss / apply, no changes to the parser, review module, or matching.

- **Microsoft Graph transport.** `mail-adapters/microsoft.ts`'s `searchMailbox` / `fetchMessage` are now implemented (they returned `unreachable` since #283). Search runs `GET /me/messages` with a `$filter` on the CourtReserve sender + a received-after date, `$select=id`, `$orderby=receivedDateTime desc`, `$top=50`, following `@odata.nextLink` up to 5 pages. Fetch selects `subject,receivedDateTime,body` with `Prefer: outlook.body-content-type="html"` so Graph returns the stored HTML directly. A Graph `429` (or any non-`ok`) is a transient `unreachable` with no retry.
- **New `mail-adapters/graph-query.ts`** (relative-import-only, `node --test`) — `buildGraphMessagesFilter` / `buildGraphMessagesUrl` / `parseGraphMessagesPage` / `collectGraphMessageIds` (the `@odata.nextLink` loop, dependency-injected so it stays pure). 8 new tests; the Gmail counterpart split (`gmail-query.ts`) is the model.
- **Sync path is now provider-generic.** `syncFromEmail` dropped its `link.provider !== "google"` guard; its allowlist re-check is now Gmail-only (an Outlook link has no allowlist — ADR-0018). The confirm / cancel / update / dismiss actions record `processed_messages` under the link's actual provider, not a hardcoded `"google"`, via a new `authorizeEmailSyncForCaller` helper that also gates a Gmail link on the allowlist and an Outlook link on nothing more than existing. The Bookings-page "Sync from Email" section now renders for any connected Mailbox Link (or a Gmail-eligible User), not just Gmail; the Settings "Outlook syncing is still being finished" note is gone.
- **Analytics** `analytics.ts` gains `trackEmailSyncEvent(event, provider, …)` — `bb_email_sync_run` (per successful sync, with candidate count) and `bb_email_sync_import` (per confirmed candidate), both carrying a `provider` dimension, emitted from `after()`.
- **ADR + runbook.** New [adr/0018](docs/adr/0018-email-sync-microsoft-provider.md) extends (does not supersede) ADR-0009, recording the `consumers` authority, `Mail.Read` scope, refresh-token rotation, `429` posture, and Gmail-only-allowlist decision; ADR-0009 gets a cross-reference note. New [microsoft-oauth-setup.md](docs/microsoft-oauth-setup.md) mirrors the Gmail runbook: Azure app registration, `consumers` account type, redirect URI, client-secret creation + its ~24-month expiry as a recurring renewal item, publisher verification, Vercel env vars + the preview-deployment redirect-URI caveat. `.env.example` points at it.
- **Privacy page** — the "Sync from Email" disclosure now renders for an Outlook-link owner too (not just allowlisted Gmail Users) and names the connected provider.
- **e2e** — `MicrosoftMock` gains Graph list/get endpoints + `registerMessages` (shared `MockGraphMessage` shape). The Gmail-only sync scenarios in `email-sync.spec.ts` were factored into `support/sync-from-email-scenarios.ts`, which both `email-sync.spec.ts` (Gmail, Ben) and the new `outlook-sync.spec.ts` (Outlook, Amy — proving no allowlist) call — 8 shared scenarios each. Real credentials for the Microsoft OAuth client still need a human (Azure app registration + Vercel env vars); until then "Connect Outlook" doesn't render.
- Verified: `node --test` 746 green (+8 `graph-query.test.ts`); `npx tsc --noEmit` (after `next typegen`) + `npm run lint` clean; `e2e` — `email-sync` + `outlook-sync` + `outlook-connect` (25) run green PW-managed. Full suite: see below.

**[#286](https://github.com/AdrianLuk/juice-bros/issues/286) (re-import a CourtReserve email after its confirmed Booking is deleted) is shipped.** Before this, confirming an Import Candidate recorded a `processed_messages` row that filtered the email out of every later sync *regardless of outcome* — so deleting that Booking in the UI left a ledger row pointing at nothing, and the only way back was hand-editing prod. Same gap for an applied Reservation Update. `dismissed`/`cancelled` rows are unaffected and still suppress their emails for good.

- **New migration `20260901160000_processed_messages_booking_fk.sql`** — one nullable column, `processed_messages.booking_id uuid references bookings(id) on delete cascade`, plus its index (the referencing side of a cascade, which Postgres doesn't index for you) and a rewritten table/column comment. Reconcile-on-delete is *entirely* the FK cascade: deleting a Booking drops its `confirmed`/`updated` ledger row, so the next sync sees that email as fresh — **no app code in the delete path** (`deleteOwnedBooking` / `syncFromEmail` both untouched). Forward-only: pre-migration rows keep a null `booking_id` and stay permanently filtered exactly as before, no backfill.
- **`insertValidatedBooking` (`bookings.ts`) now returns `{ ok: true, bookingId }`** — it already `select("id")`d the row, it just wasn't handing the id back. Return type widened to `ActionResult & { bookingId?: string }`; a Players-only failure still returns `{ error }` with no id. `createBooking` is unaffected (it returns the result straight through).
- **`confirmImportCandidate` and `confirmUpdateCandidate` (`email-sync.ts`) set `booking_id` on their ledger insert** — the new Booking's id, and the matched Booking's id respectively. `dismissReviewItem` (never had one) is unchanged.
- **`confirmCancellationCandidate` re-records the superseded confirmation.** Confirming a cancellation also deletes the matched Booking, which would cascade away its `confirmed`/`updated` ledger rows and re-open the original confirmation email — wrong here (a cancelled reservation isn't something to re-import; only a *UI* delete means "offer it again"). It now reads those rows' message ids before the delete and re-inserts them as `cancelled` afterwards. INSERT-only, so the rows are replaced, not edited — the "no update grant" invariant holds. (Found by the pre-merge `/code-review`.)
- **Glossary** — CONTEXT.md's Import Candidate entry now records the reconcile-on-delete behaviour, why auto-resurface is intentional (the realistic path back to a deleted confirmed Booking is recovery; a deliberate delete just Dismisses the re-offered email once), and the cancellation carve-out.
- **Tests** — `processed_messages.test.sql` (+3): the `booking_id` column; a cascade test (insert Booking + confirmed row, delete Booking, assert the row is gone); and the cancel-then-re-record sequence `confirmCancellationCandidate` runs (capture → cascade-delete → re-insert as `cancelled`, assert it sticks). The "no update grant" note is reworded (an outcome is never *rewritten*, only cleared by the cascade). One new shared e2e scenario in `support/sync-from-email-scenarios.ts` (runs for Gmail and Outlook): import a confirmation → delete the Booking from the Bookings page → the next sync re-offers the email. New `deleteBooking` helper in `support/places.ts`.
- Verified: `node --test` 746 green (no new pure logic — the change is schema + glue); pgTAP 371 green (+3 in `processed_messages.test.sql`); `npx tsc --noEmit` + `npm run lint` clean. `e2e` — the Sync-from-Email block is ambiently flaky under load (sign-in / navigation races, the #279 family — failures land on different, including pre-existing, tests run to run); `email-sync` (14/14) + `outlook-sync` (9/9) + `bookings`/`dashboard` (16/16) all run green PW-managed. Migration `db push`ed to the hosted project 2026-09-01. Merged as [#299](https://github.com/AdrianLuk/juice-bros/pull/299).

**[#294](https://github.com/AdrianLuk/juice-bros/issues/294) (Calendar Feed: sync action + import review composition — backend — #288's Calendar Feed series, PR 4) is shipped.** The backend-complete Calendar Feed import path: set/clear the feed URL, `syncFacilityFeed(s)`, the `reviewCalendarFeed` composition, and the SSRF-hardened fetch. `import` kind only — the feed-diff cancellation mechanism and its four safety rails are a later ticket. No user-facing screens (the next ticket); verified end to end via an integration spec against a local ICS mock, driven through a test-only bridge route.

- **`calendar-feed-url.ts`** (pure, relative-import-only) — `validateFeedUrl` / `isCourtReserveHost` / `isAllowedFeedHost`: `https:` only (an `http:` URL is accepted **only** for a host on the test-only `CALENDAR_FEED_ALLOWED_HOSTS` widening — the mock, same `https:`→`http:` downgrade `GMAIL_API_BASE_URL` carries), host an exact suffix-match on `courtreserve.com` or a subdomain, case-insensitive. Lookalikes (`evilcourtreserve.com`, `courtreserve.com.attacker.net`) rejected. No rejection reason ever echoes the URL — it carries a private member token.
- **`calendar-feed-client.ts`** (`server-only`, seam-only, not unit-tested) — `fetchCalendarFeed(rawUrl)` with every SSRF guard: host + scheme re-validated at fetch time (not trusted from save time), `redirect: "manual"` (a 3xx is a failure, not a hop), `AbortSignal.timeout`, a 5 MiB response-size cap read in bounded chunks, `credentials: "omit"` and only an `Accept` header, and no URL in any log line or returned `reason`.
- **`calendar-feed-review.ts`** (pure, `node --test`) — `reviewCalendarFeed({ events, org, existingBookings, seenEvents, now })` → `{ items, autoLinked }`. Per-Org (no `matchOrgByName`), no player matching (a member feed has none). Reuses the #291 shared helpers (`splitOverlongCourtLabel` / `stripCourtLabelPrefix` / `isDuplicateBooking` / `isPastConfirmation`). Future-dated, unmatched, non-dismissed event → `import` candidate; an event matching a Booking (same Org, court, date/time, however that Booking was made) → `autoLinked`, filtered from the list; a cancelled event → neither (the diff's job).
- **`actions/calendar-feed.ts`** — `setCalendarFeedUrl` (validates at save time, stores ciphertext via `encryptRefreshToken` + the existing `MAILBOX_LINK_ENCRYPTION_KEY`), `clearCalendarFeedUrl` (nulls the column **and** purges that Org's `org_feed_events` rows), `syncFacilityFeeds()` / `syncFacilityFeed(orgId)` (decrypt → hardened GET → `parseCourtReserveFeed` → `reviewCalendarFeed` → upsert the seen-set → collect candidates; per feed, sequential, one failing reported per-Facility and never aborting the rest; same status-envelope shape `syncFromEmail` returns), `confirmFeedCandidate` (reuses `parseNewBooking` + `insertValidatedBooking`, writes the `org_feed_events` row `imported` + linked), `dismissFeedCandidate` (writes `dismissed`). Not allowlist-gated.
- **Confirm-time duplicate guard** — `confirmFeedCandidate` **and** `confirmImportCandidate` (email) now re-run the duplicate-Booking check at confirm time, against the caller's Bookings as they are then. Confirming an email candidate and then a feed candidate for the same slot in one session (the feed review ran before the email confirm's Booking existed) yields one Booking; the reverse direction too.
- **`readCalendarFeedAllowedHosts`** (`env.ts`) — the test-only `CALENDAR_FEED_ALLOWED_HOSTS` reader, a parsed host list (mirrors the `GMAIL_API_BASE_URL` reader). `.env.example` documents it. New `FacilitySyncEvent` analytics (`bb_facility_sync_run` / `bb_facility_sync_import`).
- **Mock + bridge** — `e2e/support/calendar-feed-mock.ts` (a fixed-port `.ics` server, port 5605, mirroring `gmail-mock.ts`; simulates 5xx / empty / malformed / oversized / redirect, and asserts no `Cookie`/`Authorization` header ever arrives). `src/app/api/e2e/calendar-feed/route.ts` — a test-only bridge (404 unless `E2E_WEB_SERVER=1`, same guard as `/api/e2e-preflight`) letting the spec drive the real actions with the browser's session, since the UI is deferred. Delete both when the Calendar Feed screen ships. `playwright.config.ts` sets `CALENDAR_FEED_ALLOWED_HOSTS=127.0.0.1`.
- **Tests** — `calendar-feed-url.test.ts` (13), `calendar-feed-review.test.ts` (9), 2 new `readCalendarFeedAllowedHosts` cases in `env.test.ts`. `e2e/calendar-feed.spec.ts` (8): set-URL → sync → confirm → Booking → auto-link on resync; dismiss → resync; one feed failing among many; empty/malformed body; redirect not followed; clear purges the seen-set; email-then-feed and feed-then-email dedup.
- Verified: `node --test` 887 green (+24); `npx tsc --noEmit` (after `next typegen`) + `npm run lint` + `npm run build` clean. `e2e/calendar-feed.spec.ts` 8/8 and `e2e/email-sync.spec.ts` 14/14 green PW-managed (the latter re-run because `confirmImportCandidate` changed). Full suite not run. No migration (schema was #293). Nothing to `db push`.

**[#293](https://github.com/AdrianLuk/juice-bros/issues/293) (Calendar Feed: schema + ADR + glossary — #288's Calendar Feed series, PR 1) is shipped.** The storage and the decision record for the whole feature. No behaviour — nothing reads or writes either of these yet (`reviewCalendarFeed` is PR 3, `syncFacilityFeed(s)` PR 4).

- **New migration `20260901210000_create_calendar_feed.sql`** — two pieces, no behaviour:
  - `orgs.calendar_feed_url` — nullable `text`, holds ciphertext only. The pasted CourtReserve club feed URL, encrypted at rest with the same utility and the same key as `mailbox_links.encrypted_refresh_token` (`token-encryption.ts`, AES-256-GCM, the existing `MAILBOX_LINK_ENCRYPTION_KEY` — the key's role simply widens; no new key, no env-var rename). Column comment spells out the ciphertext-only / server-decrypt-only discipline, matching `encrypted_refresh_token`'s.
  - `org_feed_events` — the per-feed seen-event history the cancellation diff will run against, shape verbatim from the spec: `id`, `owner_id → auth.users` (denormalized, `on delete cascade`), `org_id → orgs` (`on delete cascade`), `uid`, `sequence` (default 0), `starts_at`, `status` (`check in ('pending','imported','dismissed')`, no default), `booking_id → bookings` (`on delete set null`), `first_seen_at`/`last_seen_at` (default `now()`). A **dedicated table, not an extension of `processed_messages`** — that store is write-once (insert/select grants only) and the feed diff needs a mutable `last_seen_at` plus pruning. Unique `(owner_id, org_id, uid)` (its index's leading `(owner_id, org_id)` also serves the per-Org sync read); FK-side indexes on `org_id` (the orgs cascade) and `booking_id` (the bookings set-null). Coarse owner-only RLS (ADR 0003, same as `mailbox_links`/`bookings`), all-four-verbs `authenticated` grant. `assert_org_feed_event_coherent` trigger keeps the denormalized `owner_id` in step with the Org's owner *and* pins any linked `booking_id` to that same User's Booking in that same Org — the RLS-can't-cover-this gap `assert_booking_coherent` fills, both halves (the booking-link half added after a `/code-review` note that the row's own coherence check stopped short of it).
- **New [adr/0019-calendar-feed-second-import-source.md](docs/adr/0019-calendar-feed-second-import-source.md)** — extends (does not supersede) ADR-0002 (the feed is not an API, so ADR-0002's API ruling is untouched) and ADR-0009 (the feed is the second import source that ADR anticipated; the feed-diff mechanism answers its "a feed has no cancellation signal" objection). Records: per-Org (not account-wide) scope, second independent source (not a `MailAdapter`), encrypted-URL storage + key reuse, the feed-diff cancellation mechanism and its four safety rails (healthy-fetch gate / in-window-only / explicit-cancelled-is-unconditional / sanity cap), and on-demand-only for v1. ADR-0002 and ADR-0009 get cross-reference notes.
- **Glossary** — CONTEXT.md's new **Calendar Feed** entry, sibling to Mailbox Link (both a standing grant to an external source of Booking data; Mailbox Link is account-wide OAuth, Calendar Feed is per-Org URL). Folds in the `org_feed_events` seen-set and the cancellation-by-absence rule with its rails. `_Avoid_`: iCal Feed, ICS Feed, Feed Link, Calendar Subscription, Calendar Sync.
- **`.env.example`** — the `MAILBOX_LINK_ENCRYPTION_KEY` comment now names `orgs.calendar_feed_url` as the second secret it covers; the var name is unchanged.
- Verified: `npm run test:rls` green — `calendar_feed.test.sql` (39 new: the columns, the `(owner_id, org_id, uid)` uniqueness, the `status` check, `col_hasnt_default` on `status`, the three indexes, RLS reads/writes/silent-zero-row-delete plus a real cross-tenant `calendar_feed_url` check with a positive control, the coherence trigger on both `org_id` and `booking_id`, and all three FK behaviours — org cascade, owner cascade, booking set-null), full suite `Files=29, Tests=460`. `node --test` 821, `npx tsc --noEmit`, `npm run lint`, `npm run build` clean — no TypeScript touched. Migration applies cleanly on `supabase db reset`; **hosted `supabase db push` still pending** — additive (one nullable column, one new table), nothing deployed reads it, so it can't break what's live.

**[#292](https://github.com/AdrianLuk/juice-bros/issues/292) (`parseIcsFeed` — CourtReserve feed text to structured events — #288's Calendar Feed series) is shipped.** The pure ICS reader and its CourtReserve field mapping. No wiring yet: the HTTPS fetch with SSRF hardening (#293-ish, PR 4) hands the text in; `reviewCalendarFeed` (PR 3) shapes the events into Import Candidates.

- **New `ics-feed.ts`** (relative-import-only, `node --test`) — `parseIcsFeed(text, { fallbackTimeZone })` → `{ events, unreadableUids }`. A hand-rolled, deliberately partial RFC 5545 reader: line unfolding (CRLF / bare LF + space/tab continuation), top-level `VEVENT` extraction with any nested component (`VALARM`) skipped so no alarm prop leaks, property/parameter/value split with quoted params, TEXT unescaping (`\n` `\,` `\;` `\\`, left-to-right so `\\n` stays literal). Datetime forms: trailing `Z` → UTC instant; `TZID` → wall-clock through that zone, validated against `isKnownTimeZone` (unrecognised → the caller's zone); floating → the caller's zone; `VALUE=DATE` all-day → dropped. `DTEND`, or `DTSTART` + `DURATION`. `RRULE` / `RDATE` / `RECURRENCE-ID` dropped outright (no recurrence expansion — a recurring reservation is never an on-the-hour Booking). Never throws: a `VEVENT` with a UID but no usable event (malformed datetime, a start/end off the hour grid *in its own zone*, a missing required prop) goes to `unreadableUids` so the caller keeps it "seen" and a parse gap never diffs as a cancellation; a UID-less `VEVENT` is dropped silently. Output carries absolute ISO instants — the review composition projects them into the Org zone.
- **New `courtreserve-feed.ts`** (same discipline, no email-sync imports) — `mapCourtReserveFeedEvent` / `parseCourtReserveFeed`. The `SUMMARY` / `LOCATION` / `DESCRIPTION` mapping, **finalised against a real captured member feed** (Vaughan Pickleball, one account, eight reservations — club name and UID ids scrubbed for commit; ADR-0009 is why this waited for the real sample): `SUMMARY` is exactly `"Doubles"` / `"Singles"` → `format` (+ kept verbatim as `name`); `DESCRIPTION` is `"Court #10"` → raw `courtLabel` (the shared `stripCourtLabelPrefix` / `splitOverlongCourtLabel` run in the review composition, as on the email path); `LOCATION` is the club → informational `facilityName` (a Calendar Feed is already per-Org). No player data anywhere in the feed → `playerNames` always `[]`. `STATUS:CANCELLED` (absent on every real event) → `cancelled: true`, the unconditional cancellation signal (spec #288 rail 3).
- **Tests** — `ics-feed.test.ts` (29): the folding forms, every datetime form incl. a winter/summer TZID offset and a Newfoundland Org making an on-the-hour UTC time read as half-past, out-of-range dates/hours rejected rather than rolled forward, `VALUE=DATE` / `RRULE` kept-seen, `VALARM` non-leak, the never-throws / unreadable-but-seen / drop-silently branches, `STATUS` / `SEQUENCE` defaults. `courtreserve-feed.test.ts` (9): the real-feed fixture mapped to eight events end to end, plus the mapping units.
- **`/code-review` (high)** caught three, all fixed: `resolveDatetime` now range-validates the datetime components (a `20260230` Feb-30 no longer rolls to Mar 2 and imports as a real event — it goes to `unreadableUids`); `isOnTheHour` reuses `datetime.ts`'s `clockInZone` instead of a second copy of the `hourCycle: "h23"` formatter; all-day and `RRULE` events now carry their UID out in `unreadableUids` too (not silently dropped) so a feed that corrupts a real timed reservation into one of those shapes can't read as a cancellation.
- Verified: `node --test` 800 green (+38); `npx tsc --noEmit` + `npm run lint` + `npm run build` clean. No e2e — nothing is wired yet.

**[#291](https://github.com/AdrianLuk/juice-bros/issues/291) (extract shared Import Candidate shaping into a pure module — #288's Calendar Feed series) is shipped.** A pure prefactor, no user-visible change: the source-agnostic shaping helpers an Import Candidate review composition needs are pulled into one module so #288's upcoming `reviewCalendarFeed` can reuse them without importing the email-sync modules.

- **New `import-candidate-shaping.ts`** (relative-import-only, `node --test`) — `splitOverlongCourtLabel` + `stripCourtLabelPrefix` move out of `bookings.ts`, `isDuplicateBooking` + `isPastConfirmation` + the `BookingIdentity` type move out of `email-sync-matching.ts`. Function bodies and doc comments carried over verbatim; the `COURT_LABEL_MAX_LENGTH` / `NOTES_MAX_LENGTH` schema-mirror constants stay in `bookings.ts` and the new module reads them from there.
- **`email-sync-review.ts`** now imports the four helpers + `BookingIdentity` from the new module; the rest of its `email-sync-matching.ts` import (the CourtReserve-specific matching / reconciliation) is unchanged. `email-sync-matching.ts` drops its now-unused `isPastDate` import.
- **Tests moved with the code** — the `stripCourtLabelPrefix` / `splitOverlongCourtLabel` cases leave `bookings.test.ts`, the `isDuplicateBooking` / `isPastConfirmation` cases leave `email-sync-matching.test.ts`, all landing verbatim in the new `import-candidate-shaping.test.ts`. Each departure site left a one-line breadcrumb.
- Verified: `node --test` 757 green (same count — tests relocated, none added or dropped); `npx tsc --noEmit` (after `next typegen`) + `npm run lint` + `npm run build` clean. `e2e/email-sync.spec.ts` not run locally — the change is a pure module move with no behaviour, selector, or import-graph reachability change that `tsc` + `build` don't already prove; CI covers it.

**[#283](https://github.com/AdrianLuk/juice-bros/issues/283) (Connect an Outlook / Hotmail mailbox — spec #280's second slice) is shipped.** A User can now connect a personal Microsoft account as their Mailbox Link. Syncing an Outlook inbox is the third slice; this is the OAuth pipe + Settings UI only.

- **`mail-adapters/microsoft.ts`** — the placeholder's `buildAuthorizeUrl` / `exchangeCodeForTokens` / `refreshAccessToken` are now real: authorize against the `consumers` authority with `offline_access Mail.Read openid email`, auth-code exchange, refresh with a rotating refresh token. The connected account's email comes from the token response's `id_token` `email` claim (decoded, not signature-verified — same trust as reading `access_token` from the same body), so there's no Graph `/me` call. `resolveAccountEmail` / `searchMailbox` / `fetchMessage` stay `unreachable` stubs until the sync slice.
- **`TokenExchangeOutcome`** gains an optional `accountEmail` — Microsoft fills it from the `id_token`; Google leaves it unset and the callback falls back to `resolveAccountEmail`.
- **Unified callback route.** `settings/gmail-callback` → `settings/mailbox-callback`, one route for both providers. The provider rides in the OAuth `state` cookie value (`"<provider>:<nonce>"` — `encodeMailboxOAuthState` / `parseMailboxOAuthState` in `mailbox-oauth.ts`) alongside the CSRF nonce, read back to pick the adapter. The allowlist re-check forks on provider: Gmail-only, never Microsoft. Both providers register the same redirect URI.
- **`connectMailbox(provider)`** no longer rejects `"microsoft"`. Google keeps the allowlist gate; Microsoft is gated only on `MICROSOFT_OAUTH_CLIENT_ID` being set (a direct hit with it unset redirects to the connect-failure page rather than 500ing).
- **Settings "Sync from Email" section is visible to every User now** (was absent for non-allowlisted Users). When nothing's connected it shows stacked "Connect Gmail" (allowlisted only) / "Connect Outlook" (configured only) buttons, each with an inline brand-SVG icon and a one-line "you'll see an unverified-app screen, here's how to get past it" note. Connected/reconnect copy names the provider: "Connected as me@hotmail.com via Outlook", "Microsoft needs you to reconnect". `disconnectMailbox` still just deletes the row.
- **New env:** `MICROSOFT_OAUTH_CLIENT_ID`, `MICROSOFT_OAUTH_CLIENT_SECRET`, `MICROSOFT_API_BASE_URL` (test-only host override, mirrors `GMAIL_API_BASE_URL`). `.env.example` + the OAuth setup runbook's redirect-URI updated to `mailbox-callback`. **Deploy step:** any environment with a live Google OAuth client must add `<origin>/booking-buddy/settings/mailbox-callback` in Cloud Console (the old `gmail-callback` URI stops being used) — safe right now because the OAuth vars are still unset on Vercel.
- **Not in this slice:** the ADR extension and the Azure setup runbook (spec puts them with the sync slice), Microsoft mailbox *sync* (the Bookings-page "Sync from Email" section is now hidden for a microsoft link, and `syncFromEmail` guards against one; both flip in the sync slice), and the `account_email`-column / `processed_messages`-table renames (deferred by #281).
- Verified: `node --test` 738 green (new `mailbox-oauth.test.ts`, `mailbox-provider.test.ts`, Microsoft env readers in `env.test.ts`); `npx tsc --noEmit` (after `next typegen`) + `npm run lint` clean; `e2e` — new `outlook-connect.spec.ts` (5) + `microsoft-mock.ts`, `email-sync.spec.ts` updated for the always-visible section and neutral failure copy; `outlook-connect` + `email-sync` + `settings` specs run green (36) PW-managed. Full suite not run.

**[#282](https://github.com/AdrianLuk/juice-bros/issues/282) (MailAdapter seam + Google adapter + shared token-lifecycle helper — prefactor for #280's Outlook support) is shipped.** A pure prefactor, no user-visible change (error/reconnect copy is untouched — provider-named copy is #280's slice 2/3). The provider-specific transport now lives behind one interface.

- **New `mail-adapter.ts`** — the `MailAdapter` interface: `buildAuthorizeUrl`, `exchangeCodeForTokens`, `refreshAccessToken` (its `RefreshOutcome` carries an optional *rotated* `refreshToken` — Microsoft rotates on every exchange, Google never), `resolveAccountEmail`, `searchMailbox` (takes the neutral `{ sender, after }` `MailSearchCriteria`), `fetchMessage`. Every I/O method returns a discriminated `{ ok }` result, same posture the old Gmail client held.
- **`mail-adapters/google.ts`** — the old `gmail-client.ts` lifted out verbatim as `googleMailAdapter`, one function-object. Only behaviour difference: it formats the Gmail `q` string itself (`from:… after:YYYY/MM/DD`) from the criteria object rather than receiving a ready-made query. `gmail-client.ts` deleted.
- **`mail-adapters/microsoft.ts`** — a placeholder adapter whose every method throws; the `Record<MailboxProvider, MailAdapter>` literal in `mail-adapters/index.ts` needs the entry, but `connectMailbox` still redirects `"microsoft"` to the connect-failure page so none of it is reachable. Replaced in #280's Microsoft-connect slice.
- **`mailbox-token-lifecycle.ts`** — `resolveMailboxAccessToken({ supabase, ownerId, adapter, encryptedRefreshToken, encryptionKey })`: decrypt → `adapter.refreshAccessToken` → persist a rotated refresh token when one comes back → mark the link `expired` on `invalid_grant`. `syncFromEmail` now calls this instead of inlining the decrypt/refresh/expire dance; the callback route goes through `adapter.exchangeCodeForTokens` / `adapter.resolveAccountEmail`.
- **`courtreserve-email.ts`** — `buildCourtReserveSearchQuery` (returned a Gmail `q` string) → `buildCourtReserveSearchCriteria` (returns `{ sender, after: Date }`). `COURTRESERVE_SEARCH_WINDOW_DAYS` unchanged. The CourtReserve parser / review / matching modules are untouched (the `RawCourtReserveEmail.gmailMessageId` field name still stands — a later-slice rename).
- **`MailboxProvider`** moved to its own `mailbox-provider.ts` (was in the `"use server"` `actions/email-sync.ts`) so the seam and the callback route can name it; re-exported from `actions/email-sync.ts` for existing importers.
- Verified: full `node --test` (721) green; `courtreserve-email.test.ts`'s two search-query tests updated to assert the criteria object; `npx tsc --noEmit`, `npm run lint`, `npm run build` clean; `e2e/email-sync.spec.ts` 14/14 green unchanged (no selector changes needed — the Gmail HTTP mock endpoints are untouched).

**[#281](https://github.com/AdrianLuk/juice-bros/issues/281) (Mailbox Link schema + provider-neutral renames — prefactor for #280's Outlook support) is shipped.** A pure prefactor, no user-visible change: the Mailbox Link storage and every Gmail-specific name are generalised so a second provider (Microsoft) can be added in #280's later slices without touching these things again.

- **New migration `20260901150000_mailbox_link_provider_prefactor.sql`.** `mailbox_links.google_account_email` → `account_email`; a `provider` column (`check in ('google','microsoft')`) on both `mailbox_links` and the processed-messages table, backfilled to `google` via a column default that is then dropped so every future insert names the provider. `processed_gmail_messages` → `processed_messages`, its `gmail_message_id` → `provider_message_id`, its owner index, RLS policy, and the auto-named pkey/fkey/outcome-check constraints all renamed to match, and its uniqueness constraint widened to `(owner_id, provider, provider_message_id)` so opaque ids from different providers can't collide. RLS, grants, and the `status` / `outcome` value sets are unchanged.
- **Code renames.** `connectGmail` → `connectMailbox(provider)` (only `"google"` reachable today; `"microsoft"` redirects to the connect-failure page until #280's later slices wire it up), `disconnectGmail` → `disconnectMailbox`, `isEmailSyncAllowed`/`isEmailSyncAllowedForCaller` → `isGmailConnectAllowed`/`isGmailConnectAllowedForCaller` (the allowlist is Gmail-only by design now — spec #280), `GmailSyncSection` → `MailboxSyncSection`, `gmail-oauth.ts`/`GMAIL_OAUTH_STATE_COOKIE` → `mailbox-oauth.ts`/`MAILBOX_OAUTH_STATE_COOKIE`. The `MailboxLink` type gains a `provider` field and its `googleAccountEmail` → `accountEmail`. Settings query params `gmail_connected` / `error=gmail_connect_failed` → `mailbox_connected` / `error=mailbox_connect_failed`; the `email_sync_not_allowed` key stays (Gmail-only by definition). The OAuth callback route path (`settings/gmail-callback`) and the `RawCourtReserveEmail` / review-module `gmailMessageId` field are left for #280's later slices, which rework the callback and the adapter seam.
- **Glossary** — CONTEXT.md's Mailbox Link entry generalised from "Gmail inbox" to "email inbox (Gmail today; Outlook/Hotmail planned)".
- Verified: full `node --test` (721) green; `mailbox_links.test.sql` + renamed `processed_messages.test.sql` updated for the new columns, constraint, and dropped defaults, full pgTAP green (`Files=25, Tests=368`); `npx tsc --noEmit`, `npm run lint`, `npm run build` clean. Migration `db push`ed to the hosted project 2026-09-01.

**[#279](https://github.com/AdrianLuk/juice-bros/issues/279) (e2e: "Sync from Email" block hangs the dev server partway through) is fixed — and it wasn't the dev server.** `email-sync.spec.ts`'s two `afterEach` cleanups probed the page with a non-auto-waiting `disconnect.isVisible()` / `strays.count()` immediately after `page.goto`, racing the route's own `loading.tsx` skeleton. On a lost race the Gmail disconnect (or the stray-Org sweep) silently no-op'd, the leaked Mailbox Link row survived into the next test, and that test's "Connect Gmail" step then waited out its full 30s timeout against a Settings page showing "Disconnect" instead — the failure cascading down the rest of the block (exactly #279's "failing subset shifts run to run, after ~8 tests"). The `⨯ Error: The destination stream closed early.` log lines are benign navigation-abort noise, not a wedge — the server stayed responsive at ~250ms throughout. Fix: each cleanup now waits for a marker that renders only on the real page, never the skeleton (the "Sync from Email" heading on Settings; the "Your facilities" heading on `/orgs`) before probing. The full `email-sync.spec.ts` block now runs 14/14 green in 1.9m (was ~5.8m when thrashing).
